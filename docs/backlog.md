# Backlog — kullanıcı eylemi bekleyen işler

> **Bu dosya yalnız KULLANICININ yapabileceği işleri tutar** — fiziksel cihaz gerektiren
> ölçümler, hesap/abonelik kararları, sudo isteyen kurulumlar, dış servis kayıtları.
> Ajan bunları kodla kapatamaz.
>
> **Bu bir durum dosyası DEĞİL.** Projenin canlı durumu tek yerdedir:
> [docs/plan/state.md](plan/state.md) (CLAUDE.md §12). Buraya görev durumu, ledger
> satırı veya "sıradaki iş" yazma — çelişirler. Buradaki bir madde bir görevi
> bloklamıyorsa bunu açıkça söyler.
>
> Kullanıcı **"backlog ekle"** dediğinde madde buraya eklenir.

---

## Açık

### B1 — iOS Safari çerez ömrü ölçümü (Q11)

**Durum:** açık · **Bloklar:** hiçbir şeyi · **İlgili:** Q11, M5-01 (`a71e1b2`), M8-05

**Ne yapılacak.** Gerçek bir iPhone'da uçtan uca çerez ömrünü ölç:

1. Plakete dokun → Safari açılır → aktive ol (çerez yazılır).
2. **Günler/haftalar** bekle — arada Safari'yi normal kullan (ITP'nin tetiklenmesi için).
3. Aynı telefonla tekrar dokun → **hâlâ tanınıyor musun?**

**Neden önemli.** Ürünün "telefon seni tanır" vaadi buna dayanıyor. Çalışan uygulama
kurmuyor; tek kimlik kanıtı bu kalıcı çerez (§5'in "kim" kanıtı).

**Teknik olarak sınanan şey.** Safari ITP, **JavaScript ile yazılan** çerezleri 7 güne
kırpar. **Sunucunun `Set-Cookie` ile yazdığı `httpOnly` çerez** bu kırpmaya tabi
*olmamalı* — M5-01 tam olarak öyle yazıyor (`httpOnly`, `SameSite=Lax`,
`Max-Age=31536000`). Ölçüm bu ayrımın gerçekte tuttuğunu doğrular.

**M5-01'i bloklamadı, çünkü:** sunucu tarafı sonuçtan bağımsız — ölçüm ne çıkarsa
çıksın kod aynı. Bu yüzden bir kabul kriteri olamaz (kart düzeltmesi, 2026-07-31).
Sonuç çıkınca: [open-questions.md](plan/open-questions.md) → Q11'e yaz.

**Sonuç kötü çıkarsa** (çerez erken düşüyorsa) seçenekler: aktivasyonu daha sık
tekrarlatan bir akış, ya da çereze ek olarak ikinci bir tanıma kanıtı — **ama §4.1
gereği biyometri/WebAuthn/attestation değil.** Karar o zaman verilir.

### B3 — Saklama süresinin hukuki onayı (Q13)

**Durum:** açık · **Bloklar:** hiçbir şeyi (kod hazır) · **İlgili:** Q13, M5-02, M8-06

**Ne yapılacak.** `TAPPA_RETENTION_YEARS`'in **gerçek** değerini bir hukukçuya doğrulat
(Malta/AB istihdam + bordro/vergi saklama yükümlülüğü) ve `.env`'e yaz.

**Neden önemli.** Bu sayı **GDPR Art. 13 aydınlatma metninde çalışana gösteriliyor** —
yani hukuki bir beyan. Yanlışsa metin çalışana yanlış bilgi vermiş olur.

**Kod tarafı hazır ve bu yüzden bloklamıyor** (kullanıcı kararı 2026-07-31): sayı
**koda gömülmedi**, config'den geliyor; aydınlatma metni onu render ediyor. Dev/demo
değeri bir **varsayılan**dır, hukuki iddia değildir — kod bunu böyle söylüyor.
Doğru değer öğrenilince tek yapılacak `.env`'i güncellemek; sürüm çıkmaya gerek yok.

**Sonuç çıkınca:** [open-questions.md](plan/open-questions.md) → Q13'e yaz ve buradaki
maddeyi "Kapananlar"a taşı. Q13 ayrıca "GDPR silme talebi × immutable `transactions`"
sorusunu da taşıyor (M8-06) — saklama süresi onun bir parçası, tamamı değil.

### B2 — ~~arm64 Go toolchain kurulumu~~ → **DÜŞTÜ: BU MAKİNE arm64 DEĞİL** (Q26)

**Durum:** 🔴 **geçersiz, 2026-08-13'te ölçüldü** · **Bloklar:** hiçbir şeyi ·
**İlgili:** Q26, M0-07 · **Yerine geçen iş:** T31 (`go1.26.5 → 1.26.6`, güvenlik)

**Neden düştü.** Maddenin dayandığı önerme yanlıştı. Ölçüm (M7-03 A, 5. tur denetimi,
sonra orkestratör tarafından doğrulandı):

```
uname -m                      -> x86_64
hw.model                      -> MacBookPro16,1
machdep.cpu.brand_string      -> Intel(R) Core(TM) i9-9980HK CPU @ 2.40GHz
kernel                        -> RELEASE_X86_64
hw.optional.arm64 / sysctl.proc_translated  -> OID MEVCUT DEĞİL
file $(which go)              -> Mach-O 64-bit executable x86_64
go env GOARCH                 -> amd64
```

Yani makine **Apple Silicon değil** ve **Rosetta altında da değil** — Intel bir
MacBook Pro. `go` zaten **yerel** koşuyor. *"Rosetta altında ~2-3× yavaş"* gerekçesi
ölçülmemiş bir varsayımdı; kurulacak arm64 tarball'ı bu donanımda **hiç çalışmaz**.

⚠️ **Aynı yanlış inanç iki yerde daha yazılı ve düzeltilmeli:**
`docs/plan/m0-bootstrap.md:704-706` (*"`uname -m` → arm64"*, *"toolchain tamamen
Rosetta altında"*) ve `internal/adminauth/password.go:43`'ün zamanlama tablosunun
`darwin/arm64` etiketi (`4bc2e72`, 2026-08-03'ten beri). Sonuncusu M7-03 A'da
düzeltildi; ilki hâlâ açık.

**Ders.** Donanım iddiası da bir **ölçümdür**. Bu madde beş oturum boyunca *"kullanıcı
şunu kurmalı"* diye taşındı ve kimse `uname -m` yazmadı — maddeyi kapatan komut
**tek satır** ve **sıfır saniye** sürdü.

---

## Ertelenmiş teknik borç (sahibi bir görev kartı DEĞİL)

> Bunlar **kullanıcı eylemi değil**, ama hiçbir yakın görev kartının da sahiplenmediği işler —
> çoğu dağıtım (M8) zamanına ait. Buraya yazılmalarının sebebi kaybolmaları: M8 uzak.
>
> ⚠️ **Görev-kapsamlı devirler buraya YAZILMAZ** — onlar [state.md](plan/state.md)'deki
> "M5-04/M5-05'e devralınan" gibi bölümlerde ve sahibi bellidir (ör. 429'un §4.6 kalıntısı → M5-04,
> N5 tenant-mismatch → M5-05, font self-host → M5-04). İki yere yazılan gerçek çelişir.

| # | Ne | Nereden | Sahibi |
|---|---|---|---|
| **T1** | **Oran sınırlayıcı süreç-içi ve sabit-pencere.** İki instance sınırı ikiye katlar; pencere sınırında kısa sürede 2×limit mümkün; `limiterMaxKeys` (100k) aşılınca map **toptan sıfırlanıyor** (fail-open, bilinçli ve yazılı). Paylaşılan store gerekir. | M5-02, M5-03 | M8 |
| **T2** | **`TAPPA_TRUSTED_PROXIES` dağıtım kontrol listesi.** Proxy XFF'e **append** etmeli — **replace** eden proxy koddan ayırt **edilemez**. Kapı yalnız tek girdilik `/0`'ı yakalar; `/1` veya birleşimle tüm uzayı kaplayan liste (`0.0.0.0/1,128.0.0.0/1`) yakalanmaz. Güvenilen aralık **gerçek istemcileri içeriyorsa** onlar serbestçe IP uydurabilir (ölçüldü). | M5-03 | M8 |
| **T3** | **Çerez gölgeleme + `__Host-` öneki.** Aynı isimli iki `tappa_activation` çerezinde `r.Cookie` **ilkini** alıyor; alt alan adı kontrolü gerektirir. | M5-02 | M8 |
| **T4** | **`docker-compose.yml` `log_statement=all` bind parametrelerini log'a yazıyor** (denetçi 30 dk'da `$1 = '<64-hex>'` biçiminde **1036 satır** saydı). `code_hash` taşıyıcı kimlik bilgisi olduğu için dev Postgres log'u bir aktivasyon anahtarı deposuna dönüşüyor. **Dev-only** ve denetçiler bu log'u ölçüm aracı olarak kullanıyor → bilinçli bırakıldı; üretim deploy config'i repoda henüz yok. | M5-02 | M8 |
| **T5** | **`config.Load` `TAPPA_BASE_URL`'ü doğrulamıyor.** `NewCookies` bir **prefix testi** yapar, URL parse etmez → başında boşluk olan veya URL olmayan değer NOT-Secure dalına düşer (**non-prod'la sınırlı**; prod koşulsuz Secure). | M5-01, M5-02 | M5-03 sonrası / M8 |
| **T6** | **Dev-DB test kalıntısı birikiyor.** `tenants` ~7250 satır; `audit_log`/`transactions` append-only olduğu için **tasarımca** temizlenemez (M1-09: imkânsızlık = garanti). Kırmızı çizgi değil, hijyen. Demo/prod öncesi `make db-reset`; kalıcı çözüm testcontainers ile izole DB. | M3-02, M5-03 | M8 |
| **T7** | **`aes_key_ref` KEK zarfı ŞEMA TARAFINDAN ZORLANMIYOR — ve M6-06 B ona bir EKRAN YÜZÜ verdi.** Seed yarısı M5-09'da kapandı (gerçek 44 baytlık zarf + drift guard). Açık kalan: şema `bytea`ya *"bu bir KEK zarfıdır"* dedirtemiyor, yani **insert yolu** disipline bağlı. ⚠️ **YENİ (güvenlik merceği, 2026-08-10):** panelin ***"Encoded by Tappa"*** etiketi `location_id IS NULL`'a bakıyor, **anahtara DEĞİL**. Ölçüldü: 2 baytlık bir `aes_key_ref` taşıyan satır kartta **"Encoded by Tappa"** gösteriyor, ve o plakete NFC tap → **500** ve `transactions`'a **hiçbir satır yazılmıyor** (sayaç 1→1) — **o yolda bir §4.6 deliği**. Panelin kusuru **değil** (panel öyle bir satır yaratamaz; `tags` INSERT'i yok → **T16**), ama ekran artık **doğrulamadığı bir şeyi adlandırıyor**. Dağılım: **44 → 55.689 · 2 → 19.783 · diğer → 3.873**; `CHECK (octet_length = 44)` bugün **21.985** satırı ihlal eder → yine `NOT VALID` + yedi fixture'ın gerçek zarfa çevrilmesi. Üç seçenek M5-09 kartında. ⚠️ **Vade sütunundaki "plaket yükleyici" 2026-08-20'de TASARLANDI** ([ADR 0017](adr/0017-encode-rolesi-ve-yarim-yazma-kurtarmasi.md) §3): yükleyici `sun.Wrap` ile **tam 44 baytlık** zarfı yazacak, yani bu maddenin dayandığı *"disiplin"* artık **yazılı**. Ama madde **kapanmıyor** — şema hâlâ zorlamıyor, ve yükleyici `tappa_app` ile bağlanacağı için (§3.1) tablo geneli INSERT hakkı da duruyor (**T16**). Yükleyici **M8-05 FAZ B2**'de kodlanıyor. | M1-05, M5-09, M6-06 B | M8-05 FAZ B2 (plaket yükleyici) |
| **T8** | **🟡 BÜYÜK ÖLÇÜDE KAPANDI — 00013 (`ba671b0`, 2026-08-09).** `CHECK (uid ~ '^[0-9A-F]{14}$')` sevk edildi; küçük harfli INSERT **reddediliyor**, iki-yazım/tek-AAD sınıfı **yeni satırlar için kapalı**. 🔴 **AMA BU MADDENİN ÖNCÜLÜ YANLIŞTI VE ÖLÇÜMLE ÇÜRÜTÜLDÜ:** *"mevcut 12 satır zaten büyük harf → veri taşıması yok"* — gerçek **18.010 küçük harfli** satır, ve **12.437'si `transactions`'ın 24.874 satırından** referanslı; `transactions` **DB düzeyinde append-only** (tetikleyici `tappa_owner`'ı da bağlıyor) → normalleştirme **§4.3 ihlali** olurdu. Bu yüzden kısıt **`NOT VALID`**: artık satırlar **donuyor** (her UPDATE 23514) ve **tap yolu onlara ULAŞAMIYOR** (`sun.Parse` daima büyük harf üretir, `char(14)` karşılaştırması harfe duyarlı — güvenlik merceği ayrıca **ulaşan yol aradı, bulamadı**). Kirlenmenin **kaynağı kapatıldı** (`ToUpper`'sız iki test yardımcısı; iki tam koşumdan sonra sayı **artmadı**). **KALAN İŞ → T15.** | M5-09 · 00013'te kapatıldı | — |
| **T9** | **✅ KAPANDI — 00013 (`ba671b0`, 2026-08-09).** `REVOKE UPDATE ON tags` + `GRANT UPDATE (location_id, last_ctr, status, retired_at, replaced_by)` + monotonluk trigger'ı (`tappa_owner`'ı da bağlıyor). Ölçüldü: `aes_key_ref` ve `uid` UPDATE'i → **42501**, rewind → **23001** (her iki rol için), advance ve `last_ctr`'a dokunmayan retire → **UPDATE 1**. 🔴 **BU MADDENİN İKİ CÜMLESİ DE YANLIŞTI:** (a) önerilen trigger koşulu `NEW.last_ctr > OLD.last_ctr` **`last_ctr`'a dokunmayan bir retire'ı REDDEDİYOR** (canlı hata mesajıyla gösterildi) — doğrusu `< OLD` ise `RAISE`; (b) sütun listesinde **`location_id` yoktu**, ama envanter modeli **bağlamayı** istiyor ve bağlama `location_id` UPDATE'idir → *aynı günün iki kullanıcı kararı birbirini uygulanamaz kılıyordu*. **Ders: bir borç maddesinin önerdiği ÇÖZÜM de, öncülü kadar ölçülmelidir.** | M5-09 · 00013'te kapatıldı | — |
| **T10** | **🔴 CI OLDUĞU GİBİ KIRMIZI VERİR VE BUGÜNE KADAR KİMSE GÖRMEDİ.** Repoda **uzak yok** (kullanıcı kararı: push/PR yok) → `ci.yml` **hiç çalışmadı**; "CI yeşil" teorik bir cümle. Olduğu gibi koşarsa: workflow `make up` koşuyor (yalnız Postgres'i **başlatır**) ama **`make migrate` koşmuyor**, `make seed` koşmuyor, `TAPPA_TAG_KEK` **vermiyor** → `make check` → `make test` migrate edilmemiş şemaya çarpar. Ölçüldü (boş DB): **17 pakette 140 üst düzey FAIL**, 136'sı M5-09'dan **eski** (M1-09'dan beri taşınıyor). Düzeltme tek satır değil (migrate + KEK + seed) ve **uzak olmadan doğrulanamaz**. O zamana kadar `make check`'in tek gerçek koşum yeri geliştiricinin makinesidir. | M5-09 güvenlik denetimi | M8 / M0-06 |
| **T11** | **✅ KAPANDI — 00013 (`ba671b0`, 2026-08-09).** `transactions(tenant_id, location_id)` eklendi: dört sayım **indekssiz 30–75 ms → indeksli 5–9 ms**, `transactions` bacağı **Bitmap Heap Scan → Index Only Scan**; bedel **~4,5 MB**, yazma maliyeti **üç eşleştirilmiş 20.000-INSERT örneğiyle** ölçüldü, anlamlı yavaşlama yok. ⚠️ Beklenmedik ikinci kazanç: `ListTagLastSeen` de bu indeksi **tenant filtresi** olarak kullanıyor (düşünce plan `transactions_tenant_occurred_idx`'e düşüp **69.575 indeks satırı** okuyor). ⚠️ **Bu 00013'ün tek performans parçası ve hiçbir test onsuz kırılmıyor** — migration'da yazılı. | M6-06 A · 00013'te kapatıldı | — |
| **T12** | **MAC'li onay jetonu TEK KULLANIMLIK DEĞİL.** Aynı jetonla ikinci POST yine geçiyor (ölçüldü; bir denetim tek mint'i **üç kez** harcadı). Bugün zararsız, çünkü her kapının **kendi** ikinci savunması var: deaktivasyonda `status <> 'deactivated'`, kaldırmalarda **hiçbir satırla eşleşmeyen ikinci DELETE**. ⚠️ Bu, M6-06 A'da *"silme onayını imzala"* seçeneğinin **elenme gerekçesiydi** — imzalı bir iddiayı TTL içinde yeniden harcanabilir kılmak kusuru kapatmaz, **taşır**. Kapatmak harcanmış-değer tablosu **artı kendi saklama hikâyesini** ister → ayrı görev. `deactivateconfirm.go` bunu 12 parçalık listesinde **açık limit** olarak sayıyor. | M6-05 B, M6-06 A | M7 |
| **T13** | **`tappa_owner` `audit_log`'u TRUNCATE edebiliyor.** Append-only tetikleyici `BEFORE UPDATE OR DELETE` — **TRUNCATE'i kapsamıyor** (`tappa_app` edemiyor: yalnız SELECT+INSERT). 00005'in durumu, bu fazın değil; **ama bedeli M6-06 A ile değişti**: `audit_log` artık silinmiş bir lokasyonun **tek hayatta kalan kaydı** ve C′ silme bildiriminin **tek dayanağı**. Bir truncate onay **uydurmaz** — hepsini **susturur** (ekran zaten "hiçbir şey söyleme" yönüne düşüyor). | M5-09, M6-06 A güvenlik denetimi | M7/M8 |
| **T14** | **Onay jetonunu ÜRETEN GET, Origin kapısının dışında.** `?venue=<id>&confirm=remove` çapraz-origin bir üst düzey gezinmede jetonu basıp `Set-Cookie` yazıyor (`tappa_admin_confirm`, HttpOnly, SameSite=Lax, Path=/admin). Saldırgan gövdeyi **okuyamaz** (jetonu öğrenemez) ve POST Origin kapılı; kalan risk yöneticiye istemediği bir *"Remove X?"* uyarısını **gösterebilmek**. **Bu faza özgü değil** — M6-05 `employees.go` aynı şekli sevk etmişti. | M6-05 B, M6-06 A güvenlik denetimi | M7 |
| **T15** | **`tags_uid_canonical_hex` HÂLÂ `NOT VALID` — ve bunu tamamlamak veri temizliği ister.** 00013 kısıtı `NOT VALID` sevk etti (gerekçe T8'de: normalleştirme §4.3'e çarpıyor). Yeni satırlar **denetleniyor**, artık **18.010** küçük harfli satır **donmuş** durumda ve tap yolundan **ulaşılamıyor**. Tamamlayan tek satır: `ALTER TABLE tags VALIDATE CONSTRAINT tags_uid_canonical_hex;` — ama önce o satırların gitmesi gerekiyor, yani **`make db-reset`** (backlog **T6**'nın zaten istediği şey). ⚠️ **Üretimde sorun yok:** taze migrate+seed bir DB'de ihlal eden **0** satır var ve `VALIDATE` **başarıyla koşuyor** (güvenlik merceği ölçtü) — yani üretim kısıtı **fiilen validated doğar**. Bu bir **dev-DB hijyeni**, pilot öncesi. | M6-06 B veri katmanı | M8 (pilot öncesi) |
| **T16** | **`tappa_app` `tags` üzerinde TABLO GENELİNDE INSERT tutuyor — 00013'ün *"dürüst zayıf noktası"*.** Ölçüldü: `INSERT INTO tags (…, '\xdead', 'unassigned')` kendi tenant'ında **INSERT 0 1** (başka tenant'a RLS `WITH CHECK` reddediyor). Yani bir SQL enjeksiyonu ya da gelecekte yazılacak bir yükleyici sorgusu, **kendi tenant'ında** hem bir fail-open durum satırı hem **bozuk bir `aes_key_ref` zarfı** üretebilir — ikisi tek yetkiyle bitişik. **Bugün ulaşan sorgu yok** (`db/queries`'de `tags`'a **sıfır** INSERT; üretim Go'sunda sıfır; dinamik SQL yok). ⚠️ Bu, **T8'in ve fail-open guardrail'in *bugün sömürülemiyor* korumasının dayandığı tek şey** — ve o koruma bir **yükleyici yazıldığı gün** biter. Çözüm: plaket yükleme **`tappa_owner`'ın işi** (M8-05 runbook) + `REVOKE INSERT ON tags FROM tappa_app`. ⚠️ Bugün **7 test dosyası** o INSERT'i kullanıyor. 🔴 **BU ÇÖZÜM 2026-08-20'DE UYGULANAMAZ HÂLE GELDİ ve maddenin vadesi bu yüzden değişti.** [ADR 0017](adr/0017-encode-rolesi-ve-yarim-yazma-kurtarmasi.md) §3.1 encode akışının **`tappa_app` ile bağlanmasını zorunlu** kılıyor, ve bu bir tercih değil: encode uç noktası **HTTP**, `internal/db/pool.go` üretimde owner/superuser/BYPASSRLS bir DSN'i **reddediyor** (M8-04 FAZ B2, `239d427`) → owner ile bağlanan bir HTTP yolu **açılışta ölür**. Yani `REVOKE INSERT` yazıldığı gibi uygulanırsa **M8-05'in tasarladığı akış başlangıçta ölür**; iki metin bir denetim turuna kadar birbirini hiç anmadı (M8-05 FAZ B1 tur 1, 5. denetim). **Ayakta kalan azaltma sütun düzeyindedir**, tablo geneli değil: `aes_key_ref`'i INSERT hakkının dışına çıkarmak — ki bugün **dokuz sütunun dokuzu** verili (canlı katalogla ölçüldü). Bu ADR 0017 §6 md.9'da **açık madde** olarak duruyor. ⚠️ Ve maddenin **öncülü de değişti**: *"bugün ulaşan sorgu yok"* korumasının biteceği gün olarak işaret edilen *"bir yükleyici yazıldığı gün"* **artık tarihli** — encode akışı M8-05 FAZ B2'de yazılıyor. | M6-06 B güvenlik denetimi | ~~M8-05'ten ÖNCE~~ → **M8-05 FAZ B2 ile birlikte** (sütun düzeyi) |
| **T17** | **`audit_log`'da `(tenant_id, target)` indeksi yok ve `ListPlaqueHistory` tenant'ın TÜM audit hacmini tarıyor.** Güvenlik merceğinin `EXPLAIN ANALYZE`'ı (2026-08-10, geçmişi **0 satır** olan bir plaket): `Bitmap Heap Scan on audit_log` · **Rows Removed by Filter: 2.566** · `Heap Blocks: exact=1039` · `shared hit=1064` · **2,469 ms** — **dönen satır 0**. Yani maliyet plaketin geçmişiyle değil **tenant'ın toplam audit hacmiyle** büyüyor, ve `audit_log` **append-only**, retention job **yok**. Bir oturum bütçesi (300 istek / 10 dk) kadar kart açmak bunu 300 kez ödetir. ⚠️ Sorgunun yorumu *"BOUNDED BY @row_limit"* diyordu — **sınırlı olan ÇIKTI, tarama değil**; cümle M6-06 B kapanışında düzeltildi. Çözüm `CREATE INDEX ON audit_log (tenant_id, target)` (ya da `(tenant_id, action, target)`) — **migration ister**; 00013 M6-06 B'nin yuvasıydı ve harcandı. **M6-07 (raporlar) da audit indeksleri isteyecek → birlikte yapılmaları ucuz.** | M6-06 B güvenlik denetimi | M6-07 / M7 |
| **T18** | **Toplu CSV çıkışı audit yazımı düşerse İZSİZ gider.** `internal/handler/reportscsv.go` `a.record`'un dönüş değerini okumuyor; indirme koşulsuz devam ediyor. Yapıcının gerekçesi *"aynı sayılar `GET /admin/reports`'ta zaten audit'siz erişilebilir"*di ve **üçte biri yanlış**: `maxReportRows = 100`, `maxOpenRows = 100`, ve ekranda **hiç kursör yok** (`reports.go:66` kendi ağzıyla *"this page cannot page"* diyor) → **101+. kişinin satırları YALNIZCA export'tan** alınabiliyor. Fark **gizlilik değil hesap verebilirlik**: alttaki kayıtlar sayfalanabilir Transactions bölümünden okunabiliyor ve o da audit yazmıyor, yani **§4.6 ihlali değil** (kaybolan bir *kayıt* yok). Pencere dar (rapor okuması başarılı + aynı Postgres'e audit INSERT'i başarısız) ve hata `slog.Error` ile bağırıyor. Kapatmak *"audit yazamazsam indirme yok"* demek ister — **bir ürün kararı**, GDPR tarafıyla birlikte. | M6-07 B güvenlik denetimi | M7 |
| **T19** | **Çapraz-origin bir gezinme, SİLİNEMEYEN bir `audit_log` satırı yazdırabiliyor — T14'ün daha AĞIR üyesi.** `GET /admin/reports.csv` `mountSections`'da, yani `sameOriginGate`'siz; **bilinçli** (gate `Sec-Fetch-Site: none` → **red** verdiği için **yer iminden/adres çubuğundan** açılan indirmeyi kırardı — ölçüldü, *"eski tarayıcılar"* değil **her tarayıcı**). Ölçüldü: `Origin: https://evil.example` + `Sec-Fetch-Site: cross-site` + canlı çerez → **200 + 1 audit satırı**. **Saldırgan dosyayı OKUYAMIYOR** (`Access-Control-Allow-Origin` **yok**; çerez `SameSite=Lax`). Kalan zarar **kalıcı iz kirliliği**: T14'ün yan etkisi bir **çerez** (istemci tarafı, kendi kendini sınırlar), buradaki **append-only bir tablodaki satır** ve §4.3 tetikleyicisi yüzünden **kimse silemez**. Sınırlar: `floodGate` + `sessionGate` ücretlendiriyor (301. istek **429**), satır aktörü **adlandırıyor**. T14 ile birlikte bakılmalı. | M6-07 B güvenlik denetimi | M7 |
| **T20** | **Türetilmiş §4.5 kuşak ağının `INSERT … VALUES` KÖR NOKTASI — ve ağ iki bağımsız yoldan yenildi.** M6-08'de belt `transactions.sql`'e genişletildi ve denetçinin gevşetmesini (`OR e.tenant_id IS NOT NULL`) **yakaladı**. Ama ölçüldü: ürünün **70 sorgusundan 12'si INSERT, 7'si `INSERT … VALUES`** ve eşleştirici onları **yapısal olarak göremiyor** — `RecordAuditEvent` · `CreateInvite` · `EnsureBaselinePolicy` · `EnsureBaselinePolicyVersion` · `EnsurePolicyAttachment` · `CreateSession` · **`InsertTransaction`** (ürünün ana yazma yolu). ⚠️ **İki kaçış daha kuruldu ve ikisi de işledi:** (i) satırın `tenant_id`'sini **ikinci bir parametreden** yazmak — eşleştirici SELECT'in `WHERE`'ine bakıyor, INSERT'ün **yazdığı** değere **hiç bakmıyor**; (ii) kapsamsız bir okumayı **işaret edilmemiş bir dosyaya** koymak (`reviews.sql`) → üç ağ da görmedi. **Üçü de RLS ile kapalı** (`WITH CHECK` 42501, composite FK 23503), yani **belt deliği, canlı açık değil**. Anti-vacuity muhafızı var: `transactions.sql`'e yeni bir `INSERT … VALUES` eklemek testi **kırmızı** yapıyor, yani sessiz değil. | M6-08 doğrulama denetimi | M7 |
| **T21** | **`problemPanelUnavailable` yazma yollarında yanlış cümle kuruyor — 13+5 çevrim SAHİPSİZ.** Metin *"We could not **read** your records … this page is **not showing anything**"* diyor; müdür **yazıyordu**. (Son cümlesi — *"no record has been lost"* — güvenlik merceğince **doğrulandı**: INSERT patlarsa transaction geri alınıyor.) Ölçüldü: **32 çağrı yeri**, **14'ü `mountWriting` handler'larında**, 5'i daha yeniden-render yardımcılarında. M6-08 `problemPanelWriteFailed`'i ekledi ve **yalnız kendi 3 yerini** çevirdi. Kalan **13+5** nominal olarak M6-04/M6-05/M6-06'nın ama **üçü de `done`** → sahipsiz. İkinci temsil riski **düşük** (iki cümle ayrı olguları anlatıyor). | M6-08 denetimleri | M7 |
| **T22** | **`make db-reset` KIRIK — ve bu T6/T15'in beklediği tek temizlik yolu.** Ölçüldü: `00013`'ün `Down`'ı biriken geliştirme verisine takılıyor — önce `tags_status_check` (**30 `unassigned` satır**), sonra `location_id NOT NULL` (**34 satır**). ⚠️ **Yeni bulgu DEĞİL:** `00013`'ün kendi `Down` başlığı bunu **2026-08-09'da** yazmış; yeni olan, artık **fiilen kullanılamaz** olması. Bedeli M6-09'da somutlaştı: `policies` **DELETE vermiyor** (§4.6, bilinçli) ve `policy_versions` sahibe bile kapalı, yani mutasyon/prob koşularının bıraktığı satırlar **hiçbir ürün yoluyla silinemiyor** — taban `DROP SCHEMA public CASCADE` + db-init'in extension/grant'ları + `make migrate seed` + **`make simulate-day`** ile **elle** kuruldu (seed policy satırı yazmıyor; ilk tap materialise ediyor). ⚠️ Elle kurulan taban **eksiksiz çıktı** (güvenlik merceği doğruladı: `tappa_app` NOBYPASSRLS, 7 tabloda FORCE RLS, grant'lar tam) ama **bu bir prosedür değil, bir kurtarma**. Pilot öncesi ya `00013`'ün `Down`'ı veri-toleranslı yazılmalı ya `db-reset` migration'lardan bağımsız (drop+recreate) olmalı. | M6-09 B, 5.–9. tur | M8 (pilot öncesi) |
| **T23** | **🔴 SEVK EDİLMİŞ BİR BASELINE KURALININ GEREKÇESİ YANLIŞ — VE MÜŞTERİYE BASILIYOR.** `internal/policy/baseline.go:208`, `base:gps-conflict-review`'ın `Reason`'ı: *"**IP matched but** GPS places the device away from the location"*. **M6-11 bunun yanlış olduğunu ölçtü:** `decide.go:715-722` `conflict = !match` döndürüyor — **yalnız mesafeden**, adrese **hiç bakmadan** — ve `baseline.go:206-208`'in koşulu yalnız `{CtxTapGPSConflict: true}`, yani **politika da daraltmıyor**. Cümle `/admin/policies` render'ında **birebir basılıyor** (`policies.templ:448` ← `policies.go:505`), yani müşteri **var olmayan bir mekanizmayı** okuyor. ⚠️ **Neden ayrı görev:** sevk edilmiş bir baseline **belgesinin gövdesini** değiştirmek materialise/sürüm sorusu açar (`EnsureBaselinePolicyVersion` yeni sürüm yazar mı, yazan tenant'larda ne olur) ve `policy_versions` **append-only**. M6-11 kartı düzeltildi; **ürün düzeltilmedi**. | M6-11, 1. tur (genel göz N10) | M7-03 ya da ayrı |
| **T24** | **`FILTER (WHERE …)` kör noktası §4.5 ağının İKİ kardeş kopyasında duruyor.** M6-11 `internal/domain/ledger/query_test.go`'yu düzeltti (kör nokta **gerçekti**: daraltma kaldırılınca doğru SQL üzerinde **28 yanlış alarm**). `internal/domain/review/query_test.go` ve `internal/domain/tenant/query_test.go` **düzeltilmedi** (`grep -c filterRE` → 0, 0). ⚠️ **Bugün ATIL ve ölçüldü:** `db/queries/*.sql` içinde `anomalies.sql` dışında `FILTER (` **0 adet**, yani o iki paketin çağırdığı hiçbir sorgu bu şekli kullanmıyor. Sapma `ledger` tarafında **açıkça yazılı**. Riski: o paketlerden birine agregat `FILTER` taşıyan bir sorgu girdiği gün ağ **yanlış alarm** verir (fail-safe yön) ya da daraltma kopyalanmadan eklenirse **sessiz kalır**. | M6-11 doğrulama denetimi | M7 |
| **T25** | **⚠️ ÜRÜN KARARI: `ListTapsTakenTogether` YENİ BİR SOSYAL ÇIKARIM YETENEĞİ.** Ekran *"her gün saniyeler içinde birlikte tap yapan kişiler"*i listeliyor — kim kiminle, ne sıklıkta, kaç saniye arayla. **`tappa-security-auditor` §4 açısından TEMİZ buldu** ve kartın *"rapor yorum yapmaz"* ilkesine uyuluyor (sayfa *"Two people arriving together is not a finding … colleagues who share a lift produce exactly this row"* diye açılıyor, eşikler **20 sn / 2 ayrı gün** ekranda). **Ama bu, ürünün daha önce yapamadığı bir çıkarım** ve güvenlik merceğinin kendi ifadesiyle *"ürün sahibinin bilerek onaylaması gereken bir yetenek"*. Karar: kalsın mı, eşikler değişsin mi, yoksa müdür ekranından çıkıp yalnız denetim izine mi düşsün? | M6-11 güvenlik denetimi | kullanıcı kararı |
| **T26** | **TEST FİKSTÜRLERİ, ÜRÜNÜN ÜRETEMEYECEĞİ SATIRLAR ÜRETİYOR — ve bu M6-12 A'da iki ajanı yanılttı.** Ölçüldü: bir `make test` koşusu `employees`'e **~+375 çelişen satır** ekliyor (`status='active'` ama `activated_at IS NULL`), ve bunlar **hiçbir ürün yolunun yazamayacağı** bir şekil (`invites.sql:241` ve `employees.sql:461` damgayı `COALESCE` ile garantiliyor). Deaktivasyon yarısı **kapatıldı** (`seedflow_db_test.go`'nun `hire()` yardımcısı artık `deactivated_at` yazıyor; KF'de koşu başına +1 durdu); **aktivasyon yarısı duruyor** — ~**185 INSERT noktası**, ölçüldü. Bedeli somut: M6-12 A'da bir orkestratör devir cümlesi ve bir yapıcı gerekçesi **bu kirliliği gerçek veri sanarak** yazıldı ve bir denetim turu bunu çürütmeye gitti. ⚠️ Ve `make db-reset` **kırık** (T22), yani kirlilik pratikte birikiyor. Fikstürlerin `status`'a uygun damga yazması istenir; alternatifi bir `CHECK` ama o **ölçüldü ve elendi** (201 ihlal / 215 kırmızı test, ve kısıt **kuralı kanıtlayan fikstürü** reddediyor). | M6-12 A, 1.–5. tur | M7 (T22 ile birlikte) |
| **T27** | **KAPATILMAMIŞ BİR AY, `plan` DEĞİŞİMİNDEN KORUNMUYOR.** `billing_periods` dondurulmuş satırı `free_period`'ü **kapatma anındaki** `tenants.plan`'dan hesaplıyor (`billing.sql:159,165`) ve dönemlerin **sırayla ya da zamanında** kapatılmasını zorlayan hiçbir kısıt yok (`pg_constraint`'te doğrulandı). Yani bir tenant `founding → standard`'a geçerken **kapatılmamış ücretsiz ayları** varsa, o aylar sonradan **ücretli** kapanır. Erişim bugün dar (`plan` M6-12 A'dan sonra `tappa_app`'e **hem INSERT hem UPDATE**'te kapalı, yalnız operatör yazabiliyor) ve `00016`'nın yorumu güvenceyi **kapanmış aylarla sınırlı** olarak dürüstçe yazıyor. Eksik olan iki şeyden biri: bir **plan geçmişi** tablosu (dönem, o dönemdeki plan) ya da bir **sıralama/zamanlama kısıtı**. Pilot faturalaması başlamadan karara bağlanmalı. | M6-12 A, 2. tur (U3) + güvenlik denetimi | M8 (pilot öncesi) |
| **T28** | **HSTS ürün genelinde YOK — ve M7-01 bunun ilk gerçek kurbanı olabilecek yüzeyi açtı.** `Strict-Transport-Security` hiçbir rotada set edilmiyor (üretim kodunda **0 eşleşme**, güvenlik denetimi 2026-08-13). **M7-01'in getirdiği bir kusur DEĞİL** — ama bugüne kadar her rota ya oturum ya davet kodu ya SUN istiyordu; `/` ve dört yasal sayfa **herkese açık** ve bir downgrade saldırısının hedefleyebileceği ilk yüzey. TLS büyük olasılıkla **ters vekilde** sonlanıyor, yani başlığı orada mı yoksa uygulamada mı set edeceğimiz bir **dağıtım kararı** — ve `includeSubDomains`/`preload` ayrı kararlar, geri alınması **aylar** sürer. Pilot öncesi karara bağlanmalı. | M7-01 güvenlik denetimi | M8 (pilot öncesi) |
| **T29** | ✅ **YARISI KAPANDI — M8-01 (`4ddd11f`, 2026-08-15):** `/healthz` **ve** yeni `/readyz` artık GET **ve** HEAD alıyor, ikisi de `no-store` + `nosniff` taşıyor. *"En güçlü kalan aday `/healthz`"* cümlesi **artık geçerli değil**; kalan `/t` · `/activate` · panel (farklı patlama yarıçapı, bilinçli olarak açık bırakıldı). ⚠️ **VE KALAN YARININ ŞEKLİ ÖLÇÜLDÜ, DEĞİŞTİ:** güvenlik denetçisi *"405 `Allow: GET` diyor, HEAD'i saymıyor"* diye raporladı; yapıcı gerçek artefaktta yeniden ölçtü ve **iki ayrı başlık satırı** buldu (`Allow: GET` + `Allow: HEAD`) — chi `Add` kullanıyor, `Set` değil (`mux.go:527`, orkestratör bağımlılık kaynağından teyit etti). Yani HEAD **ilan ediliyor**; gerçek kusur **`Header.Get` tarzı API kullanan istemcilerin yalnız ilkini görmesi** ve **`OPTIONS`'ın 204 + `Allow` yerine 405 alması**. Çare: router'a `Allow`'u **tek satırda virgülle birleştiren** ve `OPTIONS` yanıtlayan bir `MethodNotAllowed` handler'ı → **M8-02**. *(Aşağıdaki özgün metin tarihsel.)* **`HEAD` ürün genelinde 405 veriyor — M7-01 yalnız pazarlama yüzeyini düzeltti.** chi'nin `r.Get`'i **yalnız GET** kaydediyor; ölçüldü: `HEAD /healthz`, `HEAD /admin/login`, `HEAD /activate`, `HEAD /t` → **405 `Allow: GET`** (`/static/*` istisna, `http.FileServer` kendi hallediyor). M7-01 `/` ve dört yasal rotaya `r.Head` ekledi ve **bilinçli olarak router geneline yaymadı**: tap ve panel uçları oran ölçüyor ve kimlik çözüyor, yani **farklı patlama yarıçapı**. **En güçlü kalan aday `/healthz`** — uptime monitörleri onu HEAD'ler ve 405 *"servis bozuk"* diye okunur. ⚠️ Ve bu sınıfı test ederken **gerçek `httptest.NewServer` gerekir**: `ResponseRecorder` net/http'nin HEAD gövde bastırmasını **uygulamıyor**, yani aynı iddia recorder'da gerçek istemcilerin **hiç almadığı** bir gövde raporlar (M7-01 bunu ölçtü). | M7-01, 2. tur (U3) | M8 |
| **T30** | **`TAPPA_TRUSTED_PROXIES` boşken panel giriş bütçesi 25× SIKILAŞTI — ve o kurulumda anahtar TÜM ürün için tek adrese çöker.** M7-02 `adminLoginWorkLimit = 120/10dk/**adres**` ekledi (gerekçe: dolgu her girişi 8 bcrypt'e çıkardı ve başarılı giriş yolunda **hiçbir bütçe yoktu** — ölçüldü: 18/18 başarılı giriş, 0 adet 429, ~15 çekirdek). Ters vekil arkasında `TAPPA_TRUSTED_PROXIES` **boş** bırakılırsa `realip` gerçek istemciyi çözemez ve anahtar tek adrese çöker — **o kurulumda panel giriş tavanı tüm müşteriler için 120/10 dk** olur; değişiklik öncesi aynı çöküş `adminFloodLimit`'te **3000** idi, yani **25× daha gevşekti**. ⚠️ **Yeni bir SINIF değil** (aynı yetenek flood bütçesinde zaten vardı ve `adminratelimit.go` takası *"IT CAN REFUSE A CORRECT PASSWORD"* diye **ilan ediyor**; `.env.example` de diğer bütçeler için uyarıyor) — ama sıkılık 25× arttığı için **dağıtım belgesinde artık zorunlu bir adım**. Ayrıca paylaşılan çıkış adresi (CGNAT) ardındaki bir saldırgan, **kendi kaydettiği** geçerli bir hesapla 120'yi doldurup pencerenin kalanında ofisi dışarıda bırakabilir. Pilot dağıtımı yapılmadan karara bağlanmalı. | M7-02 güvenlik denetimi (2. tur) | M8 (pilot öncesi) |
| **T31** | 🔴 **`make audit` BUGÜN KIRMIZI VE SEBEBİ ARAÇ ZİNCİRİ: govulncheck 6 stdlib açığı sayıyor, hepsi `go1.26.5 → 1.26.6` ile kapanıyor** (GO-2026-6088, GO-2026-5972, GO-2026-5026 ve üç tanesi daha; `make audit` **exit 3**). `./scripts/redline-check.sh` tek başına **exit 0**, yani kırmızı çizgi taraması temiz — kırılan yalnız güvenlik açığı taraması. ⚠️ **Bu bir repo değişikliği değil, kullanıcının Go kurulumu** — bu yüzden ajanlara yaptırılmadı. Ve şu an `state.md` sağlık tablosu `make audit`'in yeşil olmasını bekliyor, yani **her oturum bu satırda takılacak**. Kullanıcı `go1.26.6`'ya çıkana kadar `make check`/`make audit` kırmızılığının sebebini bilerek okumak gerekiyor. | M7-03 A güvenlik denetimi (2026-08-13) | Kullanıcı — sonra M8 |
| **T32** | **`log_statement=all` GELİŞTİRME COMPOSE'UNDA AÇIK VE BIND PARAMETRELERİNİ İKİ KEZ LOG'A YAZIYOR — signup'ın yazdığı GERÇEK cost-12 digest dahil.** Ölçüldü (`tappa_app`, extended protocol): `LOG: bind … DETAIL: Parameters: $1 = '<değer>'` ve ardından `LOG: execute … DETAIL: Parameters: $1 = '<değer>'`. Hata satırının kendisi **temiz** (`log_parameter_max_length_on_error=0`), sızdıran şey normal ifade log'u. **00018'in getirdiği bir kusur DEĞİL** — o migration'ın reddettiği değerler tanım gereği hiçbir şeyi doğrulamıyor; asıl mesele **aynı mekanizmanın `CreateAdminUser`'ın yazdığı geçerli digest'i de** yazması, ve bu M7-02'den beri böyle. Kapsam **dev/CI** (üretimde yönetilen Postgres). İki çare: compose'ta `log_statement`'ı `ddl`'e indir, ya da `admin_users`'a yazan yolları parametresiz say. §4.7'ye değiyor. ⚠️ **KAPSAM BÜYÜDÜ, 2026-08-14 (M7-04 B güvenlik denetimi):** aynı mekanizma artık **`password_resets.token_hash`**'i de bind parametresi olarak yazıyor, ve M7-04 B o yolu **ilk kez HTTP'den erişilebilir** kıldı — bir sıfırlama token'ının hash'i, dev makinesinde kimliksiz bir istekle Postgres log'una düşürülebiliyor. **Yeni bir sapma değil** (ADR 0015 kabul edilen riskler arasında kayıtlı, borç `docker-compose.yml`'e ait) ama artık **iki sır sınıfı** aynı satırdan geçiyor. | M7-03 A güvenlik denetimi (F2) · kapsam: M7-04 B güvenlik denetimi | M8 (pilot öncesi) |
| **T33** | **`redline-check.sh` R5 bloğu ALTER-only migration'ları GÖREMİYOR — ve ölçülerek gösterildi.** R5 yeni tabloların beşlisini (`tenant_id` · `ENABLE RLS` · `FORCE RLS` · politika · indeks) yalnız **`CREATE TABLE` ile doğan** tablolarda arıyor. Denetçi geçici bir `db/migrations/99999_zzz_audit_probe.sql` koydu — içinde `ALTER TABLE admin_users NO FORCE ROW LEVEL SECURITY;` + `ALTER TABLE transactions DISABLE ROW LEVEL SECURITY;` + `GRANT ALL ON admin_users TO tappa_app;` → **tarama exit 0, tek satır uyarı yok** (dosya hemen silindi). Yani §4.5'i **geri alan** bir migration mekanik ağdan geçer; bugün onu tutan tek şey `internal/db/rls_test.go`'nun kendi sondaları (onlar RLS kapatılınca **kırmızıya dönüyor**, mutasyonla doğrulandı). Ağı ALTER ifadelerini de görecek şekilde genişletmek gerekiyor. ⚠️ Ayrıca R7 ağı `.pem/.key/.aes` dosyalarına bakıyor; `test/fixtures/ids.go`'ya giren 60 karakterlik digest biçimli sabiti **mekanik olarak hiçbir şey elemedi** (sahte olduğu elle kanıtlandı). | M7-03 A güvenlik denetimi (madde 9) | M8 |
| **T34** | 🔴 **`state.md`'nin "bağlantı tükenmesi" flake'inin MEKANİZMASI bulundu — ve bu bir tükenme değil, KİLİTLENME.** Üçüncü göz `internal/db`'de `53300 too many clients` ve `internal/domain/signup`'ta **10 dakikalık paket zaman aşımı** gördü; goroutine dökümü zinciri gösterdi: `newBusinessVAT` (`internal/domain/signup/signup_db_test.go:386`) **dıştaki `WithTenant` işlemi bir bağlantı tutarken** ikinci bir `WithTenant` açıyor → bağlantı alamayınca `t.Fatalf` → `Goexit` → `t.Cleanup` → `DB.Close()` → `puddle.Pool.Close()` **aynı goroutine'in hâlâ tuttuğu bağlantıyı bekleyerek sonsuza kadar bloke oluyor**. Yani havuz doyduğunda suite **hata vermiyor, ASILIYOR** — ve 10 dk'lık Go paket zaman aşımına kadar bekliyor. `newBusinessVAT` **M7-02'den**, M7-03 A'da değişmedi; temiz koşularda yeşil. ⚠️ Bu, `state.md` sağlık tablosundaki *"iki bilinen flake"* satırının ikincisinin gerçek sebebi olabilir — orada *"`internal/db` + `internal/sun` 54'er bağlantı açıyor = 108 > 97"* yazıyor ve **doğru**, ama asılmanın sebebi bağlantı sayısı değil **iç içe `WithTenant` + `Cleanup` sırası**. Çare bağlantı sayısını düşürmek değil (o bir §4.4 testini zayıflatır): iç içe `WithTenant`'ı kaldırmak ya da `Cleanup`'ta bağlantıyı önce bırakmak. | M7-03 A, 3. tur üçüncü göz (N1) | M8 |
| **T35** | 🔴 **"AYNI TRANSACTION ⟹ AYNI SNAPSHOT" YANLIŞ, VE ÜRÜNDE BEŞ YERDE DAHA YAZILI — BİRİ TAP KARAR MOTORUNDA.** `WithTenant` `pool.Begin` ile açıyor, izolasyon **READ COMMITTED**, yani **her ifade kendi snapshot'ını alır**. Ölçüldü (M7-03 B güvenlik denetimi): tek transaction içinde iki özdeş sayım, arada başka bir oturum commit ederken `in_stock=0/loaded=0` → **`in_stock=1/loaded=1`**. M7-03 B kendi yazdığı iki cümleyi düzeltti; kalan beşi **kapsam dışıydı**: `internal/domain/ledger/review.go:155`, `roster.go:237` ve `:271`, `internal/domain/tenant/directory.go:91`, ve 🔴 **`internal/domain/checkin/checkin.go:929-931`** — *"a shift cannot change between reading the employee and reading the location"*. **Sonuncusu tap karar motorunun içinde ve §5'in geç-kalma hesabına bakıyor:** bir müdür tam o anda vardiyayı değiştirirse çalışan bir snapshot'ta departman vardiyasına, diğerinde lokasyon vardiyasına göre değerlendirilebilir. **Bugün bir kusur ürettiğine dair ölçüm YOK** — pencere mikrosaniyeler ve yazma yolu nadir; ama **cümle sağlamadığı bir garantiyi beyan ediyor** ve bu projenin imza kusur sınıfı. İki çare: cümleleri *"aynı transaction ve aynı tenant bağlamı"*na indirmek, ya da gerçekten snapshot isteniyorsa `BEGIN ISOLATION LEVEL REPEATABLE READ` (bedeli: **serialization failure'da retry** gerekir, bugün hiçbir yerde yok). | M7-03 B, 3. tur üçüncü göz (bulgu 4) | M8 |
| ~~**T36**~~ | ✅ **KAPANDI — M7-03 B, 3. tur** (eklendikten saatler sonra). `rg` yokken script artık **exit 2** veriyor (*"ATLANDI … TARAMA KOSMADI · Bu bir 'temiz' sonucu DEGILDIR"*) ve `make audit` özeti `redline-check **SKIPPED(no rg)** exit=2` yazıyor. **Exit 2 seçildi, 1 değil**: 1 zaten *"ihlal bulundu"* demek, ikisini aynı sayıya bindirmek *"tarayamadım"*ı *"ihlal var"* diye raporlardı. Mutasyonla doğrulandı (`exit 0`'a döndürülünce `make audit` yine yeşil). Aşağıdaki özgün metin tarihsel. **`redline-check.sh` ripgrep YOKKEN YEŞİL GEÇİYOR — ve `make audit`'in yeni özet satırı bunu temiz taramadan ayırt edilemez kılıyor.** Ölçüldü: `env PATH=/usr/bin:/bin ./scripts/redline-check.sh` → *"uyari: ripgrep (rg) bulunamadi — tarama atlaniyor."* **EXIT=0**; `make audit` de `redline-check exit=0` yazıyor. ✅ **Ürün kapısı tutuyor** — `.github/workflows/ci.yml:60-67` ripgrep'i kuruyor **ve `rg --version` ile kanıtlıyor**; kör olan **yerel döngü**. ⚠️ Delik M7-03 B'den **eski**, ama o görev `make audit`'i düzeltirken başlığa *"iki tarama da koşar"* yazdı ve bu durumda tarama **koşmuyor** — ağ hakkındaki iddia ağın kendisinden kuvvetli. **M7-03 B 3. turda kapatılmaya çalışıldı; kapanmadıysa burada duruyor.** Çare: `rg` yokken sıfır olmayan çıkış, ya da özette `SKIPPED(no rg)` — *"atlandı"* ile *"temiz"* aynı satırda görünmesin. | M7-03 B güvenlik denetimi, 2. geçiş (bulgu 3) | M8 |

| **T37** | 🔴 **MÜŞTERİ ZAMAN DİLİMİNİ DEĞİŞTİREREK İLK ÜCRETLİ AYINI BİR AY ÖTELEYEBİLİYOR.** M7-05 `tenants.timezone`'u panelden düzenlenebilir yaptı (00016'nın grant'ı zaten veriyordu), ve `tappa_first_chargeable_month(plan, created_at, timezone)` **o sütuna bağlı**. Ölçüldü (`tappa_app`, `created_at=2026-01-31T23:30Z`): `zone=UTC` → **2026-04-01** · `zone=Europe/Malta` → **2026-05-01** · `Pacific/Kiritimati` → 2026-05-01 · `Pacific/Midway` → 2026-04-01. Ay sınırına yakın kaydolmuş bir işletmenin sahibi, ay dondurulmadan **önce** zone'u değiştirerek bir ayı bedavaya çevirebilir ve `billing_periods` **immutable** olduğu için sonuç **kalıcı** olur. Ayrıca `tappa_local_month_start` zone'a göre **14 saate kadar** kayıyor, yani ay sınırında işe alınan/çıkarılan kişiler dondurulmamış ayda sayıma girip çıkabilir. ⚠️ **§4'ün hiçbir maddesini ihlal etmiyor** — `transactions` korunuyor, `billing_periods` değişmiyor. **Üç hafifletme ölçüldü:** (a) donmuş ay **kendi** `period_from/to/timezone`'unu taşıyor (`billing.sql:231,257`) ve `GetBillingPeriod` onu okuyor → **kapanmış ay kaymıyor**; (b) her zone değişimi `tenant.account_updated` satırına **`timezone_before/after`** olarak düşüyor → operatör tespit edebilir; (c) ekran geri alınamazlığı **açıkça söylüyor** (`account.templ:263-271`). Çare bu kartın değil **fatura tarafının**: *"dondurma anında zone son N gün içinde değiştiyse operatöre bayrak"*. **T27 ile birlikte karara bağlanmalı** — o da aynı sınıf (`plan` değişiminin kapatılmamış ayları). | M7-05 güvenlik denetimi (2026-08-15) | M8 (pilot öncesi) |

| **T38** | 🔴 **DÜZ HTTP ÜZERİNDEN, `localhost` DIŞINDAKİ HİÇBİR ADRESTEN PANELE GİRİLEMİYOR — VE ZİNCİRİN İKİ HALKASI DA ÜRÜNÜN KENDİSİ.** Kullanıcının ilk canlı denemesi LAN üzerinden (`http://192.168.0.98:8080`) *"That sign-in step expired"* verdi; **süre dolması değildi** — `adminlogin.go:1391-1394` same-origin reddini de `problemAdminRestart` ile basıyor, yani kullanıcı **beklenmesi gereken bir şey** sanıyor ve **beklemek hiçbir şeyi çözmüyor**. **MEKANİZMA, KONTROLLÜ ÇİFTLE ÖLÇÜLDÜ** (aynı headless Chrome 148, aynı sayfa, aynı form, araya konan loglayan vekil tel üstündeki başlıkları kaydetti): `http://192.168.0.98:8081` → `Origin="null"`, `Sec-Fetch-Site=""` → **400** · `http://localhost:8081` → `Origin="null"`, **`Sec-Fetch-Site="same-origin"`** → **303, oturum açıldı**. Kullanıcının gerçek tarayıcısı (**Chrome 151**) da aynı satırı üretti: `Origin="null" Sec-Fetch-Site="" Referer=""`. **Halka 1 — `Origin` DALINI ÜRÜN KENDİ ÖLDÜRÜYOR:** giriş sayfasının kendi `<head>`'inde `<meta name="referrer" content="no-referrer">` var; bu politika altında tarayıcı `Origin`'i **`null`** gönderiyor (ölçüldü: **güvenilir kaynakta da, güvenilmezde de** `null`). Yani `sameOrigin`'in `EqualFold(origin, baseURL)` karşılaştırması **hiçbir zaman çalışmıyor** — meta'ya uyan her tarayıcıda dal daima `null`'a düşüyor ve `TAPPA_BASE_URL`'i düzeltmek **işe yaramıyor** (denendi). **Halka 2 — GERİYE TEK DAYANAK `Sec-Fetch-Site` KALIYOR**, ve o başlıklar yalnız **potansiyel olarak güvenilir** kaynaklara gönderiliyor (HTTPS ya da `localhost`). Düz HTTP + herhangi bir IP/hostname = başlık **yok** = `default: return false`. **Sonuç: kapı, üretimde HTTPS altında çalışır; TLS öncesi her pilot, her LAN demosu ve yanlış yapılandırılmış her ters vekil PANELİ TAMAMEN KİLİTLER**, ve ekran *"süresi doldu"* dediği için operatör yanlış şeyi bekler. ⚠️ **Çalışan tarafı ETKİLENMİYOR ve bu bilinçli:** `checkin.go:134` yalnız **var olan ve uyuşmayan** `Origin`'i reddediyor (savunma **oturuma bağlı MAC + `SameSite=Lax`**), `activate.go:370` eksik `Origin`'i kabul ediyor (`strict=false`) ve gerekçesi birebir bu vakayı tarif ediyor: *"aşırı hevesli bir origin kuralı başlığı göndermeyen tarayıcıları kırar; asıl yük senkronizatör token'ında"*. **Panelin `sameOrigin`'inde gevşek mod yok** (`adminlogin.go:1503`, `strict` parametresi bile yok) — oysa panelin de **senkronizatör token'ı var**. **M8-04'ün kararı üç başlıkta:** (a) panel neden çalışandan katı — bilinçli mi, kaza mı; (b) `Origin: null` **kabul edilebilir** mi (meta'nın kendi ürettiği değer, saldırgan sinyali değil) yoksa meta mı kalkmalı; (c) reddediş ekranı *"süresi doldu"*dan **ayrılmalı** mı. | Kullanıcının ilk canlı denemesi (2026-08-15) + orkestratörün Playwright/vekil ölçümü | **M8-04** |
| **T39** | 🔴 **DÜZ HTTP'DE GPS KANITI TAMAMEN KAYBOLUYOR — §5'İN DÖRT KANITINDAN BİRİ.** İlk canlı denemede Ana Silva'nın dokunuşu `gps_lat IS NULL` ile kaydedildi; kullanıcı *"cihazın GPS'ini mi alamadın"* diye sordu. **Ölçüldü** (aynı headless Chrome, iki adres): `http://192.168.0.98:8080` → `isSecureContext=false`, `getCurrentPosition` → **"Only secure origins are allowed" (code 1)** — **izin istemi bile çıkmıyor** · `http://localhost:8080` → `isSecureContext=true`, API **izin veriyor**. Yani tarayıcı, güvenilir olmayan bir kaynağa konumu **hiç vermiyor**; cihazın, iznin ya da ayarın bununla ilgisi yok. **T38'in kardeşi** (aynı "potansiyel olarak güvenilir kaynak" kuralı `Sec-Fetch-*` başlıklarını da esirgiyordu) **ama daha ağır**: T38 bir giriş engeli, bu bir **ürün yeteneği kaybı** — §5 satır 6'nın GPS yarısı ve `base:qr-requires-ip`'in tek alternatifi. ⚠️ **Ve `tap.js:97` yalnız `!navigator.geolocation` kontrolü yapıyor** — nesne **var**, çağrı hata veriyor, yani kod "GPS yok" dalına değil **hata** dalına düşüyor; ekranın kullanıcıya ne dediği ayrıca denetlenmeli. **Sonuç: TLS olmadan hiçbir pilot GPS kanıtını ölçemez** ve bunu yalnız kayıttaki `NULL`'dan anlarsın. | Kullanıcının ilk canlı tap denemesi (2026-08-15), orkestratör ölçümü | **M8-04** (M8-02 TLS ile birlikte) |
| **T40** | 🔴 **BİR MEKÂNA `0.0.0.0/0` VERİLEBİLİYOR VE KAYIT "AĞ KANITI" DİYE YAZIYOR — DEĞİŞMEZ BİR SATIRA.** İlk canlı denemede KM Plant'in adres aralığı `0.0.0.0/0` yapıldı ve sonraki dokunuş şunu kaydetti: `verdict=ok trust=70 ip_match=t`, `note="network proof of place: the source IP matches the location"`. **Eşleşmeyecek IP yok** — yani cümle bir olguyu beyan ediyor ama arkasında hiçbir kanıt yok, ve `transactions` **§4.3 gereği değişmez**, yani düzeltilemez. Ürün engellemiyor: `StaticIPs []netip.Prefix` (`venue.go:230`) ve `/0` **geçerli bir prefix**; genişliği denetleyen hiçbir kural yok. ⚠️ `venue.go:20-27` *"bir mekânın `static_ips`'ini değiştirmek O MEKÂNDAKİ HER GELECEK DOKUNUŞUN nasıl yargılanacağını değiştirir"* diye **uyarıyor** ama yalnız **boşaltmayı** vaka olarak sayıyor; **fazla genişletmeyi** saymıyor — oysa boş bir liste kanıtı *yok eder*, `/0` kanıtı **taklit eder**, ve ikincisi tehlikeli olanı. **Bu, oturumun imza kusur sınıfı** (*sağlanmayan bir garantiyi beyan etmek*), bu kez bir **kayıt satırında**. **M8-04'ün kararı:** kabul edilebilir en geniş prefix nedir (`/8`? `/16`?), reddedilmeli mi yoksa **uyarılıp trust'a yansıtılmalı mı**, ve `ip_match` notu *"aralık şu kadar geniş"* bilgisini taşımalı mı. | Kullanıcının ilk canlı tap denemesi (2026-08-15), orkestratör ölçümü | **M8-04** |
| **T41** | 🔴 **NETWORKPOLICY MIGRATION'I KESİYOR — VE ŞU AN ÜRETİMDE SİLİNMİŞ DURUMDA.** İlk canlı deploy'da (2026-08-15) migration Job'ı `dial tcp 10.42.0.228:5432: connect: **connection refused**` ile üç kez düştü. Postgres o sırada `1/1 Running` ve **dokuz dakikadır** `0.0.0.0:5432`'de dinliyordu (log: `listening on IPv4 address "0.0.0.0"`), yani **yarış değildi**. Kural `deploy/k8s/12-networkpolicy.yaml`, ingress'e `podSelector: app.kubernetes.io/name=tappa` ile izin veriyor ve **migrate pod'u tam o etiketi taşıyor** (ölçüldü: `kubectl get pod … -o jsonpath='{.metadata.labels}'`). **Kuralı silince migration ANINDA geçti** — 20 migration, `successfully migrated database to version: 20`. ⚠️ **Belirti yanıltıcı:** NetworkPolicy dropu genelde **zaman aşımı** verir; k3s'in denetleyicisi **RST** gönderdiği için `connection refused` görünüyor ve bu *"Postgres hazır değil"* diye okunuyor (ilk teşhisim de oydu, yanlıştı). 🔴 **VE BU, DENETÇİNİN "DOĞRULANAMADI" DEDİĞİ TAM MADDEYDİ:** M8-02'nin üçüncü göz raporu *"etiketler tutarlı, kural uygulamayı kesmiyor"* diye **statik** doğruladı, ama aynı raporda dürüstçe şunu yazdı: *"NetworkPolicy'nin bu kümede **uygulandığı**, `apply` etmeden doğrulanamıyor."* Uygulanınca kesti. **BUGÜNKÜ DURUM:** kural **kümede yok** (elle silindi), yani Postgres'e küme içi erişim **kısıtsız** — 31 namespace'ten herhangi biri `tappa-postgres:5432`'ye ulaşabilir. `deploy.yml:300` onu **her koşuda yeniden uyguluyor**, yani bir sonraki deploy migration'ı **yine kesecek**. **Çare üç yoldan biri:** kuralı k3s'in denetleyicisiyle **fiilen sınayıp** düzeltmek · `ipBlock`/namespace seçiciye çevirmek · ya da kaldırıp **sayılmış limit** olarak yazmak. **Ölçmeden birini seçme.** ✅ **KAPANDI — `0ce5615` (2026-08-16), M8-02 FAZ C. 🔴 VE YUKARIDAKİ TEŞHİSİN İKİ YARISI DA YANLIŞTI, ÜÇÜNCÜSÜ DE:** *"yarış değildi"* → **yarıştı**, ama Postgres'in değil **kuralın**: k3s yeni bir pod'un adresini kuralın izin kümesine **asenkron** yazıyor, yani ilk anında bağlanan bir pod izinli etiketi taşısa **bile** reddediliyor. Ölçüldü: izinli etiketli **5 taze pod → 5/5 ilk denemede ret**, 0,2–1,0 sn sonra kabul; kural silinmiş kontrol → **3/3 ilk denemede bağlantı**. Ve `restartPolicy: Never` bir Job **sonsuza kadar kaybediyor**, çünkü her backoff denemesi **yeni IP'li yeni bir pod** (`restartPolicy: Always` ile 1 çökme → **aynı IP** → bağlandı). Kural **kaldı ve uygulandığı kanıtlandı** — tek pod, tek an, iki kontrol: kendi namespace'i `accepting connections`, canlı `tappa` namespace'i **`no response`** (üstelik izinli etiketi taşırken) → çıplak `podSelector` **namespace kapsamlı**. Elenen: `ipBlock: 10.42.0.0/24` yarışı kaldırır ama **32 namespace'in hepsini** içeri alır. **Çare kuralda değil pod'da:** `wait-for-postgres` initContainer'ı (`20-app.yaml` **ve** `30-migrate-job.yaml`) — aynı pod içinde denediği için **tek adres** koruyor. | İlk canlı deploy (2026-08-15), orkestratör ölçümü | ✅ **kapandı** `0ce5615` |
| **T42** | **DEPLOY, POSTGRES'İN HAZIR OLMASINI BEKLEMİYOR.** İlk deploy koşusunda (`31910980778`) migration Job'ı Postgres ayağa kalktıktan **11 saniye sonra** koştu ve `connection refused` aldı (Postgres 22:03:50'de başladı, goose 22:04:01'de bağlanmayı denedi). `deploy.yml` `Apply configuration and database` adımından sonra **doğrudan** `Migrate`'e geçiyor; arada `kubectl wait --for=condition=ready` **yok**. ⚠️ Bugün T41 yüzünden ikisi karıştı ve ilk teşhis yanlış oldu — **ikisi ayrı kusur** ve ikisi de gerçek. Taze bir kurulumda (yeni PVC, node yeniden kurulumu, `local-path` volume kaybı) **aynen tekrarlar**. Job'ın `backoffLimit`'i üç deneme yapıyor ama üçü de 40 saniye içinde bitiyor, yani initdb'yi (roller + uzantılar + init script'leri) beklemeye yetmiyor. ✅ **KAPANDI — `0ce5615` (2026-08-16), M8-02 FAZ C. 🔴 AMA YUKARIDAKİ GEREKÇENİN YARISI YANLIŞTI VE BU KAYIT UYARI OLARAK DURUYOR:** *"arada `kubectl wait --for=condition=ready` yok"* → **var**, `deploy.yml` `Migrate`'ten önce `kubectl -n tappa rollout status statefulset/tappa-postgres --timeout=300s` koşuyor ve `10-postgres.yaml`'ın readinessProbe'u `pg_isready`. Orkestratörün kart ölçümü bunu **brief yazmadan önce** buldu. Sonra iki hipotez daha **ölçülüp elendi**: (a) *"probe unix soketine bakıyor, entrypoint init fazını `listen_addresses=''` ile koşuyor"* — **yapısal olarak gerçek ama ATEŞLEMEDİ**, pencere **0,22 sn**, probe periyodu 5 sn; (b) yavaş initdb — hayır. **Gerçek pencere üçüncüsüydü: headless Service'in DNS kaydı `rollout status` döndükten ~3 sn SONRA görünüyor** (TCP açık 08:50:35,2 · ready 08:50:38 · kapı döndü 08:50:39 · **NXDOMAIN 08:50:42'ye kadar**). T41'in initContainer'ı bunu da kapsıyor. **Ders: bir kusurun ölçülmüş SEMPTOMU doğruyken yazılı SEBEBİ yanlış olabilir; kart ölçümü tam olarak bunun içindir.** | İlk canlı deploy (2026-08-15) | ✅ **kapandı** `0ce5615` |
| **T43** | **`ghcr` SIRRI TAZELENMEDEN YARATILAN POD KALICI `ImagePullBackOff`'TA KALIYOR.** `deploy.yml` çekme kimliğini her koşuda kendi `GITHUB_TOKEN`'ıyla yazıyor (`1194e23`), ama sır **yazılmadan önce** var olan bir pod ölü kimlikle backoff'a giriyor ve kubelet'in geri çekilme aralığı büyüdüğü için **kendiliğinden toparlanmıyor**. Bugün fiilen yaşandı: uygulama pod'u 4 dk 39 sn `ImagePullBackOff`'ta kaldı, sır tazelendikten sonra bile; **elle `kubectl delete pod`** gerekti. ⚠️ Bu, T-GHCR'ın yazılı sınırının (*"düğüm imajı düşürürse çekme ölü kimlikle denenir"*) **ikinci yüzü**: yalnız imaj düşerse değil, **pod sırdan önce doğarsa** da oluyor. Çare: rollout'u sır yazımından sonra sıralamak (bugün öyle) **ve** başarısız çekmede pod'u yeniden yaratan bir adım, ya da kalıcı bir kimlik (`read:packages` PAT / public paket — ikisi de kullanıcının işi, **paket görünürlüğünün API'si yok**, ölçüldü: `PATCH /user/packages/...` → 404). 🔴 **2026-08-16'DA DARALMADI, GENİŞLEDİ (M8-02 FAZ C) — VE KÖK NEDEN SIRALAMA DEĞİLMİŞ.** Sıralama **zaten doğruydu**: `deploy.yml` `ghcr` sırrını her `apply`'dan **önce** yazıyor. Gerçek sebep kubelet: `kubectl get --raw …/configz` → **`imagePullCredentialsVerificationPolicy: NeverVerifyPreloadedImages`** (KEP-2535) + `imageMinimumGCAge: 2m0s`. **Özel olarak çekilmiş bir imaj, KAYITLI kimliği olmayan bir pod tarafından kullanılamıyor** — düğümde katmanlar dursa bile: sır yok → **401** · kullanılmayan sır → **401** · `imagePullPolicy: Never` → `ErrImageNeverPull`; kontrol: **public** imaj + `Never` → önbellekten açıldı, exit 0. **Bu, `20-app.yaml`'ın ve README'nin taşıyıcı cümlesini (*"düğümde katmanlar varken kayda hiçbir şey sorulmaz"*) YANLIŞLIYOR** — ikisi de düzeltildi. **İki keskin sonucu:** (1) **`kubectl rollout undo` 401 alır**, çünkü sonraki bir deploy sırrı yeniden yazmıştır — yani olay anında operatörün ilk uzanacağı komut; (2) `GITHUB_TOKEN` iş akışı bitince öldüğü için **her deploy'dan sonra ürün, tek bir pod yeniden başlatmasına uzakta** ve kendiliğinden geri gelmez. `deploy.yml` takılmış pod'u kurtaran bir adım kazandı (seçici 164 pod'a karşı negatif test edildi) ve `verify-deployment.sh pull-credential` iki damgayı **kanıt olarak** basıyor (hüküm basmıyor — hüküm üç kez fail-open üretti, kaldırıldı). ⚠️ ~~**KALICI ÇARE HÂLÂ KULLANICININ:** ya paketleri arayüzden public yap, ya `read:packages` yetkili **uzun ömürlü bir PAT**'i `ghcr` sırrına yaz.~~ ✅ **KAPANDI — M8-02 FAZ D (2026-08-16), KÖKÜNDEN.** 🔴 **VE YUKARIDAKİ İKİ REÇETE DE ARTIK ÖLÜ — İZLEME:** `secret/ghcr` **yok** (onu yazan adım, iki pod spec'indeki `imagePullSecrets` ve `verify-deployment.sh pull-credential` **silindi**), yani *"PAT'i `ghcr` sırrına yaz"* bugün **hiçbir şeyin mount etmediği** bir Secret yaratmak ve kümeye **uzun ömürlü bir registry kimliği** koymak demek — tam da bu fazın kaldırdığı §4.7 yüzeyi. **Seçilen çare kullanıcı kararıyla registry değişimi oldu:** GHCR → **public Docker Hub**. Gerekçe ölçülmüş: kusurun kökü **KEP-2535**'ti (kubelet `NeverVerifyPreloadedImages`; **özel çekilmiş** imaj, **kayıtlı kimliği olmayan** pod tarafından kullanılamıyor), ve FAZ C'nin ayırt edici kontrolü **public imaj + `imagePullPolicy: Never` → önbellekten açıldı, exit 0** demişti. **Public registry kusuru tanım gereği yok ediyor** — kimlik yoksa bayat kimlik penceresi de yok, `rollout undo` da 401 almıyor. **AWS ECR elendi** (private aynı sorunu dönen IAM token'ıyla geri getirir; ECR Public yalnız `us-east-1`'den push kabul eder). **GHCR public elendi çünkü ULAŞILAMIYOR:** görünürlüğün **API'si yok**, üç kez ölçüldü — `gh api -X PATCH /user/packages/container/tappa -f visibility=public` → **404 (uç nokta yok)**. ⚠️ **Yerine gelen sayılmış limitler:** Docker Hub anonim çekme bütçesi (`x-ratelimit-limit: 100;w=3600`) · **etiket listesi herkese açık ve her etiket `sha-<12hex>`, yani üretimde hangi commit'in koştuğu dışarıdan okunabilir** (M8-01'in *"derleme kimliği herkese açık uç noktaya çıkmıyor"* kararının yeni bir kanaldan delinmesi; imaj **içeriği** sır taşımıyor — `docker export` ile sayıldı: `/tappa` + CA paketi, migrate'te `/goose` + zaten public 20 `.sql`) · ve **kapı yalnız deploy anını ölçer**, bir depoyu sonradan private'a çeviren kimseyi engellemez. 🔴 **AMA "KAPANDI" AĞAÇ İÇİN DOĞRU, KÜME İÇİN DEĞİL — ve bunu ayırmamak bu oturumun imza kusuru olurdu.** Ölçüldü (2026-08-16, denetçi): `gh secret list` → **yalnız `KUBE_CONFIG`** · `hub.docker.com/.../atknatk/{tappa,tappa-migrate}` → **404, 404** · kümede `secret/ghcr` **VAR** ve **üç ReplicaSet'in üçü de** `ghcr.io/...` + `imagePullSecrets: ghcr`. Yani bugün bir deploy **1. adımda** durur ve tarif edilen durum **ulaşılamaz**. | İlk canlı deploy (2026-08-15) · kök neden + çare 2026-08-16 | ✅ ağaçta · 🔴 **kullanıcı: 2 secret + 2 Public depo** |

| **T44** | 🔴 **DEPLOY'UN KENDİ RBAC'İ REPODA YOK — VE İÇİNDEKİ BİR EKSİK, BİR KAPIYI DOĞDUĞU GÜNDEN BERİ ÖLÜ TUTUYORDU.** `0ce5615` ilk kez migration'ı geçirdi (`Complete 1/1`, **8 sn**) ve deploy **19. adımda** durdu: `pods "tappa-postgres-0" is forbidden: User "system:serviceaccount:tappa:github-deployer" cannot create resource "pods/exec"`. Kapı **doğru davrandı** — *"the pg_stat_activity query did not run, so NOTHING was checked. This is not a pass."*, exit 2, fail-closed. 🔴 **Ama asıl bulgu şu: `db-role` kapısı ÜRETİMDE HİÇ KOŞMAMIŞ.** Ölçüldü: `Role/github-deployer` `pods`'a tüm fiilleri veriyor ama **`pods/exec` ayrı bir alt kaynaktır ve listede yok**; önceki her deploy migration'da düştüğü için 19. adıma **hiç ulaşılmamıştı**. Yani *"uygulama asla migration rolüyle bağlanmıyor"* kriterinin **tek çalışma-anı kanıtı**, yazıldığı günden beri **hiç ölçmemiş**. **Bu oturumun imza sınıfının altıncı örneği: ölçülmeyen yol, normal yoldu.** 🔴 **İKİNCİ YARISI DAHA AĞIR: `Role`/`RoleBinding`/`ServiceAccount` üçü de yalnız KÜMEDE var** (`2026-08-15T20:15:45Z`, elle yaratıldı), **repoda hiçbir izi yok** (`grep -rln 'github-deployer\|kind: Role' deploy/` → **boş**) — yani (a) taze bir küme kurulumu deploy'u **hiç koşturamaz**, (b) bu rolü **hiçbir denetim görmedi**, ve (c) rol `ns/tappa`'da `secrets`'a **tam CRUD** veriyor, yani **`tappa-secrets` ve içindeki `TAPPA_TAG_KEK`** (§4.7) bir iş akışının patlama yarıçapında ve **bunu yazan bir dosya yok**. ✅ **KAPANDI — M8-02 FAZ D (2026-08-16), `deploy/k8s/01-rbac.yaml`.** 🔴 **VE YUKARIDAKİ ÖLÇÜM EKSİKTİ, YAPICI DÜZELTTİ: kümede ÜÇ değil BEŞ nesne var** — `Role`/`RoleBinding`/`ServiceAccount`'a ek olarak `ClusterRole/tappa-namespace-only` + `ClusterRoleBinding`'i (`namespaces`, `resourceNames:[tappa]`), beşinin de `creationTimestamp` **aynı**. Beşi de artık dosyada, ve **ölçerek daraltıldı**: kum havuzunda yalnız önerilen kurallara bağlı bir SA yaratılıp `deploy.yml`'in **19 küme çağrısı** o kimlikle yeniden koşuldu (⚠️ ilk rapor **15** dedi ve yanlıştı — o sayı rehearsal script'inin **adım numaralandırmasıydı**, çağrı sayısı değil; iki denetçi bağımsız olarak **19** saydı: adım 14→2 · 15→5 · 16→5 · 17→3 · 18→1 · 19→3, `version --client`/`config current-context`/`--dry-run=client` hariç, `db-role`'un `exec`'i dahil). **`secrets` artık tek ad tek fiil** (`resourceNames:["tappa-secrets"]`, `verbs:["get"]`) — canlı rol **yedi fiil + `resourceNames` yok** veriyordu. Çıkarılanlar: `pods` create/update/patch/watch/**get** (`logs`/`exec`/`get pods` üçü de **LIST** üzerinden çözülüyor, `-v=8` ile ölçüldü) · `persistentvolumeclaims` · `serviceaccounts` · `events` · `replicasets` · `*/status` · `externalsecrets` · `jobs patch` · `namespaces create`. **İki yetki taşıyıcı çıktı ve düşürülmesi FAIL-OPEN üretiyordu:** `apps: watch` yokken `rollout status` **exit 0 veriyor ama stderr'de `Failed to watch … is forbidden`** — yani **hiçbir şey gözlemlemeden geçiyor**; `namespaces: patch` ise **naif testte** gizleniyor (değişmemiş namespace `get` ile *"unchanged"*, exit 0; **gerçek bir etiket değişikliğiyle** exit 1). **`pods/exec` (a) yoluyla verildi ama KAPSAMLANARAK**: `resourceNames: ["tappa-postgres-0"]` — ortamında `TAPPA_TAG_KEK` bulunan uygulama pod'unda **kabuk yok**; zorlanabilirliği iki bağımsız ölçümle doğrulandı (ad gövdede değil **URL yolunda**). **Sayılmış limit, ve dürüst olduğu ayrıca kanıtlandı:** kimlikte zaten `jobs: create` + `pods/log: get` var ve bir pod şablonundaki `secretKeyRef` **yaratıcıya karşı yetkilendirilmez** (kubelet çözer) → sırrı okumak **zaten açıktı**; daraltma **yıkıcı yarıyı** kaldırıyor. ⚠️ **Ve bir sınır dürüstçe açık:** `KUBE_CONFIG` secret'ının **içindeki kimlik doğrulanamadı** — hâlâ cluster-admin bir kubeconfig ise bu daraltma **hiçbir şeyi kısıtlamaz**, çünkü cluster-admin RBAC'e bakmaz. 🔴 **KULLANICI EYLEMİ:** `01-rbac.yaml` deploy tarafından **uygulanamaz** (rolü değiştirmek `rbac.authorization.k8s.io` yetkisi ister ve **yok — doğrusu da bu**), yani `tappa-secrets` gibi **operatörün bir kez** cluster-admin ile uygulaması gerekiyor. ~~**Çare:** RBAC'i `deploy/k8s/` altına al (**§4.7 merceğiyle daraltarak** — `secrets` tam CRUD gerçekten gerekli mi, yoksa `create`/`patch` yeter mi), ve `pods/exec`'i **yalnız gerekiyorsa** ekle. ⚠️ **Elle yamama:** T41/T42/T43'ün üçü de tam olarak *"kümede elle düzeltildi, manifest bilmiyor"* diye doğdu.~~ 🔴 **VE ORİJİNAL KUSURUN (c) YARISI BUGÜN HÂLÂ CANLI:** canlı `Role/github-deployer` `secrets`'a **yedi fiil, `resourceNames` yok** veriyor (ölçüldü), yani `TAPPA_TAG_KEK` **hâlâ** patlama yarıçapında ve `pods/exec` **hâlâ yok** → `db-role` kapısı sonraki deploy'da **yine düşer**. Daraltma **dosyada**, kümede değil. | `a7330ae` sonrası ilk başarılı deploy (2026-08-16), orkestratör ölçümü | ✅ ağaçta · 🔴 **operatör bir kez `apply` etmeli** |

| **T45** | 🔴 **ÜRÜNÜN HÂLÂ HİÇBİR YEDEĞİ YOK — VE M8-06 PİLOTU BUNA BAĞLI.** M8-02 FAZ E yedek ve **provalı** geri yükleme mekanizmasını üretti (`50-backup.yaml` gecelik CronJob · `pg-backup.sh` · `pg-backup-ship.sh` · `pg-restore-verify.sh`), ama **kümede hiçbiri yok**: `kubectl -n tappa get cronjob` → **No resources found** (ölçüldü). Aradaki fark **üç operatör eylemi**, deploy bunları yapamaz: **(a)** off-node bir **`crypt`** hedef seç ve `secret/tappa-backup-target` yaz (`rclone.conf` + `BACKUP_REMOTE`; **çıplak `remote:` reddediliyor**, yol şart) · **(b)** yeşil bir deploy'dan **sonra** `kubectl apply -f deploy/k8s/50-backup.yaml` ve ilk koşuyu elle tetikle (`kubectl -n tappa create job tappa-backup-first --from=cronjob/tappa-backup`) — deploy kimliğinde `batch/cronjobs` fiili **yok ve bilinçli olarak verilmedi** · **(c)** yedek parolasını **`TAPPA_TAG_KEK`'ten AYRI bir zarfta** emanete koy ve adım 9(b)'nin **sha256 önek** kontrolüyle doğrula. 🔴 **(c)'nin asimetrisi ölçüldü ve önemli:** KEK kaybında mesai kayıtları hâlâ okunabilir; **yedek anahtarı kaybında hiçbir şey okunamaz** — ve bunu tam olarak **küme gittiğinde** öğrenirsin. ⚠️ **Pilot öncesi kapatılmalı:** M8-06 bir haftalık **paralel kayıt** üretiyor ve o hafta **§4.3 gereği yeniden kurulamaz**. | M8-02 FAZ E (2026-08-16) · **ölçüm 2026-08-17** | 🟡 **operatör — ama BUGÜN ACİL DEĞİL, ÖLÇÜLDÜ.** Üretim veritabanı **boş**: `tenants=0 employees=0 transactions=0 tags=0`, `db=8974 kB` (yalnız şema). Bugün node ölse **kayıp sıfır** — `make migrate` ile geri gelir. 🔴 **Risk İLK GERÇEK KAYITLA başlar**, pilotla değil: `/signup` herkese açık ve ürün canlı, yani URL birine söylendiği an ilk işletmenin bordro delili **tek node'daki tek kopya** olur ve §4.3 onu yeniden üretilemez kılar. **Hazırlık durumu ölçüldü:** `configmap/tappa-backup-scripts` **kümede** (3 script) · `50-backup.yaml` `--dry-run=server` → **`created`** · eksik **tek şey** `secret/tappa-backup-target`. **Kullanıcı kararı (2026-08-17): hedef Cloudflare R2** (hesap zaten var, farklı sağlayıcı = gerçek patlama yarıçapı ayrımı, 16 GB ≈ aylık $0,09). Kurulum **pilot/ilk müşteri öncesine** ertelendi; o an gereken: bucket + Access Key ID + Secret + account ID. |
| **T46** | **ÇALIŞAN ÜRETİM VERİTABANINDA `ALTER DEFAULT PRIVILEGES` HÂLÂ GENİŞ.** FAZ E, geri yükleme artığının kökünü kapattı — `scripts/db-init/01-roles.sql` varsayılanı **`GRANT SELECT, INSERT`**'e daraltıyor, ve replay zinciri artık **ilk adımda ölüyor** (`ERROR: permission denied for table tags`). **Ama `01-roles.sql` bir init script'i, yani yalnız boş `PGDATA`'da koşar** — çalışan hiçbir veritabanını değiştirmez. Ölçüldü: `tappa-db`'de `pg_default_acl` hâlâ **`tappa_app=arwd`**. 🔴 **Ve dump'ın son ifadesi varsayılanı HER geri yüklemede yeniden genişletiyor** (`GRANT SELECT,INSERT,DELETE,UPDATE ON TABLES`), bu yüzden geri yükleme prosedürü (A YOLU adım 7 · B YOLU B3) **tek ifadelik bir yeniden daraltma** taşıyor. **Bugün gerçek bir açık DEĞİL** — 20 migration'ın hepsi istemediğini **açıkça REVOKE ediyor** (23 ifade) ve varsayılana dayanan tek tablo `goose_db_version`. Kusur **kapsam**ta: *"unutulan bir GRANT fail-closed"* vaadi yalnız **dar** varsayılanın yürürlükte olduğu veritabanlarında geçerli, yani **dev/CI üretimden KATI** ve ayrım kusuru **gizler yönde**. **Karar operatörün** (çalışan bir DB'de migration dışı DDL): `REVOKE UPDATE, DELETE ON TABLES` — komut `deploy/README.md` sınır 22'de, idempotent olduğu ve mevcut yetkileri bozmadığı **ölçüldü**. | M8-02 FAZ E (2026-08-16) | 🔴 **operatör kararı** |

| **T47** | ✅ **KAPANDI — M8-03 (2026-08-19, `ae07e7c`).** 🔴 **SEVK EDİLEN İKİ YORUM, TAŞIMADIĞI BİR ÖZELLİĞİ İLAN EDİYOR (M8-02 FAZ F'nin son denetiminden).** (a) `cmd/rotatekek/main.go` `SET LOCAL lock_timeout='5s'` satırının başlığı *"A MECHANICAL CEILING ON HOW LONG A LIVE TAP CAN BE HELD BEHIND THIS ROTATION"* diyor — **yanlış, ölçüldü**: `lock_timeout` yalnız **onu koyan oturumun kilit ALMA beklemesini** sınırlar; rotasyon satır kilitlerini tutarken **tap 10,07 sn bekledi**, pozitif kontrol (bekleyen taraf rotasyon) **5,05 sn**'de iptal oldu. `statement_timeout` sunucuda **0** ve hiçbir katmanda set edilmiyor, yani *"hiçbir katmanda tavan yok"* **hâlâ doğru**. Karar (rotasyonun fail-closed iptali) doğru, **gerekçe yanlış** — ya cümle ölçüme indirilmeli ya gerçek bir tavan (`statement_timeout`) konmalı. Ve yeni iptal yolu (`55P03`) **hiçbir operatör belgesinde geçmiyor**: runbook girdisi, semptom tablosu satırı ve *"yeniden koşmak güvenli"* cümlesi yok. (b) `cmd/rotatekek/main.go` `defer sun.Zero(key)` için *"a test can watch it happen"* diyor — **öyle bir test yok** (`cmd/rotatekek/*_test.go`'da sıfır iddia); mutasyon (`Zero` → no-op) süiti **yeşil** bırakıyor. §4.7 disiplini beyan ediliyor, **hiçbir şey tutmuyor**. | M8-02 FAZ F, 17. tur denetimi (2026-08-17) | **M8-03** |
| **T48** | **KEK ROTASYONUNUN ÇEVRESİNDE ÜÇ PİNLENMEMİŞ KORUMA VE İKİ DAR KAPSAM.** Hepsi **bugün canlı ihlal değil**, hepsi mutasyonla ölçüldü, hiçbiri ürünün çalışma zamanına değmiyor. **(1)** `ON_ERROR_STOP` taraması dört `psql` çağrısının **üçünü** görüyor — `BYPASS="$(psql …` satırı `HasPrefix("psql ")`'e takılmıyor, ama `deploy/README.md` bunu *"her psql çağrısında"* diye yayımlıyor (kardeş `-X` testi **doğru** kuralı kullanıyor). **(2)** `lock_timeout` **değeri** pinsiz (`'5s'` → `'5000s'` yeşil; yalnız varlığı iddia ediliyor). **(3)** Satır-başına runbook sözleşmesi yalnız **sondaki** yorumu görüyor: aynı yanlış iddiayı **bir üst yorum satırına** taşımak yeşil kalıyor, ve `OWNER_DSN=… ./scripts/…` biçiminde çıkarılan çağrı sayısı **sessizce 4→3** düşüp körlük kontrolünü ateşlemiyor. **(4)** Yapıştırma-tehlike dedektörü gerçek zsh'te **çalışan beş yapının ikisini** görüyor (`` ` `` ve `$(` kapsanıyor; `<(`, `>(`, `=(` **çalışıyor ve kapsanmıyor**), ayrıca **dengesiz `"`** (apostrofla birebir aynı etki) ve **satır sonu `\`** sonraki komutu yutuyor, ve **sondaki yorumlar hiç taranmıyor** (tarayıcı satırın `#` ile *başlamasını* istiyor; bölümde **16** böyle satır var). Bugün metinde **0** ihlal. **(5)** Sarkan-atıf envanteri **hâlâ büyüyebiliyor** (bütçeyi elle artırmak yeşil), oysa `known-dangling-citations.txt` *"buradan yalnız KÜÇÜLEBİLİR"* diyor — sayının kendisi (**53**) dürüst, iki yönlü bayatlık kontrolü çalışıyor. | M8-02 FAZ F, 15.–17. tur denetimleri (2026-08-17) | **M8-04** |
| **T49** | ✅ **KAPANDI — M8-03 (2026-08-19, `ae07e7c`).** **`LD_PRELOAD` İKİ KEK'İ TUTAN SÜRECE ULAŞIYOR.** `scripts/rotate-kek.sh` `PGOPTIONS PGSERVICE PGSERVICEFILE PSQLRC PGSYSCONFDIR` temizliyor (ölçüldü, psql çocuğu ikisini de `[unset]` görüyor) ve KEK **değeri** hiçbir psql çocuğuna geçmiyor. Ama **`LD_PRELOAD`** hem `go build`'e hem **iki KEK'i ortamında taşıyan tek sürece** (`$WORK/rotatekek`) ulaşıyor — kapatılan `PGOPTIONS` kanalından **kesinlikle daha güçlü**. (`DYLD_INSERT_LIBRARIES` bu makinede **işletim sistemi** tarafından düşürülüyor, script tarafından değil — yani Linux'ta koruma yok.) Ayrıca script **DSN biçimini hiç doğrulamıyor**: runbook'un URI DSN'i `PGHOST`/`PGPORT`/`PGDATABASE`/`PGUSER` tarafından ezilemez, ama çıplak `dbname=` bir DSN'de tüm koşu **başka bir sunucuya** yönlenir, guard'lar orada geçer ve gerçek park hiç dönmez. Ve `scripts/rotate-kek.sh` *"TWO CHANNELS SURVIVE -X"* diye **sayım yapıyor** — sayı yanlış. **Çare ucuz:** temizleme listesine `LD_PRELOAD` (ve `DYLD_INSERT_LIBRARIES`) ekle, DSN'in URI biçiminde olduğunu doğrula, sayımı ölçüme indir. | M8-02 FAZ F, 17. tur denetimi (2026-08-17) | **M8-03** |

| **T51** | **M6-13'ün KAPANIŞ DENETİMİNİN BIRAKTIĞI İKİ PİNLEME BOŞLUĞU — ürün doğru, ağlar dar.** Dokuz denetim turu, **son dördünde ürün davranışında sıfır bulgu**; kapanış turunda 42 mutasyonun 37'si yakalandı ve hayatta kalan beşin beşi de ölçümle açıklandı. Kalan ikisi: **(1) §4.7 sonuç tablosu, ekleme handler'ının okunamayan-form dalının yalnız `oversize` yarısını sürüyor.** Diğer yarı — **bozuk form** — `r.PostForm`'u çağıranın **adıyla DOLDURULMUŞ** bırakıyor (ölçüldü: `full_name=…&role=%zz` → `PostForm=map[full_name:[…]]`), ve harita üzerinden yazılmış bir sızıntı (`strings.Join(r.PostForm["full_name"], "")`) **hem R7b'ye hem tabloya görünmez**. **Bugün sızıntı yok** — pinleme boşluğu, kusur değil. **(2) R7b'nin kapsamı logger'ın ALICI adının `log`/`slog`/`fmt` yazılmasına bağlı.** Yedi sıradan yazım kaçıyor (`a.logger.Error` · `lg := a.log; lg.Error` · metot değeri · dilim/harita elemanı · gömülü alan · arayüz değişkeni). Bugün doğru **yalnız tesadüfen**: **347 logger alanının 347'si de `log` adında ve bunu hiçbir şey pinlemiyor**. R7b'nin limit bloğu **değer** tarafındaki tanımlayıcı bağımlılığını beyan ediyor, **alıcı** tarafındakini değil. 🔴 **İkisinin de gerçek cevabı aynı ve yapıcı onu ölçtü: TİP DÜZEYİ LOGGER REDAKSİYONU** — handler `*slog.Logger` tutmasın, yalnız güvenli tipleri (id · sayı · enum · sabit dize) alan dar bir arayüz tutsun, ki sızıntı **derlenmesin**. Emsal repoda var: `Trail` yalnız `RecordTx` sunuyor, audit'i transaction dışına almak **derlenmiyor**. Maliyet **ölçüldü: `internal/handler`'da 22 dosyada 224 çağrı yeri + `internal/domain`'de 46** — bir kartın işi değil, **paket çapında bir dönüşüm**. | M6-13 kapanış denetimi (2026-08-18) | **M8-04** (güvenlik denetimi) |
| **T52** | **YEREL GELİŞTİRME VERİTABANI, MUTASYON KOŞULARININ YAZDIĞI 128 `employee.added` AUDIT SATIRI TAŞIYOR — ve `audit_log` append-only olduğu için SİLİNEMEZ.** M6-13'ün dokuz denetim turunda ajanlar, §4.7 ağını sınamak için kasten sızdıran kod mutasyonları koşturdu; bunların bir kısmı `audit_log.detail`'e **ad ve e-posta** yazdı. Ölçüm (kapanış denetçisi): **128 satır**. ⚠️ **Yalnız yerel dev DB** — üretimde sevk edilen yolun `detail` şekli `department_id · has_email · has_role · location_id · status`, yani **ne ad ne adres** (5877 satırda doğrulandı). Zarar sınırlı ama **kayıt için**: bir mutasyon koşusu, append-only bir tabloya yazdığında **geri alınamaz**, ve denetçi brief'lerindeki *"sondaların rollback'li olsun"* kuralı **mutasyonların ürettiği yan etkileri kapsamıyor**. **Çare:** ya mutasyon koşuları atılabilir bir kopyada yapılsın, ya `make seed`'in DB'yi tazelemesi bir prosedür olsun. **Ayrıca kayda geçsin:** aynı denetçi, repoda **hiçbir yerden atıf almayan** bir `tappa_demo` veritabanı buldu; kökenini oradan kuramadı. | M6-13 kapanış denetimi (2026-08-18) | **M8-04** |

| **T53** | **SABİT ZAMANLI KARŞILAŞTIRMA ONBEŞ YERDE KULLANILIYOR, YALNIZ BİRİNİ BİR TEST TUTUYOR — ve `make audit` bunu düşürmüyor.** M2-08'de iki ajan bağımsız ölçtü: `internal/sun/verify_mac.go`'da `subtle.ConstantTimeCompare` → `bytes.Equal` mutasyonu **tüm süiti geçiyordu**, ve `redline-check.sh` R7 bunu **WARN + exit 0** ile basıyor (`make audit` `redline` çıkışını `-eq 0` diye sınıyor, yani WARN **hiçbir kapıyı düşürmez**). ✅ **O tek yer M2-08'de kapatıldı** — `verify_mac_test.go` artık `verify_mac.go`'nun kaynağını okuyup `verifyMAC`'in gövdesinde `crypto/subtle` import'unu, çağrıyı ve `== 1` yazılışını **allowlist** olarak iddia ediyor (denylist kasten reddedildi: `string(a) == string(b)` yazılışı R7'nin desenine **sıfır** isabet veriyor, ölçüldü). 🔴 **Kalan on dördünü hiçbir şey tutmuyor** ve bunlar **saldırgan tarafından beslenen** karşılaştırmalar: CSRF token'ları (`cookies.go:261`, `logincontext.go:284`, `signupstate.go:194`), imzalı çerez doğrulamaları (`tapcontext.go:355`, `logincontext.go:563`, `signupstate.go:357`, `deactivateconfirm.go:409/465`), parola sıfırlama token'ı (`adminreset.go:1224`), çalışan eylem onayı (`employeeactions.go:815`), tenant eşleşmesi (`logincontext.go:633`). ⚠️ **İki tanesi ayrı sınıf ve daha hafif** (`config.go:376/396`): ikisi de **iki sırrı birbiriyle** karşılaştırıyor (oturum anahtarı ↔ davet anahtarı, KEK ↔ önceki KEK), yani zamanlama kanalını sürecek bir saldırgan girdisi yok — yine de aynı ağın altında olmalı. **Çare iki şekilden biri:** (a) `verify_mac_test.go`'nun kaynak-okuma emsalini genelleştiren tek bir test (**kapsam: `subtle` import eden her üretim dosyası**), ya da (b) R7'yi `internal/` altında **WARN'dan FAIL'e** çekmek — ⚠️ ama (b) yalnız *bilinen yazılışları* kapatır, `string(...) == string(...)` sınıfını **kapatmaz**; M2-08 tam bu ölçümle (a)'yı seçti. **Sayılmış limit (M2-08'in kendi testinde yazılı):** kaynak okuyan bir test **metni** okur, **semantiği** değil — karşılaştırmayı başka bir dosyadaki yardımcıya taşımak ağdan kaçar. | M2-08 kapanış ölçümü (2026-08-19), iki ajan bağımsız | **M8-04** (güvenlik denetimi) |

| **T54** | **M8-03'ÜN İKİ SAYILMIŞ LİMİTİ — ikisi de "sayı bayatlar ve kimse görmez" sınıfı.** **(1)** `TestObservability_AlertSignalNames` olay adlarını **alt-dize** ile arıyor (`strings.Contains`), yani bir adın **sonuna** karakter eklemek testi **düşürmüyor** — ölçüldü: `tap.decision` → `tap.decisionMUT` **geçti**, `tap.verdictline` **düştü**. Uyarı kuralları bir olay adının **tam** eşleşmesine dayanıyor, yani bu ağ bir yeniden adlandırmanın yarısını görüyor. **(2)** Bu oturumda **dört bayat sayı** düzeltildi (`*Context` 33→32 · çıplak grep 26→27 · çok satırlı 131→137 · yapıştırılabilir blok 45→50) ve **hiçbirini bir kapı korumuyor** — üçü mutasyonla sınandı, üçü de yakalanmadı. Tek fren, `cmd/tappa/main.go`'ya yazılan ölçüm komutu. ⚠️ Bu, `deploy/README.md` sınır 23'ün (*"bu belgedeki atıflar mekanik olarak doğrulanmıyor"*) **sayısal yüzü** ve aynı oturumda **üç ayrı turda** bulgu üretti. **Çare ölçülmeli:** ya sayıları belgeden çıkarıp ölçüm komutuna indirgemek, ya `testnames_test.go`'nun atıf tarayıcısını sayılara genişletmek (maliyeti ölçülmedi). | M8-03 kapanış denetimleri (2026-08-19), üç ayrı ajan | **M8-04** |

| **T55** | 🔴 **REPODAKİ İKİ TAŞIYICI SCRIPT'İN ARKASINDA HİÇBİR OTOMATİK KAPI YOK — VE BİRİ VERİ SİLİYOR.** `scripts/db-reset.sh` (YENİ, `docker compose down --volumes` çalıştırıyor) ve `scripts/pg-restore-verify.sh` §5 (restore sonrası §4.3 guard'larını doğrulayan bölüm) hiçbir test tarafından okunmuyor. Ölçüldü: `grep -rn "db-reset.sh\|pg-restore-verify" --include=*_test.go` → **sıfır** · kabuk script'i exec eden **tek** test `cmd/rotatekek/script_test.go` ve yalnız `rotate-kek.sh`'i hedefliyor · `redline-check.sh:232` `pg-restore-verify.sh`'i **adıyla muaf** tutuyor · `make check = fmt gen lint test`, hiçbiri `.sh` okumuyor · CI `db-reset`/`pg-restore-verify` **koşmuyor**. 🔴 **Somut sonuç, mutasyonla ölçüldü (M6):** `db-reset.sh`'ten `-f`/`-p` pini kaldırıldığında script **yabancı bir compose projesinin volume'unu SİLDİ**, banner hâlâ *"pinned"* diyordu, ve `make check` · `make audit` · `redline-check` · CI'nin **hepsi yeşil kalırdı**. Üç metin mutasyonu (§5 PASS cümlesi yalan söylesin · *"⚠️ NOT MEASURED"* → *"✅ MEASURED"* · yanlışlanan *"50 bayt"* geri konsun) ve `docker-compose.yml`'den `name: tappa`'nın silinmesi de **hiçbir kapı tarafından görülmüyor**. ⚠️ **Yapıcının *"rig kurmak kapsam dışı"* savunması BU REPODA GEÇERSİZ:** `cmd/rotatekek/script_test.go` **1566 satır** ve tam olarak aynı risk sınıfını kapatıyor (`…UnrecognisedArgvNeverSelectsTheDestructiveBranch`, `…KeepsItsStructuralGuards` — script içindeki **literal** satırları assert ediyor, `…ParsesUnderBash`). Desen hazır; ucuz hâli ~20 satır. **Bugün sevk edilen kod ölçülerek doğru** (guard kaldıran 13 davranış mutasyonunun **13'ü öldü**); boşluk **gelecekteki regresyona** dair ve patlama yarıçapı geliştirici dizüstüyle sınırlı. | M8-04 FAZ B1 kapanış denetimi (2026-08-19), iki ayrı ajan | **M8-04 FAZ B3** ya da M9 |

| **T56** | **`db-reset.sh`'in yerellik sondası REDDETTİĞİ HEDEFTE YAZMA YAPIYOR, ve script kendi kuralını bir satırda çiğniyor.** **(1)** Guard, daemon'a bu checkout'un `01-roles.sql`'ini okutup baytları `cmp`'liyor (`scripts/db-reset.sh:202-211`, `compose run --rm`) — ama bu **salt okuma değil**. Ölçüldü (`probeleak` adlı, o ana kadar var olmayan bir proje): **reddedilen** koşumdan sonra `probeleak_default` **network**'ü ve `probeleak_probeleak-pgdata` **volume**'ü ortaya çıktı. Yani gerçekten uzak bir dockerd'de guard, **dokunmamak için var olduğu makineye** boş bir volume ve bir network yazıp sonra reddediyor. Yıkıcı değil; ama başlıktaki *"sayılmış limitler"* paragrafı bunu **saymıyor**. (Orphan konteyner **bırakmıyor** — `--rm` çalışıyor, ölçüldü.) **(2)** `scripts/db-reset.sh:266` operatöre `docker compose -p tappa logs db` diyor — proje adı **`$project` değil, gömülü `tappa`**, ve `-f docker-compose.yml` **yok**. Oysa aynı dosyanın 183-187. satırları şunu yazıyor: *"Ad TEK bir değişkende tutulur ve AYNI değişkenden basılır. İlk taslak iki kez yazdı … ve anons yalan söylerken yakalandı."* `project=` değiştirilirse bu operatör talimatı **sessizce yanlış projeyi** işaret eder — dosyanın kendi teşhis ettiği hatanın aynısı, bir satır ilerde. | M8-04 FAZ B1 kapanış denetimi (2026-08-19) | **M8-04 FAZ B3** |
| **T57** | 🔴 **`TestAppendOnly_TruncateCascadeIsRefusedToo/tenants_cascade` PARALEL KOŞUDA DEADLOCK VERİYOR — VE CI TAM O ŞEKİLDE KOŞUYOR.** M8-04 FAZ B3'ün son turunda ölçüldü: tam `go test -race ./...` koşusunda alt test `deadlock detected (SQLSTATE 40P01)` ile düştü, append-only tetikleyicisiyle değil. Sebep yapısal: `TRUNCATE tenants CASCADE` **tenants'a ve onu referanslayan her tabloya ACCESS EXCLUSIVE** alıyor, yani suite'in geri kalanıyla yarışıyor. **İzole koşumda 4/4 yeşil**, üç paketlik paralel koşumda yeşil, ikinci tam koşumda yeşil — yani **flake**, regresyon değil. Testin kendisi FAZ B1'de (`6f4df34`) sevk edildi. ⚠️ **Ürün kusuru değil** (tetikleyici doğru çalışıyor, §4.3 sağlam) ama **CI'ı rastgele kırar** ve kırdığında sebebi *"append-only bozuldu"* diye okunur — yanlış teşhise davetiye. Muhtemel çözüm: bu alt testi kendi seri fazına almak (`t.Parallel()` çağırmamak yetmiyor — sorun paket **dışı** eşzamanlılık), ya da `lock_timeout` ile hızlı ve **açık** bir hata mesajına çevirmek. | M8-04 FAZ B3 son turu (2026-08-20), yapıcı kendi bildirdi | ✅ **KAPANDI 2026-08-20** (kullanıcı kararı: *şimdi düzelt*). 🔴 **Kök sebep sevk edilmiş korumanın ÖLÜ olmasıydı:** dosyada zaten `SET LOCAL lock_timeout = '30s'` vardı ve *"çekişmeyi teşhis edilebilir `55P03`'e çevirir"* diye söz veriyordu — ama **hiç ateşleyemezdi**, çünkü Postgres'in deadlock dedektörü `deadlock_timeout`'ta (**1 sn**) koşuyor; bekleme eşitsizliğin **yanlış tarafındaydı, 30 kat**. Flake iki kez **üretildi** (biri iki `psql` oturumuyla deterministik: kurban **seyirci** oturum çıktı, yani uzun bekleme **başka bir paketin masum testini** düşürebiliyordu). Ölçülen alternatif `-p 1`: **599 sn ↔ 324 sn = 1,85×, her `make test`'e +275 sn**; seçilen çözüm **25 ms `lock_timeout`** — süre maliyeti **sıfır**, ve sonda her kilidi hiçbir bekleyenin dedektörü koşmadan bırakıyor. Sayı **bağlandı**: `TestAppendOnly_TruncateProbeCannotOutwaitTheDeadlockDetector` iki operandı da **canlı** okuyup `ilişki × timeout < deadlock_timeout` şartını uyguluyor (17 × 25 ms = 425 ms ↔ 1 sn). Cascade garantisi **düşürülmedi**; tükenme `t.Fatal`, **`t.Skip` değil**. Üç tam suite koşusu temiz |
| **T58** | **ÇALIŞANIN TAP ONAY EKRANI, KARARI VEREN KURALIN CÜMLESİNİ KATMAN İŞARETİ OLMADAN BASIYOR.** Fiş (docket) ve rapor `NoteIsTenants` ile *"bu kararı tenant'ın kuralı verdi"* diye işaretliyor; `web/templates/pages/result.templ` işaretlemiyor. **Bugün zararsız ve bu ölçüldü:** tenant serbest metin `reason` **yazamıyor** (`copyOfShipped` ifadeyi bütün kopyalıyor, `AuthorCommand`'da alan yok, formda alan yok), yani bugün o ekranda görünen her cümle **bizim**. **Risk ileriye dönük:** Q22/M9-07 bir ham-JSON kural editörü getirirse `reason` müşteri tarafından yazılabilir hâle gelir ve o cümle hem çalışanın ekranına hem **değişmez** bir `transactions` satırına düşer (§4.3). 🔴 **KAPATILMADI ÇÜNKÜ §9:** *"çalışan tap ekranı kutsaldır — tek ekran, tek buton… bu ekrana özellik eklemek istiyorsan önce sor."* Kapanış önerisi hazır ve **uygulanmadı**: M4-04'ün `appendNote`'u tenant kararlarına bir ek yazsın. **Ürün kararı — kullanıcıya sorulacak.** | M8-04 FAZ B3 (2026-08-20), güvenlik merceği | **M9-07 ile birlikte** ya da kullanıcı kararı |
| **T59** | **ÇALINMIŞ BİR TAP OTURUMUNU İPTAL EDEBİLECEK HİÇBİR EKRAN YOK.** Ölçüldü: `internal/session/manager.go`'nun `Revoke` (`:326`) ve `ListForEmployee` (`:386`) metotlarının **üretimde SIFIR çağıranı** var; `RevokeAllForEmployee`'nin tek çağrı yeri `activate.go:458`, yani tek çare **yeniden aktivasyon** (müdürün ürettiği davet gerekiyor — self-servis değil). ⚠️ **Çalınmış çerez tek başına kayıt yazamıyor** (`POST /api/checkin` sunucunun ürettiği ve oturum kimliğine bağlı bir `ctx` istiyor; o bağlam yalnız geçerli SUN arkasındaki `GET /t`'den geliyor, `checkin.go:176`), panele ulaşamıyor, tenant geçemiyor. 🔴 **AMA ÇEREZ PRATİKTE TEK BAŞINA DEĞİL:** §5 QR'ı *"fotoğraflanır ve süresiz geçerlidir, sayaç yok"* diye tanımlıyor — **çalınmış çerez + fotoğraflanmış bir QR** hırsıza `GET /t` ile taze bir `ctx` bastırır ve kayıt yazdırır; mekânın ağından `ok, trust=70`. 15 dakikalık `ctx` TTL'i fren değil, her `GET` yenisini basıyor. **§4 ihlali değil** (kayıt yazılıyor, tenant sınırı geçilmiyor, bir çare var) ama ADR 0005'in kabul edilen riskleri **bunu içermiyor**: risk 1 *gönüllü* devri anlatıyor. ⚠️ *(Bu cümle 2026-08-20'ye kadar **"altı** kabul edilen risk" diyordu; sicil aynı gün **sekize** çıktı — M8-05 FAZ B1'in risk 7 ve 8'i. Sayı buradan **çıkarıldı**, çünkü bir sicilin uzunluğunu üçüncü bir dosyada tekrarlamak tam olarak bu satırın bayatlamasına yol açtı; sayıyı bağlayan kapı artık `TestADR0005_TheRiskCountMatchesTheTable`.)* **Ürün kararı.** | M8-04 FAZ B3 (2026-08-20), güvenlik merceği | kullanıcı kararı → M9 |

| **T60** | 🔴 **`redline-check.sh` `docs/` VE `.claude/`'YA HİÇ BAKMIYOR — yani mekanik ağ, M8-05 FAZ B1'in ASIL ÜRÜNÜNÜN üzerinden hiç geçmedi.** `scripts/redline-check.sh:30` SRC listesi: `cmd internal db web/templates web/static/js scripts test deploy .github`. Sonuç: R1/R2/R3/R4/R5/R7/R7b/R7c desen kuralları bir ADR'nin, bir görev kartının ya da bir **skill dosyasının** üzerinden **hiç koşmuyor** — ve CLAUDE.md §11 her NFC işini bir skill dosyasına yolluyor, yani orada duran yanlış bir cümle **talimat** diye okunuyor (bu tam olarak yaşandı: `tappa-sun` SKILL.md *"ölçekte encode'lu tedarikçiden alınır, anahtarlar bize teslim edilir"* diyordu — Q10'un tersi, ADR 0003 md.3'ün reddettiği tek-nokta felaketi). ⚠️ **Patlama yarıçapı ÖLÇÜLDÜ** (2026-08-20, script değiştirilmeden, desenler `paths=(docs .claude)` ile elle koşuldu): toplam **366** isabet — R1 **21** · R2 **5** · R3 **7** · R4a **0** · R4b **30** · R6 **5** · **R7 298** · R7b/R7c/R8 **0**. 🔴 **298'in tamamı bir bulgu değil, bir DESEN UYUMSUZLUĞU:** R7 `(slog|log|fmt)\.Xxx(` arıyor ve nesirde bu dizeler **alıntı** olarak geçiyor. Ölçülmüş dar çözüm: `docs/` + `.claude/` yalnız **R1/R2/R3/R6** için taransın → **38** isabet, hepsi elle triyaj edilebilir; R7 ve R4b kod dizinlerinde kalsın. ⚠️ **M8-05 FAZ B1'de bilinçli olarak UYGULANMADI**: mekanik ağı, denetlemesi gereken dokümanla **aynı commit'te** değiştirmek kendi ödevini kendi notlandırmaktır. | M8-05 FAZ B1 tur 1 (2026-08-20), güvenlik merceği | M9 ya da ayrı bir hijyen turu |

| **T61** | **Gömülü-anahtar kuralı `git ls-files` kullanıyor → UNTRACKED bir sır dosyası GÖRÜNMÜYOR.** `scripts/redline-check.sh`'in repo geneline bakan tek kuralı (`:1704-1705`) dosya listesini `git ls-files`'tan alıyor. Ölçüldü (ayrı bir scratch repo'da, 2026-08-20): takip edilmeyen bir `leak.pem` için sonuç **boş**; `git add` sonrası aynı dosya **listeleniyor**. Yani `make audit` yeşilken çalışma ağacında henüz eklenmemiş bir `.pem`/`.key` durabilir — ve tam da bu, bir şeyin **yeni yazıldığı** andır. ⚠️ Somut örnek: M8-05 FAZ B1'in ADR'si tüm denetim boyunca **untracked**'di, yani o kural onun hakkında **hiçbir şey kanıtlamadı**. §4.7'ye değiyor. Çözüm `git ls-files --others --exclude-standard` eklemek ya da dosya sistemini yürümek; ikisi de `.gitignore`'u yok saymamaya dikkat ister. | M8-05 FAZ B1 tur 1 (2026-08-20), güvenlik merceği | M9 ya da T60 ile birlikte |

| **T62** | **AN12196 revizyon eşleme sicili DÖRT TURDA DÖRT KEZ yanlışlandı — elle bakılan bir sicil bu hızı taşımıyor.** `internal/sun/an12196_kat_test.go:69`'un tablosu *"Every pair this repository cites, measured"* diyordu ve M2-08'den beri repo genelindeki AN12196 atıflarının rev 1.8 ↔ rev 2.0 karşılıklarını tutuyor (gerekçesi gerçek ve ağır: **rev 2.0'ın §6'sı 'Personalization example' değil 'Special functionalities'** — revizyonsuz bir atıf **başka bir bölümü** gösterir). Ölçüldü: tur 2 → §6.14 eksik · tur 3 → §7/§6 ve T27 eksik · tur 4 → sayı *"üç"* yazıldı, §6.5 eksik · tur 5 → dördüncü kez. **Her tur çift eklendi, her tur yeni bir atıf kaçtı.** M8-05 FAZ B1'de *"kapatma, say"* uygulandı: **evrensel iddia DÜŞÜRÜLDÜ**, tablo ne olduğuyla (elle bakılan, **eksik olduğu bilinen** kolaylık listesi) adlandırıldı. **Kalan iş mekanik kapıdır** ve şekli hazır: atıf ratchet'i (`cmd/tappa`, `TestEveryNamedTestExists`) zaten **537 metin dosyasının tamamını** yürüyor — aynı yürüyüşe *"bulunan her AN12196 bölüm atfı bu tabloda olmalı"* koşulunu eklemek bir uyarı sicilini **kapıya** çevirir. ⚠️ FAZ B1'de yazılmadı çünkü o tur **kod turu değildi**. | M8-05 FAZ B1 tur 1–5 (2026-08-20), dört bağımsız denetçi | M8-05 FAZ B2 ya da M9 |

---

## Kapananlar

*(T47 ve T49 satırlarında ✅ ile işaretli — M8-03, `ae07e7c`)*
