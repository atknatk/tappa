# Ajan brief'leri — yapıcı ve üçüncü göz

Bu dosya [README.md → Çalışma modu](README.md)'nun uygulama detayıdır: alt
ajanlara verilecek görev tarifinin **şekli** ve her turda tekrarlanan kurallar.

**Neden yazılı:** brief'in şekli ilk turlarda sohbette taşınıyordu. Bağlam
sıkıştırılınca kaybolur ve disiplin sessizce gevşer — kalıcı olmak zorunda.

---

## Sabit kurallar — her yapıcı brief'inde tekrarlanır

Bunlar kartın içeriğinden bağımsız, her görev için geçerli:

1. **Önce oku:** `CLAUDE.md` (ilgili bölümler + §4 kırmızı çizgiler) · görev
   kartı · kartın atıf yaptığı dosyalar. Kart bir skill/agent gösteriyorsa onu da.
2. **Sır yazdırma.** Anahtar, token, CMAC, davet kodu, tam GPS — ne çıktıya, ne
   log'a, ne dosyaya. Doğrulama değeri göstermeden yapılır (uzunluk, hash öneki,
   eşitlik boolean'ı).
3. **`docs/plan/state.md` ve `roadmap.md`'ye dokunma** — orkestratörün.
4. **Commit atma** — orkestratörün.
5. **Süreç hijyeni:** sunucu başlattıysan **süreç grubunu** öldür (`set -m` +
   `kill -- -$PID`) ve **portla** doğrula (`lsof -nP -iTCP:8080 -sTCP:LISTEN`).
   Süreç **adı desenine güvenme** — `go run` bir sarmalayıcıdır, gerçek ikili
   build cache'tedir ve yolunda `exe/` **geçmez**.
6. **Kart gerçekle çelişiyorsa kartı düzelt**, gerekçesini görünür bir blokla yaz:
   `> **Kart düzeltmesi (TARİH, MX-YY uygulaması sırasında).**` — biçim örneği
   M0-01, M0-02, M0-04 kartlarında.
7. **Başarısızlığı gizleme.** Bir adım patladıysa patladığını söyle; "geçmiş
   gibi" raporlama denetimde çıkar ve tur tekrar edilir.
8. **Kapsam dışına çıkarsan açıkça işaretle** ve gerekçesini yaz. Sessiz genişleme
   yasak; gerekçeli genişleme kabul edilebilir.
9. **Teslim:** her komutun **gerçek** çıktısı · kabul kriterlerinin her biri için
   kanıt · `git status --short` ile kapsam · sapmalar.

## Sabit kurallar — her denetçi brief'inde tekrarlanır

1. **Denetçi, işi yapan ajan olamaz.** Kendi işini denetleyen ajan kendi
   varsayımlarını doğrular.
2. **Hiçbir dosyayı değiştirme.** Tek istisna: kendi başlattığın süreçleri
   temizle, portu boş bırak; sondajını repo **dışında** yap ve sil.
3. **Yapıcının raporuna güvenme.** Her ölçülebilir iddiayı **kendi komutunla**
   yeniden üret.
4. **`exit 0` kanıt değildir.** Üretilen kod derleniyor mu (`go build`, `go vet`)?
   Araç "başarılı" deyip kullanılamaz çıktı verebilir — bu vaka gerçekten yaşandı
   (`sqlc.yaml` `inet` override'ı).
5. **Sondaj asgari değil, kapsayıcı olsun.** Bir config'i doğrularken **tüm**
   yollarını tetikle. Asgari örnekle sınamak, yapıcının M0-04'te üç bozuk
   override'dan ikisini kaçırmasının sebebiydi.
6. Her bulgu: **ne · nerede (`dosya:satır` veya komut) · somut sonucu ne.**
7. **Övgü ve özet yazma.** Emin olamadığını "temiz" sayma — "doğrulanamadı" de.
8. Kapsam dışı gerçek sorun görürsen yaz, ama görevi **bloklayıp bloklamadığını
   açıkça ayır**.
9. **Çıktının son satırı** tam olarak şu iki formattan biri:
   `VERDICT: ONAY` · `VERDICT: RED — <tek cümlede en ağır bulgu>`

---

## Yapıcı brief şablonu

```
Tappa projesinde **<ID>** görevini uygula. Repo: <yol> · dal: <dal>

## Önce oku (zorunlu)
1. CLAUDE.md — <ilgili bölümler> + §4
2. docs/plan/<milestone dosyası> → <ID> kartı
3. docs/plan/agent-brief.md → "Sabit kurallar — yapıcı"
4. <kartın atıf yaptığı dosyalar>

## Görev
<kartın özeti — kartı kopyalama, ajan zaten okuyacak>

## Bu göreve özel kurallar
<kartın tuzakları + bu turda geçerli yasaklar/yetkiler>

## Doğrulama
<kartın kabul kriterleri + ek regresyon: go build, go vet, redline-check>

## Teslim
<sabit kurallar madde 9>

Not: Ardından bağımsız bir denetçi ajan her şeyi baştan doğrulayacak.
```

## Denetçi brief şablonu

```
Sen Tappa projesinde ÜÇÜNCÜ GÖZ DENETÇİSİSİN. Repo: <yol> · dal: <dal>

<ID> görevi <n>. turda. <önceki turlarda ne bulundu>.

## Önce oku
1. docs/plan/<milestone> → <ID> kartı (bu turda değiştiyse söyle)
2. docs/plan/agent-brief.md → "Sabit kurallar — denetçi"
3. CLAUDE.md §4 + <ilgili bölümler>
4. <ilgili dosyalar>

## Yapıcı ajanın iddiaları
<madde madde, ölçülebilir biçimde>

## Denetim listesi
<kabul kriterleri tek tek + kapsam + regresyon + süreç/port + sızıntı>

## Çıktı biçimi — zorunlu
<sabit kurallar madde 9>
```

---

## Orkestrasyon ritmi — M5'te fiilen uygulanan tur döngüsü

> **Neden yazılı:** [README.md](README.md) *ne* yapılacağını söylüyor (yapıcı → üçüncü göz → onay).
> Bu bölüm M5-01…M5-05'te **fiilen işleyen** ritmi kaydeder. Beş görevde **9 RED** çıkardı; ritim
> gevşerse o RED'ler kaçar. Bağlam sıkıştırılınca bu bilgi sohbette kalmaz — kalıcı olmak zorunda.

**Bir görevin turu:**
1. **Orkestratör kartı ve devirleri okur**, brief'i yazar. Kart gerçekle çelişebileceği için brief
   *"çelişirse kartı düzelt"* der. **Bilinen tuzakları brief'e önceden koy** — M5-05'te "baseline
   materyalize değil, ölç" uyarısı bloklayanı ilk turda buldurdu.
2. **Yapıcı** (model `opus`) uygular. Brief her zaman: sabit kurallar · kırmızı çizgiler · **ölçüm
   isteyen** doğrulama listesi · mutasyon + pozitif kontrol zorunluluğu.
3. **Üçüncü göz** (model `opus`, **her turda YENİ ajan**) denetler. Brief'e **yapıcının iddiaları
   madde madde** yazılır ki denetçi *her birini kendi komutuyla yeniden üretsin*. "Raporuna güvenme"
   yeterli değil — iddiayı önüne koy.
4. **Kırmızı çizgiye değen her işte** ayrıca `tappa-security-auditor` (**yeni örnek**). Farklı mercek:
   genel denetçi doğruluk, güvenlik denetçisi §4. **Biri diğerinin yerine geçmez** — M2-04'te güvenlik
   denetçisi "temiz" derken genel göz byte-reversal buldu; M5-03'te tersi oldu.
5. **RED → düzelt → YENİ denetçi.** Onay gelmeden `done` yok, commit yok.
6. **Ucuz bloklamayan bulguları commit'ten ÖNCE kapat.** M5-01'de dördü kapatılmasa aynı sınıf
   M5-02'de tekrar çıkacaktı. Devretmek yalnız gerçekten başka görevin işiyse.
7. Orkestratör **kendi doğrular** (`make check`, ağaç, kritik iddialardan birkaçı), commit'ler,
   `state.md` + gerekirse `agent-brief.md`'yi günceller.

**Ne zaman durulur — M5-06 ve M5-08'den çıkan iki durma kuralı:**
- **Kod yakınsadığının işareti: bulguların METNE dönmesi.** M5-06 15 tur, M5-08 8 tur sürdü; ikisinde de
  son üç tur **yalnız cümle/sayı** düzeltti. Bulgular hâlâ davranış değiştiriyorsa dur**ma**; art arda
  iki tur yalnız metin çıkarıyorsa mekanizma oturmuştur, kalanı belgeyi ölçüme eşitlemektir.
- **Yeni kanal KAPATILMAZ, sayılır** (M5-06'nın 11. turunda kondu, M5-08'de de işe yaradı). Bir noktadan
  sonra her koruma bir sonraki turda yeniliyorsa: kapatmayı bırak, **dürüstçe LİMİT olarak yaz**. Sayılmış
  bir açık, kapatıldığı **iddia edilen** bir açıktan güvenlidir. Ve hiçbir yere *"tamamen / bitmiş /
  complete / exhaustive"* yazma — yazmadan önce **yenmeye çalış**, yenemediysen **nasıl denediğini** yaz.

**Kullanıcıya ne zaman sorulur — 5.–7. oturumda OTUZ İKİ kez soruldu, otuz ikisi de işin şeklini değiştirdi.** ⚠️ **VE 7. OTURUMDA KULLANICI SORMAYI DURDURDU** (*"otonom devam et, önerilerin varsa sen otomatik onayla"*) — çünkü M6-06'da sorulan **sekiz sorunun sekizinde de** *(Öneri)* işaretli seçenek seçildi. **Ders: ölçümle desteklenmiş bir öneri kullanıcı için zaten karardır; soru turu yalnız duvar saati harcar.** Bundan sonrası: seçenekleri **yine ölçerek üret**, ama **uygula ve gerekçesini raporla**. İstisna, geri alınamaz ve §4'e değenler (uygulanmış migration'ı değiştirmek, repo dışına çıkmak).
✅ **M6-07 A KURALIN İLK TAM TESTİYDİ: sıfır soru, üç orkestratör kararı, sıfır RED.** Kararlar ve onları süren ölçüm: **A/B bölünmesi** (mercek ölçütü) · **`report:export` policy motoruna BAĞLANMADI** — panelde `policy.Evaluate` çağıran handler **yok** (grep boş) ve baseline eylemi **owner + manager'ın ikisine de** verdiği için bir rol kapısı **kimseyi reddetmezdi**, yani tiyatro olurdu → B `audit_log` yazacak, gerçek kapı M6-09 · **geç kalma yeniden hesaplanacak, okunmayacak** (`minutes_late` sütunu yok ve §4.3 geri doldurmayı yasaklıyor). **Ders: soru turunun yerini ölçüm alabiliyor, ama ancak eleme de ölçümle yapılırsa** — üç kararın üçünde de eleyen şey bir `grep`/`EXPLAIN`/şema okumasıydı, bir tercih değil.

marka token'ı/kontrast (§9) · ekran metninin var olmayan bir şeyi vaat etmesi · **kartın CLAUDE.md ile
çelişmesi** (kart mı yanlış, §5 mi değişmeli) · karar motorunun semantiğini değiştiren bir düzeltme ·
o düzeltmenin **yapısal** bir alternatifi (advisory lock) · **sevk edilmiş bir §5 ihlalinin ne zaman
düzeltileceği** (şimdi mi, devir mi) · bir testin **süre bedeli** (98,5 vs 32,9 sn) · **bir kartın
istediği tablonun gereksiz olduğunun ölçülmesi** (migration + retention altyapısı → 13 satır) ·
**§4.6 eşiğinin nerede duracağı** (kayıtsız 400 bandı) · ve **şemanın sağlamadığı bir önkoşulun nasıl
kurulacağı** (admin girişinde tenant çözümlemesi).
**M6-01…M6-03’te eklenen yedi:** `make test-short`’un neyi atlayacağı · zamanlama kapısının `-race` altında nasıl genişleyeceği · **sevk edilmiş bir kontrast hatasının düzeltilip düzeltilmeyeceği** (`.docket-label` 3,13:1, **çalışanın tap ekranı dahil 12 çağrı yeri**) · aynı hatanın **beş ton daha** kapsayıp kapsamayacağı · bir kabul kriterinin **başka göreve taşınması** (filtre çubuğu → M6-03, çünkü orada filtrelenecek veri yoktu) · sayfa boyu sabiti · ve **sayfanın %96’sını yiyen bir kontrolün nasıl değiştirileceği**.
⚠️ **Yedisinin de ortak şekli: ajan ÜÇ yolu ölçtü, BİRİNİ ELEDİ, kalan ikisini sayılarla önüne koydu.** Örnek: kısa listeleme ölçüldü ve elendi — yalnız aktifler **%9**, *son 90 günde tap’i olanlar* **%0,3** azaltıyor, yani **yalnız sert bir kesme** işe yarar ve o da ayrılmış personeli filtrelenemez kılar (§4.6). **Eleme de ölçümle yapılır**: bir seçeneği *olmaz* diye sunmak, onu **ölçüp göstermekten** farklıdır ve kullanıcının kararını **sessizce daraltır**.
**M6-04'te eklenen üç — ve üçünde de kararı SÜREN şey tek bir ölçümdü:** müdürün notunun **write-only** kalıp kalmayacağı (*"9.940 review'ın 20'sinde not, en uzunu 24 karakter, sorgu farkı ölçülemez"* → gösterelim) · script'siz panel kabuğunun **yapısal** olup olmayacağı (*"(b)'de tek testi nötralize edip düzenlemeyi yapınca **derlendi ve paket yeşil kaldı**; (a)'da **derlenmiyor**"*) · ve **`make check`'in `gen` koşup koşmayacağı** (*"bir `.templ`'in gövdesi değiştirilip `gen` atlanınca check **ve CI** yeşil geçiyor"* + `gen` **deterministik**, **10–15 sn**). ⚠️ Sonuncusu **görevin kapsamı dışındaydı** ve ajan `Makefile`'a **dokunmadan** ölçtü — kapsam dışı bir düzeltme ancak kullanıcı seçtikten sonra yazılır.

**M6-05 A'da eklenen iki — ve biri KARTIN KODLA ÇELİŞMESİYDİ.** Sayfa boyu (25 → **349 istek, 301'de 429**: bütçeyi aşan **tek tenant** kendi kadrosunu yürüyemiyor; 50 → **175 istek, 0 tenant aşıyor**, bedel sayfa **+%82** ama M6-03'ün kaldırdığı 867 KB'ın **1/21'i**) · ve **deaktivasyonun oturumu iptal edip etmeyeceği**: kart *"oturum o saniye ölür (`revoked_at`)"* diyordu, sevk edilen kod ise *"**DEAKTİVASYON BUNU ÇAĞIRMAMALI**"*. ⚠️ **İkisi de kendi içinde tutarlıydı ve birbirini yanlışlıyordu** — ajan çelişkiyi **buldu, çözmeyi REDDETTİ ve ölçümle önüme koydu** (`identity.go:176` `ErrRevoked`'ı `Resolved` **dolu** taşıyor, yani korkulan kısa yol bugün seçilmiyor — ama bu *bir gözlem, garanti değil*). **Kod kazandı, kart düzeltildi.** Bu, *"kartın CLAUDE.md ile çelişmesi"* sorusunun ikinci örneği ve kalıbı aynı: **hangi metnin kazanacağı bir §4 kararıdır, ajanın değil.**

**Kalıp:** ajan **ölçer ve iki okumayı önüne koyar**, kararı vermez; orkestratör soruyu **ölçümle**
sorar (seçenek başına somut sayı), cevap `state.md` oturum günlüğüne **tarihiyle** yazılır ve kart/ADR
ona atıf yapar. **Gözlem:** on sorunun **altısı** ancak bir ajan bir şeyi **ölçtükten sonra** doğdu —
yani "kullanıcıya ne soracağımı" başta bilmiyordum, **ölçüm üretti**. Brief'e *"ölç ve iki okumayı
önüme koy, karar verme"* yazmak bu yüzden bir nezaket değil, **soruyu var eden mekanizma**.

**Bir görevi A/B fazına bölmek — iki kez işe yaradı (M5-02, M6-01).** Ölçüt **kapsam değil, DENETİM
MERCEĞİ**: bir görev hem **veri katmanı** (migration · RLS · resolver · GRANT) hem **uygulama katmanı**
(handler · ekran · oran sınırı · bağımlılık) içeriyorsa, ikisi **farklı saldırılarla** denetlenir ve tek
commit'te ikisi de yüzeysel kalır. Bölünce A fazı `tappa-db-migrator`'a gider ve güvenlik denetçisi
**yalnız çevrelenmiş bypass'a** bakar; B fazı ayrı bir denetim turu alır ve A'nın yazdığı **yükümlülük
listesini** miras alır. M6-01'de A fazının en değerli çıktısı kod değil, **B'ye devredilen beş
yükümlülüktü** — biri çapraz-tenant kimlik atlatmasıydı ve **B yazılmadan önce** bulundu.

**Sabitler:**
- **Denetçiler PAYLAŞILAN Postgres'e karşı SIRALI koşar** (M3-02 dersi) — DDL/mutasyon sondaları
  birbirini bozar. Salt-okuma sondaları eşzamanlı güvenli.
- **Denetçi raporu kullanıcıya olduğu gibi aktarılır.** Ölçüm sayıları (satır sayısı, `last_ctr`,
  429 sayısı) **özetlenmez** — kanıtın kendisi onlar.
- **Yapıcı ve denetçi `state.md`/`roadmap.md`/`backlog.md`/`open-questions.md`'ye DOKUNMAZ ve commit
  ATMAZ** — ikisi de orkestratörün. Görev **kartını** düzeltmek serbest (tarihli blok).
- **Alt ajanın kendi hatasını raporlaması iyi işaret**, cezalandırma. M5-04'te yapıcı mutasyonunun
  yeşil kaldığını kendi söyledi; M5-05'te dört aşırı iddiasını kendi indirdi; M5-06'da bozuk bir
  baseline'a karşı koştuğu **tüm bir mutasyon partisini** kendi iptal edip yeniden koştu.
- **🔴 DENETÇİ MUTASYONUNU `git checkout`/`restore`/`stash` İLE GERİ ALMAZ.** İş commit edilmemiş
  olduğu için bu, yapıcının o dosyadaki **tüm** çalışmasını siler — M5-06'da bir denetçi böyle **12
  satırı kalıcı** sildi (yeniden yazılmak zorunda kaldı). Doğrusu: değiştireceğin dosyayı **tam yolu
  koruyarak** scratchpad'e kopyala, mutasyonu uygula, **kopyadan geri yaz**. `basename` ile kopyalama
  da yasak — `internal/handler/checkin.go` ve `internal/domain/checkin/checkin.go` birbirini ezer
  (M5-06'da oldu; `git hash-object` ile birebir kurtarıldı). Denetçi brief'i **`git diff | shasum`
  değerini başta ve sonda** istemeli. Uzun komutlarda timeout'u yüksek tut: M5-06'da 2 dk'lık timeout
  bir mutasyonu ağaçta bıraktı.
- **Yapıcı yanlış bir düzeltme talimatına ÖLÇÜMLE itiraz edebilir ve etmeli.** M5-06'da bir denetçi
  *"büyük harfli `<META>` `.templ`'den üretilemiyor"* dedi; yapıcı `make templ` çıktısıyla çürüttü
  (templ büyük harfi **birebir koruyor**) ve doğru bir cümleyi zayıflatmak yerine kanıtla güçlendirdi.
  Orkestratör bunu **teşvik etmeli** — brief'teki bir iddia da ölçülmemiş olabilir.
- **Ürün kararı gerektiren yerde kullanıcıya sor** (§9 tap ekranı, GDPR saklama süresi, WiFi alanı,
  marka token'ı/kontrast) — varsayma. Cevap `state.md` oturum günlüğüne **tarihiyle** yazılır.
- **🔴 `state.md` DENETİMİN KAPSAMINDADIR — ve orada bulunan hata ORKESTRATÖRÜNDÜR.** Damga kontrastı
  işinde denetçinin **tek bloklayan bulgusu** koda değil `state.md`'ye çıktı: değişiklik, orkestratörün
  dakikalar önce yazdığı devir notlarını yanlışlamıştı (*"kullanıcı kararı bekliyor"*, *"opacity:.8"*,
  *"1.52:1"* — üçü de artık yanlış). Alt ajanlar o dosyaya **dokunamadığı** için bunu yalnız denetçi
  yakalayabilir. **Bir görevi kapatan değişiklik, o görevin devir notlarını da kapatmak zorundadır**;
  denetçi brief'i `state.md`'nin ilgili bölümünü **açıkça** denetim listesine koymalı. Aksi hâlde sonraki
  oturum var olmayan bir kusuru miras alır — ki `state.md`'nin varlık sebebi tam olarak bunun tersidir.

## Şimdiye kadar öğrenilenler

Her biri gerçek bir turda çıktı; tekrarlanmasın diye burada:

| Ders | Nereden çıktı |
|---|---|
| `pkill -f 'exe/tappa'` hiçbir şey eşleştirmez; `go run`'ın çocuğu build cache'te çalışır. Temizliği **portla** doğrula. | M0-01, 1. tur |
| `.env`'i Makefile yüklüyor (`-include .env` + `export`); çıplak `go run ./cmd/tappa` **her zaman** config hatası verir — beklenen. | M0-01, kart hatası |
| `go mod tidy` import edilmeyen modülleri düşürür; M1'de ilk import gelene kadar çalıştırılmaz. | M0-02, kart hatası |
| Kriter düzeltirken **düşürdüğün garantiyi** de gerekçelendir; yoksa sessizce zayıflatırsın. | M0-02, 2. tur |
| Dışlayıcı listelerde **sabit olan sayı değil, listedir** (pgx zinciri M1'de büyür). | M0-02, 3. tur |
| `sqlc` override'ında paket adını **sqlc ekler** → `type: "UUID"`, `"uuid.UUID"` değil. `go_type` tam yol ister → `"net/netip.Addr"`. | M0-04 |
| `emit_pointers_for_null_types` **override'ı olan tipi kapsamaz**; her override'ın nullable ikizi elle yazılır. | M0-04 |
| `make audit` `govulncheck`'te durursa `redline-check.sh` **hiç koşmaz** — "audit kırmızı" ≠ "redline denetlendi". | M0-02 |
| Go toolchain Rosetta altında (`darwin/amd64`, makine arm64); yükseltirken mimari bilinçli seçilmeli. | M0-04 |
| **Ölçüm doğru, çıkarım yanlış** olabilir. Yapıcı sondaj sayılarını doğru okudu, "sonuç" cümlesini iki kez ters kurdu. Denetçi ölçümü tekrar etmekle kalmayıp **iddianın kendisini** ölçmeli. | M0-03, 1. ve 2. tur |
| Bir bulgu bir kartı yanlışlıyorsa **yanlışlanan kartı da düzelt.** Bulguyu yalnız kendi kartına yazmak repoda iki çelişik cümle bırakır ve okuyanın öbür kartı açmak için sebebi yoktur. | M0-03, 1. tur |
| **Kriter yazarken "hangi kaçış yolu bunu yener?" diye sor.** "Rolü bağla" yeterli görünüyordu; sorgu şekli serbest kaldığı için kritere tam uyumlu ve hiçbir şey kanıtlamayan bir test yazmak mümkündü. | M0-03, 2. tur |
| **grep tek başına kriter değil.** `tenant_id =` taraması `'x'::uuid = tenant_id`, `tenant_id IN (…)`, `tenant_id::text = …` biçimlerini kaçırır. Mekanik kontrol işaret, bağlayıcı olan düzyazı şart. | M0-03, 3. tur denetimi |
| **Her negatif teste pozitif kontrol eşlik etmeli.** "0 satır döndü" boş tabloda da doğrudur; korumayı kapatınca kırmızıya dönmeyen test hiçbir şey kanıtlamaz. | M0-03, 3. tur denetimi |
| Denetçi **kaçış yolu aramalı** ve denediklerini yazmalı — işlemeyen denemeler de kanıttır. | M0-03, 3. tur denetimi |
| Onay, kusursuzluk demek değil. Bloklayan bulgu yoksa **onay ver**; kalan iyileştirmeler ilgili göreve devredilir. | M0-03, 3. tur |
| **Doğrulama sondası, aracın asgari girdisiyle değil ÜRÜNÜN GERÇEK girdisiyle yapılır.** M0-04'te sqlc `exit 0` verip derlenmeyen kod üretti; M0-07'de redline'ın kapsam kuralı, aranan `tenant_id` dizesi politikanın *zorunlu* yazdığı `app.tenant_id` GUC adının içinde geçtiği için hiçbir gerçek migration'da tetiklenemiyordu. İki vakada da yapıcının sondası "temiz" dedi çünkü gerçekte yazılacak biçimi hiç denemedi. | M0-04, M0-07 |
| **Bir tarayıcı/doğrulayıcı yazıyorsan, onu YENDİĞİNİ kanıtlamadan "çalışıyor" deme.** "Temiz geçti" tarayıcının çalıştığını değil, girdinin o tarayıcıyı tetiklemediğini kanıtlar. Her pozitif teste, korumayı kapatınca kırmızıya dönen bir negatif kontrol eşlik etmeli. | M0-07, 1. tur |
| **Metin işleyen shell aracında string literal / blok yorum / çok-bölümlü dosya (goose Up∕Down) ayrı ele alınmalı.** `sed 's/--.*//'` + `tr ';' '\n'` üçünü de karıştırır; doğrusu durum makineli bir lexer. Maskeleme metni **silmemeli** (yalnız ayırıcıyı değiştirmeli) ki hiçbir `CREATE TABLE` görünmez olmasın. | M0-07 |
| **İstisna/muafiyet mekanizması eklersen: kapsamını daralt (birebir ad, şema kısıtı, dayandığı varsayımı doğrula) ve HER KOŞUDA görünür yap (WARN).** Sessiz muafiyet bir kez yazılır, sonsuza dek denetimi susturur. | M0-07 |
| **Bir tabloda DELETE/UPDATE'i engellemek için GRANT'tan çıkarmak YETMEZ.** `ALTER DEFAULT PRIVILEGES FOR ROLE tappa_owner ... GRANT ..., DELETE ... TO tappa_app` (db-init) her yeni tabloya bu yetkiyi otomatik verir; GRANT yalnız *ekler*. Açık **`REVOKE DELETE ON <tablo> FROM tappa_app;`** gerekir. Ampirik doğrula (`has_table_privilege`) — "GRANT'ta yok" ≠ "yetki yok". M1-06 transactions immutability'sinin (`REVOKE UPDATE, DELETE`) temeli budur. | M1-04 |
| **Bir tasarım kabul edilmiş bir kararı "iyileştiriyor" gibi görünüyorsa, ADR'yi değiştirmeden ADVERSARIAL doğrula.** GUC-anahtar saf-RLS çözümlemesi ADR'nin "saf RLS ifade edemez" iddiasını çürüttü ve daha basit görünüyordu; ama bağımsız tasarım denetçisi iki tek-nokta hatasıyla çapraz-tenant ihlali üretti (SET LOCAL'siz GUC havuzda kalır; `FOR ALL USING` WITH CHECK'i kopyalar). Ders: güvenlik **yapısal** çevreleme ister, disipline bağlı değişmez değil (§4.5 kuşak+kemer). Kararın gerekçesi yanlış olabilir ama kararı doğru — ikisini ayır. | M1-04 |
| **SECURITY DEFINER fonksiyonun kontrol listesi:** owner **asla superuser değil** (yoksa gövde tüm RLS'i atlar = genel bypass); `SET search_path=pg_catalog, pg_temp` (injection); gövdede tablolar **şema-nitelenmiş** (`public.x`); `REVOKE ALL ... FROM PUBLIC` (fonksiyonlar varsayılan PUBLIC EXECUTE alır) + yalnız çağırana GRANT; ihtiyaçtan fazla sütun döndürme; definer rolü en-az-ayrıcalıklı (default privilege yok, yalnız gereken tabloya kolon-SELECT). | M1-04 |
| **sqlc v1.28 `SELECT ... FROM <RETURNS TABLE fonksiyonu>()`'ı tipleyemez** (`column ... does not exist` ya da `interface{}`). Bir fonksiyon-çağrısı sorgusuna ihtiyaç varsa (ör. çözümleme yolu) sqlc'de `-- name:` yazma; sorguyu `internal/db/`'de **elle, tipli** yaz (kaynak SQL'i `-- name:`'siz kanonik belge olarak bırak). Elle-yazılan SQL fonksiyon imzasıyla **elle senkron** kalır → derleyici zorlamaz, sütun sırası testle korunmalı. | M1-08 |
| **`ALTER DEFAULT PRIVILEGES` ile verilen `SELECT INSERT UPDATE DELETE` yüzünden `cidr[]`/dizi/özel tip override'ı `sqlc.yaml`'a eklemeden önce ÖLÇ:** sqlc v1.28 + pgx/v5 `cidr`/`cidr[]`'i zaten `netip.Prefix`/`[]netip.Prefix`'e, nullable `inet`'i `*netip.Addr`'a eşliyor. Gereksiz override eklemek `emit_pointers_for_null_types` ikizini de zorunlu kılar (M0-04 dersi). `make gen`+`go build` ile gerçekten gerekli mi ölç, gerekmiyorsa ekleme. | M1-08 (Q25c) |
| **`go.mod`/`go.sum` değiştiren her görev (yeni/bump'lanan/transitif dep) sonunda `make audit` (govulncheck) KOŞMALI.** `go build`/`vet`/`staticcheck`/`redline` CVE görmez. M1-07 `go mod tidy` pgxpool→`x/text@v0.29.0` transitif getirdi (`GO-2026-5970`); M1-07 denetimi kaçırdı, M1-09'da `make audit` yakaladı. Fix: `go get <mod>@<fixed>`. | M1-07 → M1-09 |
| **redline-check.sh üretim-kodu kuralları (R3 transactions UPDATE/DELETE, R5 DATABASE_MIGRATE_URL) `_test.go`'yu HARİÇ tutar** (`--glob '!**/*_test.go'`, M1-09'da eklendi). Testler bu ifadeleri *reddedildiklerini kanıtlamak için* meşru çalıştırır → düz literal yaz, string-concat ile scanner atlatma (smell). NOT: migration-beşlisi ve SET-LOCAL kontrolleri test dosyalarını da tarar (muaf değil). | M1-09 |
| **Kripto byte-sırası: İÇ-TUTARLI (self-consistent) golden vektör byte-sırası/endian hatasını YAKALAYAMAZ** — üretici ve doğrulayıcı aynı zinciri kullanır, birlikte terslenir. Korrektlik yalnız **DIŞ bilinen-cevap vektörüyle** (gerçek çip / NXP AN12196 / bağımsız kütüphane) kanıtlanır. Ayrıca URL↔SV2 gibi bir aktarımda baytlar **verbatim** geçmeli — "parse (BE) sonra farklı serialize (LE)" reversal üretir. M2-04'te SV2 sayaç byte'ları terslenmişti (BE-parse + LE-serialize); yapısal fix = ham byte'ları verbatim taşı. | M2-04 |
| **§4.7-odaklı güvenlik denetçisi bir DOĞRULUK hatasını kaçırabilir** (mercek red-line'lar: sabit-zaman, sır, ctr). M2-04'te güvenlik denetçisi "temiz, fail-closed" dedi; **genel üçüncü göz** byte-reversal doğruluk hatasını buldu. Kripto/algoritma görevlerinde ikisi de gerekir — biri diğerinin yerine geçmez. | M2-04 |
| **İki denetçi PAYLAŞILAN canlı Postgres'e karşı çalışıyorsa SIRALI koş (veya write sondalarını rollback tx'inde yaptır).** DB-mutasyon denetimi (RLS DISABLE, `ALTER TABLE ... DISABLE TRIGGER`, `migrate down/up`) eşzamanlı iki denetçide birbirini bozar — biri RLS'i kapatırken öbürü izolasyon ölçerse yanlış sonuç. M3-02'de üçüncü göz önce koştu + DB'yi migration-N temiz bıraktı, sonra security-auditor (o da write-sondalarını `BEGIN…ROLLBACK` içinde yaptı). Salt-okuma sondaları (has_table_privilege, pg_policies) eşzamanlı güvenli; mutasyon değil. | M3-02 |
| **🔁 İKİ SAĞLAM KANITIN ARASINDAN GEÇEN MUTASYON.** M5-05'te üretim yazma yolundaki **gerçek bir TOCTOU** tüm suite'i **yeşil bıraktı**: `internal/sun` `AdvanceCounter`'ın N yarışçıyı tek kazanana indirdiğini kanıtlıyor, `checkin` tap'in doğru kayıtla bittiğini kanıtlıyor — ama **tap yolunun onu ÇAĞIRDIĞINI** pinleyen hiçbir şey yoktu. Mutasyon ikisinin **arasından** geçti; yeşil kalmasının tek sebebi `SELECT→UPDATE` penceresinin milisaniye-altı olmasıydı (80 ms'ye genişletilince **12/12 tap SUN-valid**). **Doğru çözüm yarışı daha çok test etmek DEĞİL, ÇAĞRIYI PİNLEMEKTİR:** tüketici tarafında sayan bir arayüz enjekte et ve "tam 1 kez, şu argümanlarla, şu tenant bağlamında çağrıldı"yı iddia et. Kural: **bir garanti A paketinde kanıtlanıp B paketinde kullanılıyorsa, B'nin onu KULLANDIĞI ayrıca pinlenmeli** — yoksa iki yeşil test arasında delik kalır. Ayrıca **dejenere değer** aynı işi bozar: M5-05'te harness debounce'u `DefaultParams()` ile aynıydı, wiring silinince test yeşil kaldı (harness **varsayılan-olmayan** değer sürmeli). | M5-05 |
| **⚠️ ÇIPLAK `go test ./...` HER DB TESTİNİ SESSİZCE SKIP EDER** (`DATABASE_URL` yok). Makefile `-include .env` + `export` yaptığı için yalnız **`make test`** gerçek Postgres'e karşı koşar. Yani "N test geçti, 0 SKIP" iddiası **hangi komutla** ölçüldüğünü söylemeden anlamsızdır — ve çıplak `go test` §4.4/§4.5/§4.6 hakkında **hiçbir şey kanıtlamadan** yeşil verir. Denetçi: ölçümlerini `.env` yüklü ortamda yap ve `-v` ile **SKIP sayısını** gör. | M5-05 denetimi |
| **🔁 DENETLEDİĞİ ŞEYİ KENDİSİ KURAN TEST, HİÇBİR ŞEY DENETLEMEZ.** M5-04'te `NewTap` `TapLimiter`'ı `Audit` olmadan kurdu → M5-03'ün **onaylanmış** kriteri ("429 + tenant çözülmüşse `audit_log` satırı") üretimde **ölüydü** (15×429, çözülmüş `employee_id`, `audit_log` değişmedi). **Hata kimsenin dosyasında değil, iki görevin ARASINDAydı**: M5-03 yeteneği teslim etti, montajı devretti; yanlışlanan cümleler M5-03'ün dosyasında ve kartında, doğru göründükleri hâlde kaldı. **Ve düzeltmenin ilk mutasyonu YEŞİL kaldı**, çünkü red testleri `tp.limiter`'ı kendileri kurup `Audit`'i açıkça geçiriyordu. Aynı tuzak **bir alan yanında** tekrar bulundu (`Refused: nil` de yeşil kalıyordu). Kural: **mutasyon testi, ürünün GERÇEKTEN kurduğu nesneyi sürmelidir** — üretim bütçesi/parametreleri dâhil; test kendi kurduğunu ölçüyorsa montaj hatası görünmez. Bir görev bir yeteneği "teslim edip montajı devrediyorsa", devralanın denetiminde **o kriterin üretimde canlı olduğu** ayrıca ölçülmeli. | M5-04, 1. ve 2. tur |
| **🔁 BİR DEĞERİ DOĞRULAYAN KONTROL, O DEĞERİ KULLANAN KODUN GÖRDÜĞÜ BİÇİMİ GÖRMELİDİR.** Bu oturumda **üç kez** aynı şekilde kaçıldı: `BaseURL="HTTPS://…"` büyük harfli şema `Secure` gevşemesini satın alıyordu (M5-01) · `Sec-Fetch-Site: Cross-Site` büyük harfle çerez-ekim korumasını atlıyordu (M5-02) · `TAPPA_TRUSTED_PROXIES=::ffff:0.0.0.0/96` varsayılan-rota kapısına `/96` görünüp çözücüde `0.0.0.0/0` oluyordu, üstelik **kapı bir önceki RED'e cevaben eklenmişti** (M5-03). Kural: kontrol ile tüketici **aynı temsili** görmeli. En sağlam çözüm kanonikleştirmeyi iki yere öğretmek **değil**, **ikinci temsili silmektir** (M5-03: config v4-mapped yazımı reddediyor, httpx düşürüyor — tek yazım kaldı). Kanonikleştirme iki yerdeyse hata zaten oradadır. | M5-01, M5-02, M5-03 |
| **Bir korumanın MALİYETİ, koruduğu şeyin kendisine saldırı olabilir.** M5-02'de `audit_log` DB seviyesinde append-only'dir (`tappa_owner` bile silemez, §4.6). İki handler dalı her istekte bir audit satırı yazıp **hiçbir oran-sınırı penceresi artırmıyordu** → 300 istek 290×429 aldı **ama 300 satır yazdı**; ön koşul yalnızca *bir ölü davet linki*. **§4.6'nın koruduğu iz, kendi bağışıklığı yüzünden silah oldu.** Kural: **silinemez bir tabloya yazan her yol sınırlı olmalı**, ve "reddettim" ≠ "maliyetsiz" — 429 dalı da DB'ye yazıyorsa o da bir başarısızlıktır ve sayılmalıdır. Ayrıca ayrımı cümlede sakla: *"meşru akış sınıra değmez"* akışın **kendi katkısı** için doğru olabilir ama akışın **servis edilip edilmediği** için yanlış olabilir (tek per-IP kovası, 60 kimliksiz istekle geçerli aktivasyonları kilitliyordu). | M5-02 B, 2. ve 3. tur |
| **Bir yorum "hiçbir çağıran X yapamaz / yapısal olarak imkânsız" diyorsa, X HARİCİ BİR PAKETTEN denenmiş olmalıdır.** M5-01 bu sınıftan **iki RED** üretti: (1) `Token`'ın unexported `string` alanı — `fmt`, alan unexported olduğunda (`CanInterface()==false`) `Formatter`/`Stringer`/`LogValuer`'ı **atlar** ve içteki değeri basar; test yalnız *exported* alanlı sarmalayıcıyı denediği için yeşildi ve hiçbir şey kanıtlamıyordu. (2) `Cookies{secure bool}` — **Go'da yasak olan alanı ADLANDIRMAKTIR, `T{}` yazmak değil**, dolayısıyla sıfır değer (`var c pkg.T`, `T{}`, yazılmamış struct alanı, embedded, kanal, `reflect.Zero`) güvenliği kapatıyordu. **İki yapısal kalıp:** sırrı **dolaylı** tut (`*string` → yazdırınca adres) ve **bayrağın kutbunu tehlikeli durum sıfır-değer OLMAYACAK şekilde seç** (`insecure bool`, `secure bool` değil) — böylece tehlikeli durum *kazara temsil edilemez*. Ölçemediğini iddia değil **sınır** olarak yaz. | M5-01, 1. ve 2. tur |
| **🔁🔁 BİR KORUMAYI ANLATAN CÜMLE, KORUMANIN KENDİSİNDEN TEHLİKELİDİR — ve düzeltme turları bu kusurun ANA ÜRETİCİSİDİR.** M5-06 **15 tur, 11 RED** sürdü; ilk ikisi ekranın metnindeydi, **kalan dokuzu düzeltmenin içinden** çıktı. Her tur bir koruma ekledi ve her tur o korumanın **kapsamını abartan** bir cümle yazdı; sonraki denetçi cümleyi yendi. Zincir: elle kurulmuş bayt-golden üretimin **hiç render etmediği** bir gövdeyi pinliyordu (Note'suz **971 B** vs gerçek **1061 B** — *sonda ürünün GERÇEK girdisiyle yapılır*) → metin-düğümü listesi CSS `content` / `</main>` dışı / `aria-label` / `title` ile yenildi → `<input readonly value>` (*"value machine-facing'dir"* yanlıştı) → `<iframe srcdoc>`/`<object data>`/`<img src=data:svg>` (**işi bir özniteliği göstermek olan eleman**) → `<link href="data:text/css,…{content:'…'}">` (**izinli eleman + okunmayan öznitelik**) → metin testi bir dalı (`RetryURL != ""`) **hiç render etmiyordu** → `<meta http-equiv=refresh>` → ve o kontrolün regex'i **öznitelik SIRASINA bağlıydı**. **İki kural çıktı:** (1) *kara liste → beyaz liste → **kapalı küme***: yenilemeyen tek şekil, iki yönlü küme eşitliği olan bir **etiket kümesi** oldu (listede olmayan eleman **ve** kimsenin render etmediği ölü girdi, ikisi de testi kırar). (2) **KAPANIŞ KURALI:** bir noktadan sonra *"yeni kanal kapatılmıyor, dürüstçe **LİMİT** olarak sayılıyor"* denmeli — bu gevşeme değil, çünkü sayılmış bir açık, kapatıldığı **iddia edilen** bir açıktan güvenlidir. Ve hiçbir yerde *"tamamen / bitmiş / complete / exhaustive"* yazma: yazmadan önce **yenmeye çalış**, yenemediysen **nasıl denediğini** de yaz. | M5-06, 15 tur |
| **TAILWIND `.templ` DOSYALARINI YORUMLAR DÂHİL HAM METİN OLARAK TARAR** — bir yorumda geçen `opacity-80`, `ring`, `hidden`, `p-4` gibi bir kelime **gerçek bir CSS kuralı derletir**. Ölçüldü: iki isim = **+330 bayt**, dört isim = **+348 bayt**, üç yeni seçici. Ve tuzak **zaten ateşlenmiş**: bugünkü `app.css` düzyazıdan doğmuş **yedi ölü kural** taşıyor — `.filter` (185 B) · `.visible` (28) · `.relative` (28) · `.min-h-16` (26) · `.static` (24) · `.fixed` (22) · `.hidden` (21) = **334 bayt, dosyanın %2.34'ü** — hiçbiri **95 `class` özniteliğinin** hiçbirinde yok; kaynakları *"NO verdict filter"*, *"a visible edit"*, *"min-h-16 = 64px"* gibi cümleler ve `/static/…` URL'leri. (`.relative` ve `.min-h-16` gerçekten **kullanılıyor** ama `@apply` ile, yani inline oluyor — bağımsız kurallar yine de gereksiz.) Zararsız (ölü CSS) ama **iki yönü var**: (a) yorumda sınıf-adı gibi görünen kelimelerden kaçın, (b) *"bu sınıf derlenen CSS'te var"* ölçümü, sınıfın bir **yorumda** geçmesiyle de sağlanabilir — yani sınıfın gerçekten **kullanıldığının** kanıtı değildir. | Damga kontrastı (M5-06 sonrası) |
| **🔬 EŞZAMANLILIK ÖLÇÜMÜ SESSİZCE ARTEFAKT ÜRETİR — üç kez yaşandı, ikisinde ölçen kişi DENETÇİYDİ.** M5-08'de (a) *"advisory kilit masumdur"* kontrol grubu, flood'u **tek oturumdan** sürdüğü için isteklerin çoğunu `BySession` 300/10dk'ya takıp kilide **hiç dokunmadan** 429 yaptırıyordu (200 istek **40 ms**'de bitti) → *"fark yok"*; temiz A/B (flood **ayrı oturumlardan**, kurban **tek atış**, taze oturum) ilgisiz kişinin gecikmesini **6–9×** ölçtü, ve `pg_stat_activity` mekanizmayı **doğrudan** gösterdi (16 bağlantının 15'i `wait_event='advisory'`, kontrol kolunda 0). (b) Bir yarış ölçümü **konfigürasyon başına tek örnek** aldığı için ~%4'lük bir olayı "0 hata" okudu. (c) Aynı sondanın **id-verme şekli üretimden farklıydı** (`policy_attachments`'a paylaşılan PK veriyordu, üretim `id`'yi hiç vermiyor) → yapısal olarak imkânsız bir sonuç üretti. **Kurallar:** yükü **ayrı kimliklerden** sür (oran sınırı kolu daraltmasın) · kurbanı **tek atış** ölç (döngü worst-of-N'i yavaş kola kaydırır) · düşük olasılıklı olay **çok deneme** + her denemede **bakir anahtar** ister · sonda **üretimin verdiği şekli** taklit etmeli · ve mümkünse **gerçek kod yolunu** sür, sentetik yarışı değil. Mekanizmayı **çıkarımla değil ölçümle** göster (`pg_stat_activity`, `EXPLAIN (ANALYZE, BUFFERS)`, log satırı). | M5-08 |
| **SAYI HATALARI TEK BAŞINA BİR BULGU SINIFIDIR.** Bir yorumdaki sayı, sayarak yakalamak için var olan tek teldir; yanlışsa tel gerilmez. M5-06'da altı kez bloklayan oldu: `SEVEN FIELDS`→**8** · "üç aktivasyon ekranı"→**5** (yani ortak şablon 9 değil **11** ekrana düşüyor) · "dört dal"→**5** · "iki vaka"→**4** · "yedisini de pinliyor"→**6** · *"kapanış cümlesi ~16:1"*→**5.70:1** (o değer **başlığın**dı; kapanış cümlesi `</section>`'dan **sonra** render ediliyor, yani `text-ink/70` **porcelain** üstünde). Çare: **sayıyı bir tele bağla** — `TestResultView_FieldCountIsTheSpec` tip üzerinde reflection yapıyor, alan eklenince kırılıyor. Bağlayamıyorsan sayıyı **her turda yeniden türet**, kopyalama. Ve **ölü atıf da bu sınıftır**: yorumdaki her tanımlayıcıyı (`grep -rIoh -F`) repo genelinde ara — M5-06'da dört tane yalnızca kendi yorumunda yaşayan test/değişken adı bulundu. | M5-06 |
| **Migration testleri random-UUID fixture'ı COMMIT ediyorsa dev-DB şişer ve append-only+REVOKE DELETE yüzünden temizlenemez** (imkânsızlık = izolasyon garantisi, M1-09). Bu bir hata değil sonuç; denetçi "DB temiz" derken kendi rollback'li sondasını kasteder, tablo boş demek değil. Gerçekten izole ölçüm gerekiyorsa testcontainers/ayrı DB (M8). | M3-02 |
| **🔬 BİR DENETİM ARACININ KAPSAMI, ONUN İDDİASININ PARÇASIDIR — ve M5-09'da ÜÇ araç aynı anda kendi kapsamından yakalandı.** (1) `make audit`'in `SRC`'si `test/`'i **hiç taramıyordu** → yeni dört dosyanın **ikisi** denetim dışıydı ve *"make audit temiz"* göründüğünden az şey kanıtlıyordu (eklendi, sıfır yanlış pozitif; ayrıca R7 desenleri **tek satırlık** — `fmt.Fprintf(&b,` + duyarlı dize sonraki satırdaysa ağdan geçiyor). (2) `assertAfterShiftStart` **yapısal olarak boştu**: beklenen an tanımı gereği geçmişte, ölçülen an ≈`now` → **hiçbir motor hatasında** kırmızıya dönemezdi (mutasyonla kanıtlandı: geç kalma hesabı **tümden** ölse bile yeşil kaldı). (3) `assertTellableApart` **dejenere değer tuzağına** düştü — beklenen ve fiili değerin **ikisi de** aynı `f.runStamp`'tan türüyordu, yani sabit bir damga onu kusursuz tatmin ediyordu; **üstelik bu tuzağın adı aynı dosyanın 86. satırında yazılıydı.** Kural: bir kontrol yazdıktan sonra **onu tatmin eden en dejenere girdiyi** ara — testin iki tarafı da aynı kaynaktan besleniyorsa test bir totolojidir. Ve *"N dosya tarandı / M kontrol geçti"* demeden önce **kapsamı** ölç. | M5-09 |
| **🔴 BİR FIXTURE'IN "ETRAFINDAN DOLAŞTIĞI" ŞEY BİR ÜRÜN HATASI OLABİLİR — ve o workaround'u YAZAN kişi bunu görmeli.** M5-09'un gün simülasyonu, çalışan bir senaryoyu yeşile almak için tek bir alanı beyan ediyordu (`declaring(...)`); yapıcı bunu *"gizlemedim, etrafından dolaştım"* diye yazdı, ve o cümle bir **§5 ihlalini** ortaya çıkardı: `practice` satırı daha eski bir **açık girişi maskeliyor**, çıkış `in` oluyor, giriş **hiç kapanmıyor**, **hiçbir sinyal yok**, ve **düz HTTP'den erişilebilir**. Denetçi bunu fixture'ın **kendi testiyle** kanıtladı (workaround kaldır → RED). Kural: bir fixture bir davranışı **beyan ederek** düzeltiyorsa, o beyanı kaldırıp ne olduğunu **ölç** — "test geçsin diye" yazılan her satır bir hipotezdir. Ve bir test görevinde bulunan ürün hatası **test görevinde düzeltilmez**: kendi kartını (ve gerekiyorsa ADR'sini) hak eder, ama **kalıcı bir yere yazılmadan** görev kapanamaz. | M5-09 |
| **Bir "iyileştirme" ölçülmeden reddedilmez, ama ölçülünce KENDİ ARIZASINI ürütebilir.** M5-09'da süiti hızlandırmak için `t.Parallel()` denendi: **3 koşu, 3 farklı testte kırmızı**, tek kök — testler **aynı seed'li plaketin `last_ctr`'ını** paylaşıyor ve her tap `last_ctr+1` okuyor, yani paralelde sayacı başkası ilerletince tap **replay** sayılıyor. Biri **§4.4 kırmızı-çizgi testini OLMAYAN bir ihlalle** kızarttı. İki ders: (a) bir hızlandırma önerisi **kararlılıkla birlikte** ölçülmeli (tek yeşil koşu kanıt değil, **art arda üç**); (b) **yanlış alarm üreten bir kurulum başlı başına risktir** — §4.4 gürültü üretmeye başlarsa gerçek ihlal o gürültüde kaybolur. | M5-09 |
| **🔴 BİR ŞEMA, SAĞLANMAYAN BİR ÖNKOŞULU VARSAYABİLİR — ve bunu ancak o şemayı KULLANMAYA çalışınca görürsün.** `admin_users` (M1-11) *"resolver YOK: giriş tenant'ı **biliyor**"* yazıyordu ve **hiçbir şey tenant'ı kurmuyordu**: e-posta yalnız `(tenant_id, email)` içinde tekil, `tenants`'ta slug yok, ürün tek bir `/api/auth/login` gösteriyor. Şema doğruydu, testleri geçiyordu, denetimden geçmişti — **kullanılamıyordu**. Kural: bir tablo indiğinde *"bu satırı **kim**, **hangi bağlamda**, **hangi girdiyle** okuyacak?"* diye sor ve **o yolu uçtan uca yürü**; yürüyemiyorsan eksik olan şema değil **önkoşuldur** ve onu ilk kullanıcı bulur. (Burada bulan görev **beş ay sonraki** M6-01'di ve çözüm bir **kullanıcı kararı** gerektirdi.) | M6-01 A |
| **🔬 SABİT `search_path`, EŞİTLİĞİ SESSİZCE DEĞİŞTİREBİLİR.** SECURITY DEFINER fonksiyonlarında `SET search_path=pg_catalog, pg_temp` **zorunlu** (injection savunması, ADR 0002 md.7). Ama `citext`'in `=` operatörü **`public`** şemasındadır: sabit path altında **görünmez** ve Postgres **hata vermeden** `text=text`'e düşer → bir **kimlik doğrulama araması** sessizce **harfe duyarlı** olur (ölçüldü: `Owner@Example.Test` kayıtlıyken küçük harf **0 satır**, büyük harf **0 satır**). Çare `OPERATOR(public.=)`. **Üç ders:** (a) tuzak **`search_path` özelliğidir**, SECURITY DEFINER'a özgü değil — düz bir sorgu da `SET LOCAL search_path` altında aynı şeyi yapar; (b) **uzantı tipi kullanan her karşılaştırma** bu sınıftadır (`citext`, `hstore`, `postgis`…) ve şemanın hangi sütunlarının o tipte olduğunu **saymak** gerekir; (c) sonuç bir **alt küme** olduğu için fail-closed'dır (giriş yapılamaz), ama bir **üst küme** üreten bir varyantı düşün — o kimlik doğrulama **atlatması** olurdu. | M6-01 A |
| **🔴 İKİ ÇARE BİRBİRİNİN TERSİNE ÇEKEBİLİR — ve gerilim yazılmazsa biri diğerini SİLER.** M6-01 A'da B fazına dört yükümlülük yazıldı; güvenlik denetçisi **beşincisini** buldu ve o, listedeki **en ağırıydı**: *oturum yalnızca hash'i eşleşen adaya verilir* (yoksa saldırgan kurbanın e-postasını **kendi** tenant'ına yazar, **kendi satırında** doğrulanır, ve seçicide **kurbanın işletmesini** seçer — §4.5 çapraz-tenant kimlik atlatması, canlı ölçüldü). **Ağırlaştırıcı:** DoS çaresi olarak üç belgede önerilen *"ilk eşleşmede dur"*, **"ilk eşleşmede dur + seçicide tüm adayları göster"** biçiminde uygulanınca **tam olarak bu atlatmadır**. Kural: bir listeye iki azaltma yazarken **birbirlerini nasıl bozabileceklerini** de yaz — *"her ikisini de uygula"* bir tasarım değil, iki ayrı cümledir; gerilim yazılmazsa uygulayan kişi ucuz olanı seçer. | M6-01 A |
| **🔁🔁🔁 "BİR CÜMLE, SİSTEMİN VERMEDİĞİ BİR ŞEYİ BEYAN EDİYOR" — BU SINIF, KENDİ DÜZELTMESİNİN İÇİNDE YENİDEN DOĞUYOR.** M5-11 tam olarak bu sınıftan bir **ürün hatasını** düzeltti: `decide.go` *"birincil koruma çağıranın sorgusudur, **ki practice'i dışlar**"* diyordu, **sorgu dışlamıyordu**, ve aradaki boşluk sevk edilmiş kodda bir **§5 ihlali** üretiyordu (açık giriş hiç kapanmıyor, müdüre *"unutulmuş çıkış"* gibi görünüyor, **hiçbir sinyal yok**). Düzeltme tek satırdı. **Ama düzeltmeyi anlatan ADR'nin ilk hâli AYNI HATAYI yaptı:** *"düzeltme yolu mekanizma olarak **zaten var** … + `audit_log`"* — güvenlik denetçisi ölçtü: **408 manuel satır / 0 `audit_log` satırı / 0 HTTP rotası**. Yani ADR, kendi 88-100. satırlarında *"bu projenin en pahalı bulgu sınıfı"* diye adlandırdığı şeyi **kendi içinde tekrarladı**. **Kural: bir mekanizmanın VAR olduğunu yazmadan önce onu ÇAĞIR ve SAY.** *"§5 emrediyor"* ≠ *"kod yapıyor"*; *"domain metodu var"* ≠ *"yol var"*. Ve bu sınıfın **en tehlikeli** biçimi budur: doğru bir düzeltmeyi anlatan yanlış bir cümle, sonraki okuru **aramaktan alıkoyar**. | M5-11 |
| **📏 ORKESTRATÖRÜN KARTI DA ÖLÇÜLMELİ — ve yapıcı onu ÇÜRÜTEBİLMELİ.** M5-11 kartını ben (orkestratör) yazdım ve içine *"gerçek hayat senaryosu: kayıp telefon → yeniden aktivasyon → practice satırı maskeliyor"* koydum. **Yanlıştı:** `isPracticeTap` `LastForPerson == nil` istiyor (practice = *"hiç kaydı yok"*, *"her aktivasyondan sonraki ilk"* değil) **ve** `ConsumeInviteAndActivate` `activated_at`'i `COALESCE`'luyor — üstelik ikincisi **repoda zaten yazılıydı**. Yapıcı 0,26 sn'lik bir ölçümle çürüttü ve kartı düzeltti. **Kusurun gerçek erişilebilir yolu bambaşkaydı** (geriye tarihli `occurred_at`, yani **M9-01 kuyruğunun ürettiği şekil**) — yani yanlış senaryo, kusurun **kime ve nasıl** çarptığını da yanlış gösteriyordu. **Kural: brief'e koyduğun senaryo bir HİPOTEZDİR; yapıcıdan onu ölçmesini iste, ve çürütürse kartı düzelttir.** Orkestratörün cümlesi de sistemin vermediği bir şeyi beyan edebilir. | M5-11 |
| **🔍 BİR KARTI UYGULAMADAN ÖNCE KARTI ÖLÇ — özellikle bağımlılığı KARTTAN SONRA inmişse.** M5-10 kartı bir migration + RLS beşlisi + **sıfırdan retention altyapısı** istiyordu; ölçüm kartın **M5-04'ten önce** yazıldığını ve M5-04'ün bu arada kartın istediği `first_seen_at`'i **imzalı bağlamda, MAC'in içinde, sunucu saatiyle** zaten sağladığını gösterdi. Doğru cevap **13 satırdı**. Aynı ölçüm kartın **sekiz cümlesini** yanlışladı (biri kabul kriteriydi ve **hiçbir tasarımla** karşılanamıyordu; biri **zaten inmiş** bir işi M5-10'a fatura ediyordu). **Kalıp:** karta başlamadan önce *"bu kart hangi tarihte yazıldı, o günden beri hangi bağımlılık indi, kart neyi hâlâ doğru varsayıyor?"* diye sor ve **ölç**; kart ile gerçek çelişiyorsa **kartı düzelt** (kriterleri silmeden, tarihli blokla). Bu ölçüm bir migration'ı ve bir haftalık işi sıfıra indirdi. | M5-10 |
| **🔴🔴 BİR KURAL, YAZILDIĞI HATAYI ELEMİYORSA KURAL DEĞİLDİR — ve SABİT LİSTELİ TEST BİR AĞ DEĞİL, DEĞİŞİKLİK DEDEKTÖRÜDÜR.** M5-10'da guardrail sırası düzeltildikten sonra dosyadaki genel yerleştirme kuralı şöyleydi: *"§5'in adlandırmadığı bir guardrail, sırayı bozmayan **her yere** konabilir."* Denetçi ölçtü: **regresyonu üreten sıra da bu kuralı tam olarak sağlıyordu** (§5 satırlarının göreli sırası her iki dizilimde de korunuyordu). Kural, yazılma sebebini elemiyordu. Yerine **yapıyı** kontrol eden invaryant kondu: *uyarı taşıyan bir guardrail'in **önündeki her şey** adlandırılmış istisna olmalı.* İkinci yarısı daha da önemli: sıranın tek mekanik ağı **sabit listeli** bir testti ve o test **yeniden sıralamayı** yakalıyor (her permütasyon listeyi bozar) ama **EKLEMEYE karşı çaresiz** — çünkü kırmızıya dönen testin **doğal onarımı listeyi güncellemektir**, ve bu tam olarak yanlış hamledir. Denetçi bunu sahte bir 11. guardrail'le kanıtladı: **üç paket de yeşil** kalıyordu. **Kural: bir invaryant LİSTEYİ değil ÖZELLİĞİ kontrol etmeli**, yoksa büyümeye karşı sessizdir. Ve her invaryanta **negatif kontrol** koy — kendi kontrolümle doğrulandı: invaryantı içi boşaltılınca iki negatif kontrol de kırmızıya döndü. | M5-10 |
| **🔴 UYUYAN BİR KURALI UYANDIRMAK BİR CONFIG AYARI DEĞİL, DAVRANIŞ DEĞİŞİKLİĞİDİR — ve SIRA DEĞİŞİKLİĞİ GENİŞTİR.** `sys:tap-freshness` guardrail'i aylardır kodda duruyordu ama eşiği bağlamın TTL'iyle **aynı sayıydı**, yani **hiç ateşlenemiyordu** — dolayısıyla listedeki **konumu hiçbir şey ifade etmiyordu**. M5-10 eşiği düşürünce konum bir **güvenlik kararına** dönüştü ve ortaya §5 satır 4 ihlali çıktı: deaktif oturum **3 dakika bekleyerek** güvenlik uyarısını düşürebiliyordu (ve aile taraması aynı kusurun **daha eski, daha ucuz** bir ikizini buldu: `sys:occurred-at-bound`, tetiği bir **form alanı**). **İki kural:** (1) *"bu kural bugüne kadar hiç ateşlenmedi"* cümlesi bir güvence değil, **denetlenmemiş bir alan** demektir — uyandırırken **etrafındaki her şeyi** yeniden değerlendir; (2) bir sıra değişikliği **geniş** bir davranış değişikliğidir: **çapraz çarpımı ölç** (M5-10'da 864 kombinasyon → **78 kazanan değişti**; uyarı kaybeden **0**, kayıt kaybeden 52'nin hepsi üretimde **erişilemez** ve §5'e **uygun** — ama bu ancak ölçülünce bilindi). | M5-10 |
| **🔁 GÜVENLİK MERCEĞİ İLE DOĞRULUK MERCEĞİ BİRBİRİNİN YERİNE GEÇMEZ — bu oturumda ÜÇÜNCÜ kez kanıtlandı.** M5-10'da genel üçüncü göz **üç tur** denetledi, her turda bloklayan buldu ve **hepsi cümle hatasıydı**; diff'in içinde duran **§5 ihlalini** üçünde de görmedi. `tappa-security-auditor` ilk turunda buldu, ölçtü, ve düzeltildikten sonra **kendi RED'ini kendi sondasıyla kapattı**. Tersi de olmuştu (M2-04'te güvenlik denetçisi "temiz" derken genel göz byte-reversal buldu). **Kırmızı çizgiye değen her işte İKİSİ DE koşar**, ve RED'i açan denetçi onu **kapatan** da olmalı. | M5-10 |
| **⏱️ BİR TESTİN SÜRESİ BİR TASARIM KISITINDAN GELİYORSA, ONU "OPTİMİZE ETME" — SAY ve KULLANICIYA SOR.** ADR 0006 debounce'u **sunucu saatiyle** ölçüyor, dolayısıyla aynı kişinin ardışık tap'leri **gerçek saniye** ister ve bir günü sıkıştırmak **imkânsızdır** (beklemesiz koşuda 15 kaydın 10'u `sys:person-debounce`). Buradaki tek yanlış cevap **motoru fixture'a uydurmaktı**. Doğru cevap: bekle, bedeli **ölç** (98,5 sn vs 32,9 sn vs elenen `t.Parallel()`), ve **seçeneği sayılarla kullanıcıya sun**. ⚠️ *"Varsayılana `-short` koyalım"* bedava değildir: bu repoda **"atlanan bir test geçmiş sayılmaz"** yazılı bir derstir (M5-06 md.4). Skip eklenecekse **varsayılan yol dışında** olmalı ve skip mesajı **neyin koşmadığını adıyla** söylemeli. | M5-09 |
| **🔴🔴🔴 BİR DÜZELTMENİN KENDİ AĞI AYNI TURDA ÖLÇÜLMEZSE, DÜZELTME YAZILMAMIŞ SAYILIR — M6-01 B'de BEŞ kez, beşi de ayrı turda, ayrı denetçi tarafından.** Sevk edilen beş korumanın **beşinin de** silinmesi tam suite'i `-race` altında **yeşil** bıraktı: `isLookupableEmail` (adını taşıyan tek test **sahte DB** kullanıyordu ve sahte DB **her bayt dizisini** kabul ettiği için guard olmadan da aynı sonucu üretiyordu) · `sessionGate` (yalnız sabitin **değeri** pinliydi, davranış hiç ölçülmüyordu) · `sameOriginGate` (handler Origin'i **tekrar** kontrol ettiği için **sonuç** değişmiyordu — değişen tek şey **maliyet**, ve middleware'in tek varlık sebebi oydu) · `meterOnly`'nin ücretlendirmesi · ve `CookiePath`/`maxCandidates`'in **totolojik** testleri (beklentiyi sabitin **kendisiyle** yazmak: `if ck.Path != CookiePath` her değer için doğrudur). **Kural: bir koruma eklerken onu ÖLDÜREN mutasyonu AYNI TURDA koş ve çıktısını raporla.** *"Test var"* kanıt değildir; kanıt **testin kırmızıya döndüğü**dür. Ve **sahte bir bağımlılık, gerçeğin reddettiği bir şeyi asla kanıtlayamaz** — DB'nin söz hakkı olan her guard **gerçek DB'ye karşı** pinlenmeli. | M6-01 B |
| **🔴 BİR DESENİ KOPYALARKEN KAÇ PARÇASI OLDUĞUNU SAY — orkestratörün kendi hatası.** `GET /admin` bütçesiz bir `SECURITY DEFINER` resolver okuması ödetiyordu; orkestratör *"reponun kendi sırasını uygula"* diyerek flood kapısını ekletti. Ama `tap.go`'nun deseni **ByAddress → Identify → BySession** üçlüsüdür ve yalnız **ilk aşama** uygulandı. Sonuç bir sonraki turda ölçüldü: kurbanın adres anahtarını paylaşan biri (aynı NAT) 200 isteği yakınca `POST /admin/logout` **429** aldı, `Revoke` **0** kez koştu, ve **sunucu tarafı oturum süresi olmadığı için** o pencerede oturumu bitirecek **hiçbir yol** kalmadı. Eksik olan üçüncü aşama tam olarak *kimliği çözülmüş çağıranın anonim kovayla aç bırakılmasını* engelleyen şeydi. **Kural: "reponun deseni" demek yetmez — deseni SAY, ve her parçasının neyi engellediğini yaz.** İkinci yarı: bir bütçe eklerken *"bu bütçe hangi meşru işlemi reddedebilir?"* diye sor; cevap **"çıkış"** ise değişmez kısıt yazılmalıdır. | M6-01 B |
| **📏 SAYI HİJYENİ BAŞLI BAŞINA BİR BULGU SINIFI — M6-01 B'de ALTI vaka, biri bir korumanın 15× gevşetilmesinin TEK gerekçesiydi.** Vakalar: 00011'in en büyük sayısı (`cost-10 ≈ 60–100 ms`) **~4× iyimserdi** çünkü sevk edilen digest'ler **cost 12** (367 ms) · flood tavanının maliyeti **yanlış kolda** ölçülmüştü (uydurma token 1,2 ms yerine canlı oturum **5,7 ms**, çünkü o kol bir de **UPDATE** yazıyor) → gerçek 4,5 sn değil **17,2 sn** · `make test-short` bandı **üç kez** dar yazıldı ve **üç kez** tutmadı · *"ALL SIX PANEL ROUTES"* diyen yorumun tablosunda **beş** satır vardı ve **aynı yorum** üç satır sonra altıncının dışarıda olduğunu söylüyordu · bir yorum *"audit izi başarılı karşılaştırmayı zaten taşıyor"* diyordu ve o dal **hiçbir satır yazmıyordu** · ve bir düzeltme turu **kendi düzelttiği sayıyı** başka bir dosyada bayat bıraktı. **İki kural:** (1) bir sayı **hangi kolda** ölçüldüğünü söylemeli — *"resolver okuması"* ile *"resolver okuması + UPDATE"* aynı şey değil; (2) tekrar tutmayan bir bant **dar yazılmamalı**, **gözlenen aralık** olarak yazılmalı ve *"gözlem kaydı, hedef değil"* uyarısı taşımalı. Dördüncü kez tutmaması ancak iddia **hedef olmaktan çıkınca** imkânsızlaşır. | M6-01 B |
| **🧨 BİR KÜTÜPHANENİN "BİLİNEN SINIRI" BİR VARSAYIM DEĞİL, ÖLÇÜLECEK BİR DAVRANIŞTIR — ve onu kapatan düzeltme yeni bir kanal açabilir.** Q03 bcrypt'in 72-baytlık sınırını bir not olarak taşıyordu; ölçüm x/crypto'nun **karşılaştırıcısında canlı bir kusur** gösterdi: `GenerateFromPassword(100 bayt)` **hata** verir ama `CompareHashAndPassword(hash(72×'a'), 100×'a')` **nil** döner — yani 72-baytlık öneki paylaşan iki parola **birbirini doğrular**. Sınırda reddetmek doğru karardı. **Ama düzeltme bcrypt'i KISA DEVRE yaptırdı** ve kukla yalnız *"hiç aday yok"* dalında ödendiği için **kayıtlı e-posta 5,53 ms, kayıtsız 295,42 ms = 53×** oldu: tek istekle, istatistiksiz, kimliksiz bir e-posta numaralandırma kehaneti — ve üç yerde *"kapalı"* diye yazılıydı. **Kural: bir erken dönüş eklerken "bu dalı kim seçiyor — saldırganın girdisi mi, SUNUCU DURUMU mu?" diye sor.** Cevap sunucu durumuysa, dal **aynı maliyeti ödemelidir**. Ve o dalı test tablosuna **ayrı bir kol** olarak ekle: üç kolun üçü de menzil içi parola kullanıyordu, bu yüzden hiçbiri görmedi. | M6-01 B |
| **🔢 BİR SAYIYI YAZARKEN DEĞERİNİ DEĞİL, ETİKETİNİN HANGİ BÜYÜKLÜĞÜ GÖSTERDİĞİNİ KONTROL ET — M6-02'de BEŞ turda BEŞ kez, beşi de aynı kökten.** Vakalar: (a) düzyazı *"PORCELAIN IS THE BINDING GROUND"* derken **aynı turun kendi tablosu ve kendi test dosyası** `green-lite` diyordu — iki cümle **aynı commit'lenmemiş değişikliğin** içindeydi, ve kod doğru olduğu için hiçbir test itiraz etmiyordu; (b) tek bir cümlede **yük bir paydayla (260 = görüntüleme + giriş), pay başka bir paydayla (200 = yalnız görüntüleme)** hesaplanmıştı → yazılan **15×**, gerçek **11,5×**, ve hata **%30 iyi** okunuyordu; (c) *"türetilen çağrı yeri sayısı"* diye basılan sayı **dosya sayısıydı** (5), gerçek **12** — üstelik **aynı satırdaki döküm 12'ye toplanıyordu** ve bu, bir önceki turun *"artık sayı basılıyor"* diye sunduğu **kanıtın kendisiydi**; (d) bir anti-vacuity kapısı **farklı ton sayısını** sayıp mesajında *"there are dozens"* diyordu (o "dozens" **tekrarları** tarif ediyordu) → **meşru** bir sadeleştirme (`/85`→`/70`) taramanın *"yanlış dosyaları okuyorsun"* diye kırmızı vermesine yol açıyordu. **İki kural:** (1) bir sayı **hangi kolda/paydada** ölçüldüğünü **yazmalı** — *"resolver okuması"* ile *"resolver okuması + UPDATE"* aynı şey değil, *"dosya"* ile *"çağrı yeri"* aynı şey değil; (2) bir sayıyı **raporlayan** kodun, o sayının **adını taşıdığı şeyi izlediğini** mutasyonla kanıtla (M6-02'de ölçüldü: rapor 5→4 düşüyordu ama **beş çağrı yerli bir dosyadan dördü silinince hiç değişmiyordu**). ⚠️ Ve bu sınıfı düzeltirken **düzeltmenin kendi ağını aynı turda ölç** — yapıcı ilk denemesinde **çift saydı** (24), kendi mutasyonuyla yakaladı. | M6-02 |
| **🎨 SEVK EDİLMİŞ BİR KONTRAST HATASI ANCAK TÜRETİLMİŞ BİR TEST YAKALAR — ve "bağlayıcı zemin" ayrı bir iddiadır.** M6-02 `.docket-label`'ı **3,13:1** ile sevk edilmiş buldu (AA 4,5:1), **12 çağrı yerinde**, **çalışanın kutsal tap ekranı dahil**; artı beş ton daha 2,40–4,36:1 arasındaydı. Hiçbiri yıllardır fark edilmemişti çünkü **üründe otomatik kontrast ağı yoktu — sayılar yalnız düzyazıdaydı.** ⚠️ **İki ayrı şey pinlenmeli:** ton **değerleri** *ve* **hangi zeminin bağlayıcı olduğu**. İkincisi unutuldu ve tam da beklenen şey oldu: iki tur (**ve orkestratörün brief'i**) `porcelain`'i bağlayıcı sandı çünkü o **sayfa zeminidir** — oysa karar **bağıl parlaklıkla** verilir ve `green-lite` (0,8229) porcelain'den (0,8627) **koyudur**. Yakalayan şey **türetilmiş test** oldu; elle yazılmış bir sayı olsaydı bugün de yanlış olurdu. **Kalıp:** ağ (i) paleti **config'den yeniden türetsin** (hex'in ikinci kopyası olmasın), (ii) **sıfır çağrı yerinde koşmayı REDDETSİN** (bir ton her zeminde kusursuz olup ürünün artık render etmediği bir sınıfı koruyabilir), (iii) düzyazının adlandırdığı zemini **aritmetiğin türettiğiyle** karşılaştırsın — ve **işaretin SİLİNMESİ de kırmızı vermeli**, çünkü düzyazı okuyan bir testi geçirmenin en doğal yolu cümleyi silmektir. **Ve `app.css`'e bağımlı olmasın:** üç `TestCompiledCSS_*` testi CI'da **daima SKIP** eder (`app.css` gitignore'da, CI `make css` koşmuyor) — *bir skip pass değildir.* | M6-02 |
| **📉 BİR PERFORMANS SAYISI, ÖLÇÜLDÜĞÜ VERİTABANININ İSTATİSTİKLERİ KADAR GERÇEKTİR — ve yanlış istatistik yanlış SONUCA değil, yanlış TASARIMA yol açar.** M6-03 bir `LEFT JOIN` filtresini `MATERIALIZED` CTE ile değiştirdi ve gerekçesi ölçümdü: **14.303 ms → 527 ms, 27×**. `EXPLAIN` gerçekti (planlayıcı nested loop seçip tenant'ın kadrosunu **satır başına** yeniden tarıyordu), ama **veritabanı hiç `ANALYZE` edilmemişti**: `n_live_tup` **5.326** iken tablo **111.167** satır tutuyordu, yani planlayıcı **~20× küçük** istatistikle çalışıyordu. `ANALYZE` sonrası aynı tenant/gün/sıcak önbellek: **değiştirilen şekil ~4× DAHA HIZLI**, ve çit **ayırt edilemez**. **27× geri çekildi.** ⚠️ **Ders çift:** (1) bir plan ölçümü almadan önce `pg_stat_user_tables`'a bak — `last_analyze` **NULL** ise ölçtüğün şey planlayıcının cehaletidir, sorgunun maliyeti değil; (2) **geri çekmek yetmez, kararı yeniden gerekçelendir**: CTE tutuldu ama artık *hız* için değil, *"ikisi farklı şekilde başarısız olur — CTE'nin maliyeti sınırlı, join filtresininki planlayıcıya bağlı ve tap havuzunu paylaşan bir yüzeyde 14 sn gözlendi"* diye, ve **karşı çıkanın bilerek geri alabileceği** biçimde. Bir ölçüm çürüdüğünde kod yanlış olmak zorunda değil — **gerekçe** yanlıştır. | M6-03 |
| **🤖 BİR DÜZELTMEYİ BETİKLE YAPIYORSAN, BETİĞİN NİYETİNİ DEĞİL ÇIKTISINI RAPORLA — iddia-etiketi sınıfının SEKİZİNCİ vakasının mekanizması buydu.** M6-03'ün 6. turunda yapıcı üç düzeltmeyi *"yapıldı"* diye raporladı; denetçi ölçtü: **N4 2'de 1, N5 0'da 3, N6 0'da 2**. Sebep basit ve genellenebilir: düzeltmeleri yapan python betiği **ilk `assert`'te ölmüş** (bir em-dash uyuşmazlığı) ve sonraki satırlara **hiç ulaşmamıştı**; yapıcı betiği **yazdığı için** yaptığını sanmıştı. **Kural: her düzeltme için `grep`/`shasum` ile SONUCU doğrula ve çıktısını rapora koy** — *"düzelttim"* bir niyet beyanıdır, `grep -c → 0` bir ölçümdür. ⚠️ Bu vakanın en zararlı satırı bir yorumun *"vendored into `web/static/js/`"* demesiydi: vendor'u oradan çıkarmanın **tek sebebi** Tailwind'in taradığı ağaçtan kurtarmaktı, yani o yorumu izleyip bir sonraki kütüphaneyi oraya koyan kişi **kapatılan kusuru geri açardı**. Bayat bir yol adı, bayat bir sayıdan **daha tehlikelidir** — sayı yanlış bilgi verir, yol adı **yanlış eylem** ürettirir. | M6-03 |
| **🔴🔴 BİR KUSURUN YAŞI İLE ERİŞİLEBİLİRLİĞİ AYRI ŞEYLERDİR — kapsamın "yeni kod" değil YENİ ERİŞİLEBİLİRLİK olduğu yer.** M6-05 B, var olan `invite.Manager` dikişini **ilk kez üretim yoluna** bağladı ve bununla M5-02'den beri şemada duran bir delik **erişilebilir** oldu: bir daveti harcamak **kardeş daveti emekliye ayırmıyordu**, yani iki basış = iki canlı kod ve **en eskisi, en yenisi kullanıldıktan sonra da aktive ediyordu** — ikinci-cihaz yolu `RevokeAllForEmployee` çağırdığı için **gerçek çalışanın telefonu oturumdan düşüyor** ve eski linki tutan **onun yerine trust 100 ile mesai yazıyordu**. Kötü niyetli müdür gerekmiyor: *gönder → "gelmedi" → tekrar gönder*. Hiçbir test görmüyordu çünkü **hiçbir test iki davet üretip birini harcadıktan sonra diğerini denemiyordu**. **Kural: var olan bir mekanizmayı ilk kez bir kullanıcı yüzeyine bağlarken, o mekanizmanın ESKİ varsayımlarını yeniden ölç** — ve *"bu kod bu görevde değişmedi"* bir denetim muafiyeti değildir. | M6-05 B |
| **🔴🔴 ŞEKİL MEKANİZMA DEĞİLDİR — ve KALIBIN PARÇALARINI SAYMAK, SAYININ KENDİSİNİ DE ÖLÇMEYİ GEREKTİRİR (üçüncü ve dördüncü vaka).** Deaktivasyon onayı `logincontext.go`'nun *"şekli"* diye sevk edildi: çerez **vardı**, hidden alan **vardı**, `subtle.ConstantTimeCompare` **vardı** — **anahtar yoktu**, ve bir denetçi **kendi çerezini basıp** onay ekranını hiç istemeden geri alınamaz aksiyonu çalıştırdı. O mekanizmanın güvenliği **sunucu anahtarı altındaki HMAC**'ten gelir; kopyalanan şey **şekil**, atlanan şey **güvencenin geldiği yer**di. Aynı hata M6-01 B'de (`tap.go`'nun **üç** aşamasından biri) ve M6-04'te (`sameOriginGate`'in **sırası**) yapılmıştı. ⚠️ **Çare *"parçaları say"* — ama ikinci denemede sayı ON çıktı ve denetçi ON BİRİNCİYİ buldu** (gelecek-saat sapma sınırı), yani *"saydım"* diyen dosya **kendi bitirmek için var olduğu hatayı** küçük ölçekte tekrarladı. **Kural: kalıbı kopyalarken parçalarını say, sonra SAYIYI kaynağa karşı ölç — ve listeyi kaynaktan TÜRETMEYİ dene.** | M6-05 B |
| **🔴 İKİ DOĞRU KATMAN, ARALARINDA BİR DELİK — ve BİR AĞIN ÜRÜNÜ DEĞİL HARNESS'I ÖLÇMESİ.** İki ayrı şekil, ikisi de M6-05 B'de. (a) Tüketen SQL ifadesinden `cancelled_at IS NULL` silindiğinde **uçtan uca test yeşil** kaldı, çünkü `Lookup` iptal edilmiş kodu **daha erken** reddediyor: iki katman da doğru, ama aralarındaki geçişi hiçbir şey tutmuyor → ağ **ifadenin kendi katmanına** kondu. (b) *"Onay tek atımlıktır"* testi replay'i `browser` yardımcısı üzerinden yapıyordu ve **o yardımcı çerez silmeyi uyguluyordu** — yani assertion **sunucunun değil, TEST YARDIMCISININ işbirliğini** ölçüyordu; çerezi yeniden basan bir istemci tek onayı **3 kez** harcadı. **İki kural:** (1) bir garanti iki katman tarafından sağlanıyorsa, **her katmanı ayrı ayrı** mutasyonla sına — birinin diğerini maskelediği yer tam olarak deliğin yeri; (2) bir ağ yazarken *"bu assertion ÜRÜNÜN neyini ölçüyor, HARNESS'ın neyini?"* diye sor — yardımcının nezaketini ölçen bir test, sevk edilen davranış hakkında **hiçbir şey** söylemez. | M6-05 B |
| **🧾 BİR SENTINEL/DAL EKLERKEN, ONU KAYDEDEN YOLU DA EKLE — yoksa silinmesi KAYIT KAYBEDER.** M6-05 B `invite.ErrCodeCancelled`'ı sevk etti (bir devralma girişiminin bıraktığı **tek iz**) ve **hiçbir testi yoktu**: `grep --include='*_test.go'` → **0**. Dalı silmek kontrolü `default:`e düşürüyordu, o da **`failAttempt` çağırmadan 500** veriyordu → sunulmuş bir kimlik bilgisi **kayıtsız** kalıyor, bütçe harcanmıyor, ziyaretçiye *"sunucu bozuk"* deniyordu — **§4.6 kayıt kaybı**, ve **tüm paket yeşil**. ⚠️ Sebep listesi **kapalı bir tabloydu** ve yeni sebep **satırsız** sevk edilmişti; kapalı tablo bir ağ değil **değişiklik dedektörüdür** (M5-10). Çözüm iki katmanlı: tablo **`go/ast` ile `internal/invite`'ın sentinel'lerinden TÜRETİLDİ** (eklenen bir sentinel artık kırmızı verir ve doğal onarımı **ona bir sebep vermektir**), **ve** `default:` dalı da **kaydetmeye** başladı — o dal §4.6'ya göre zaten yanlıştı. | M6-05 B |
| **🔴 BİR AĞIN *YAKALADIĞI* İLE *YAKALADIĞINI SÖYLEDİĞİ* AYRI İKİ İDDİADIR — ve ikincisi ölçülmezse birincisinden TEHLİKELİDİR.** M6-05 A'da §4.7'nin tip duvarı gerçekten çalışıyordu (`Person`'a **yeni** bir alan ekleyen sızıntı → kırmızı, doğrulandı) ama yanındaki cümle *"bir ÖNEK ya da DÖNÜŞÜM de burada kapanır — the cover is the type wall"* diyordu. Denetçi tek satırla çürüttü: `d.name AS department_name` yerine `left(s2.token_hash, 8)` → **yeni alan yok**, `Person` değişmiyor, şablon değişmiyor, ve **`make test` 16/16 yeşil**; kendi probu sayfada öneki buldu. Üründe sızıntı **yoktu** — yanlış olan **ağın yazılı kapsamıydı**, ve bir okuyucuyu *"bu kapalı"* diye yanlış yere bakmaktan alıkoyuyordu. Aynı sınıf aynı görevde **üç kez daha**: kuşak ağının kapsam sayısı **kendi bastığı komutla** çelişti (8/41 → gerçek 9/43) · bir ağın gerekçesi **var olmayan bir rota tablosu** beyan etti · alt sorgu tarayıcısı **21 saldırının 7'sinde** yenildi. **Kural: kapsam cümlesini yazmadan önce ağı YEN; yenemediğin şekli ADIYLA SAY, ve *"başka yerde kapatıldı"* yazma — o cümle, sayılmış bir açıktan daha pahalıdır.** | M6-05 A |
| **🔁 AYNI RED İKİ TUR ÜST ÜSTE GELİYORSA, DÜZELTME YANLIŞ KATMANDADIR.** M6-05 A'da en büyük kadronun boyu **altı kez** kaydı (8.718 → 9.138; **bir denetimin içinde iki kez**, çünkü `make test` her koşuda o tenant'a işe alım yapıyor ve `employees`'te DELETE grant'i yok). İki turda da yanıt *"daha taze bir sayı yaz"* oldu ve **her taze sayı tur içinde bayatladı**; üçüncü turda ajan *"artık tek evde"* diye ilan etti ve **aynı dosya 138 satır sonra** kuralı çiğnedi, dördüncü turda **kendini kapattığını ilan eden diff'in içinde üç yeni alıntı** çıktı. Çözüm sayıyı tazelemek değil, **alıntılamayı bırakmaktı**: argüman **eşitsizlikten** kuruldu (`RosterPageSize × adminSessionLimit ≥ rosterDesignCeiling`) ve kararı süren **kaymayan** olgu yazıldı (*"bu veritabanında 25'te bütçeyi aşan tam bir tenant var, 50'de hiç yok"*). **Kural: bir düzeltme aynı bulguyu ikinci kez alıyorsa, düzeltmeyi tekrarlama — KATMANI değiştir.** ⚠️ Ve teli **yanlış alarm verecek kadar** genişletmek **ölçülüp reddedildi** (30 meşru ölçümü işaretliyordu): *yanlış alarm veren bir tel, bir sonraki kişinin sileceği teldir.* | M6-05 A |
| **🧨 BİR YORUM BİR VARSAYIMA YASLANIYORSA, O VARSAYIMI *UYGULAYAN* ŞEYİ GÖSTER — yoksa varsayım kusurun kendisidir.** M6-05 A'da `maxRosterCursorName` yorumu başarısızlık modunu **kusursuz teşhis ediyordu** (*"the list would return to the same page forever, which is a §4.6 failure"*) ve hemen ardından *"512 is far above any name a payroll holds"* diyerek duruyordu. Ölçüldü: `employees.full_name` şemada **sınırsız `text`**, CHECK **yok**, validator **yok**, testte vaka **yok** — ve 605 rune'luk bir ad sayfa sınırına düşünce **10 kişi gezinerek ULAŞILAMAZ** oldu, 50 kişi iki kez listelendi. **İki genel denetim turu bu kusurun üstünden geçti**; güvenlik merceği varsayımı **veri olarak saldırdığı** için buldu. ⚠️ Düzeltme de ders: sınır **büyütülmedi, SİLİNDİ** (imleç adı değil **kimliği** taşıyor) — ve bu **ikinci bir bulguyu** da kapattı (hiçbir çalışanın adı artık URL'de yolculuk etmiyor). **Bir sınırı büyütmek, sınırın var olma sebebini ortadan kaldırmaktan daima zayıftır.** | M6-05 A |
| **🧪 BİR TESTİN SINIR VAKASINI *HESAPLAMA*, ÖLÇ — kusurun kendi hatasını tekrarlarsın.** M6-05 A'da uzun-ad testinin ilk hâli **sahte geçti**: yapıcı sayfa sınırının indeksini aritmetikle türetmiş, oysa fixture'ın kendi kişileri indeksi kaydırıyordu ve uzun ad **sınıra hiç düşmemişti**. Test artık **sayfa birine hangi satırda bittiğini soruyor**, sonra o satırı uzatıyor. ⚠️ Ve aynı turda ikinci bir süreç hatası: bir mutasyon **hiç uygulanmadı** (heredoc tırnak hatası) ve geçen test **sağkalım sanıldı** — düzeltmesi `MUTATION APPLIED` bastırmak oldu. **Kural: hem sınır vakasını hem mutasyonun uygulandığını ÜRÜNE SORARAK doğrula; ikisi de "hesapladım" ile geçilmez.** | M6-05 A |
| **📁 UNTRACKED DOSYA İÇİN `git` HİÇBİR KORUMA SAĞLAMAZ.** M6-05 A'da bir yedekten geri yazma `web/templates/pages/employees.templ`'i **bayat bir kopyayla ezdi** ve bir düzeltmeyi sessizce geri aldı; dosya **untracked** olduğu için `git status` bunu gösteremezdi ve `git checkout` bir **no-op**'tur. Grep'le yakalandı. **Kural: yeni dosyalar üzerinde çalışırken doğrulamayı `git`'e değil, İÇERİĞE sor** (`grep`/`shasum`/`cmp`) — ve yedeklerini **kendi dizininde, tam yolu koruyarak** tut; paylaşılan scratchpad'de önceki turların bayat kopyaları birikir. | M6-05 A |
| **🔴🔴🔴 BİR AĞIN MERKEZÎ CÜMLESİ, AĞIN KENDİSİ KADAR ÖLÇÜLMELİ — M6-04'te DÖRT ağ, DÖRDÜ DE tam olarak yapmadığı şeyi iddia ediyordu.** (1) Karar formu kendi URL'ini yazıyordu, yorum *"bölüm tablosundan okunuyor, **formun post ettiği yer bu**"* diyordu → hedefi `/admin/nowhere` yapan mutasyon **gerçek HTTP + gerçek Postgres testleri dahil tüm paketi yeşil** bıraktı. (2) Üç yorum bölümlerin `layout.Panel` render ettiğini söylüyordu; bileşenin **sıfır çağıranı** vardı ve *"yapısal"* denen script'sizlik bir **çalışma zamanı argümanıydı** — denetçi tam o tek kelimelik düzenlemeyi yaptı, **derlendi ve render edildi**. (3) Kuşak ağı *"**ANY** method call"* diyordu; `\.([A-Z]\w+)\(` regex'i **üç şekilde** yenildi (satır sonunda nokta · parantez öncesi boşluk · metot değeri) ve yüklem **tamamen silinmiş** hâlde `gofmt` kararlı, `vet` temiz, **iki belt de yeşil** kaldı. (4) AST'ye taşındıktan sonra **okuyucusu** kaçırıldı: alfabetik olarak önceki bir dosyadaki **tek yorum satırı**, sabitlenmemiş `-- name:` aramasını başka bir sorgunun gövdesine çözüyordu → `make sqlc` kapsamsız sorguyu **üretim koduna** yazdı ve suite **16/16 yeşil** kaldı. **Kural: bir ağın cümlesini yazmadan önce ağı ÜÇ ŞEKİLLE YEN; yenemediğin şekli LİMİT olarak yaz.** Ve **aynı olgu için iki farklı desen kullanma** — (4)'ün kökü buydu: türetim satır-sabitli, okuyucu değildi. | M6-04 |
| **🧱 BİR AĞ, YAPISAL BİR TEMSİL VARKEN METİN TARIYORSA, YENİLMESİ AN MESELESİDİR.** M6-04'ün iki ağı da regex/`strings.Contains` ile çalışıyordu ve ikisi de yenildi; ikisi de **`go/ast`**'ye taşınınca üç atlatma şekli birden kapandı (`*ast.SelectorExpr` boşluğa, satır sonuna ve değişken adına **bakmaz**; bir `BasicLit` asla bir çağrı değildir). ⚠️ **İkinci yarısı en az onun kadar önemli: AST de bir sınır taşır ve o sınır YAZILMALI.** `reflect.ValueOf(q).MethodByName("X")` bir **string**'dir, AST'ye görünmez — yapıcı *"derlenen bir örnek kuramadım"* diye **savundu**, denetçi **kurdu** (derlendi, `vet` temiz, yüklem silikken iki belt de yeşil, kuyruk çalışmaya devam etti). Yani *"ölçemedim"* ile *"olamaz"* aynı cümle değildir ve ilki ikincisi gibi yazılırsa **bir sonraki denetçi onu ölçülmüş bir deliğe çevirir**. **Kural: metin yerine yapı; ve yapının göremediğini SAY.** | M6-04 |
| **📐 BİR SEÇENEĞİ ELEMEK DE ÖLÇÜMLE OLUR — M6-04'ün iki kullanıcı kararında da belirleyici şey TEK bir ölçümdü.** `layout.Panel` kararında ajan iki yolu da **inşa etti, ölçtü, geri aldı** ve kararı süren cümle bir görüş değil bir sonuçtu: *"(b)'de tek testi nötralize edip düzenlemeyi yapınca **derlendi ve paket yeşil kaldı**; (a)'da **derlenmiyor**."* Not alanında da öyle: *"write-only bırakalım"* ancak **9.940'ta 20 not / en uzun 24 karakter / sorgu farkı ölçülemez** rakamları önüne konunca gerçek bir seçenek oldu. ⚠️ Ve **aşırı iddia etmemek kararın bir parçasıdır**: seçilen yol *"genişletmeyi imkânsız yapmıyor, bir **string** düzenlemesini bir **identifier** düzenlemesine çeviriyor ve ilkini derleyiciye reddettiriyor"* diye yazıldı; kalan yol (`PanelShellWithScript` adını vermek → derleniyor, yalnız test yakalıyor) **adıyla** sayıldı. | M6-04 |
| **⏱️ TEK BİR ÖLÇÜMÜ NOKTA OLARAK YAZMA — sayı-etiketi sınıfının ON BİRİNCİ vakası, ve bu kez bir KARAR bunun üstüne kuruldu.** `make gen`'in maliyeti **3,34 sn** diye raporlandı ve *"+%2,3"* olarak kullanıcıya sunuldu; yeniden ölçüm **9,39 / 13,75 / 14,89 sn** (soğukta 27,86) verdi — fark **`go run <mod>@version`'ın build cache'i**. Yapıcı kendi geri çekti. **Kural: bir süre/maliyet sayısı ya ARALIK olarak yazılır ya kaç kez ölçüldüğü söylenir; tek okuma "en sıcak okuma"dır ve planlamayı yanıltır.** (Aynı sınıf bu görevde iki kez de **orkestratörü** vurdu: brief'e yazdığım *"`transaction_reviews` 0 satır"* çıktıyı `head` kesmişken **görmeden** yazılmıştı — gerçek 9.813; ve *"31.193 flag"* DB geneliydi, kuyruk değil.) | M6-04 |
| **🧯 BİR KISITIN "RUHU" AYRI BİR İDDİADIR VE AYRI YAZILMALI.** `transaction_reviews`'ün `UNIQUE (transaction_id)` + `REVOKE UPDATE/DELETE` + trigger üçlüsü §4.3'ün **lafzını** kusursuz koruyor; ama aynı üçlü, §4.3'ün kendi telafi yolunu (*"düzeltme = yeni kayıt + `audit_log`"*) **o tablo için yapısal olarak kullanılamaz** kılıyor — yanlışlıkla Approve'a basan müdürün kararı kalıcı, ve tek çare bir migration (ürün içinde audit izi **olmayan** bir müdahale). **İki denetçi bunu bağımsız buldu ve ikisi de "ihlal değil" dedi** — doğrusu bu; ama **hiçbir dosya söylemiyordu**, ve bugün etkisi sunumla sınırlıyken **M6-07 raporları geldiğinde parasal** olacak. **Kural: bir kırmızı çizginin bir tabloda UYGULANMADIĞINI ölçtüysen, ADR yaz — "ihlal değil" bir susma gerekçesi değildir.** | M6-04 |
| **🩺 BİR KAPININ HAVALE ETTİĞİ ÇARE GERÇEKTEN VAR MI? — M6-04'te *"bunu `make check` yakalar"* diyen cümle ÖLÇÜLDÜ ve yakalamıyordu.** `check: fmt lint test` idi; `fmt` = `gofmt -w` + `templ fmt`, yani **biçimlendirici**, üretici değil. Ölçüldü: bir `.templ`'in gövdesi değiştirilip `make gen` atlanınca `_templ.go` **değişmiyor**, panel testleri **`ok`**, ve ürün **eski markup'ı render ediyor** — `make check` de CI de yeşil. Üstelik `ci.yml`'in **kendi yorumu** aynı yanlış iddiayı taşıyordu. **Kural: *"X bunu yakalar"* yazmadan önce X'i mutasyonla koş** — çünkü var olmayan bir yakalayıcıyı adlandırmak, bir sonraki okuyucunun **bakmasını durdurur**. (Düzeltme: `check: fmt gen lint test`; sıra da ölçüldü — kaynak **önce** biçimlenmeli, üretim **sonra**, test **en son**.) | M6-04 |
| **🧨 BİR MUTASYONUN KIRMIZI VERMEMESİ BİR ÖLÇÜMDÜR — "SONUÇ" ile "MEKANİZMA" ayrı iki şeydir.** M6-06 B panelinde eşzamanlılık ağını kurarken `Unmount`'un atomik önkoşulunu **oku-sonra-yaz** ile değiştirmek **n=10 ve n=30'da yeşil kaldı**. Sebep yapısal: `Unmount` satırı **önce okuyor** (ize yazacağı duvarı, ki yazma onu yok ediyor), yani READ COMMITTED altında geç kalan goroutine **o okumada** reddediliyor ve UPDATE'e **hiç ulaşmıyor**. Yani test **doğru sonucu** (tam bir kazanan) kanıtlıyordu ama **iddia ettiği mekanizmayı** değil. Yapıcı bunu **gizlemek yerine ölçtü** ve mekanizmayı gerçekten pinleyen yeri buldu: **`Mount`'un ön-okuması yok**, her goroutine UPDATE'e ulaşıyor, aynı mutasyon orada **deterministik KIRMIZI**. **Kural: bir mutasyon beklediğin yerde kırmızı vermiyorsa, testi zayıflatma — NEDEN vermediğini ölç; cevap çoğu zaman "ağ başka bir şeyi tutuyor"dur.** | M6-06 B panel |
| **🔁 "ŞEMA TERSİNE İZİN VERİYOR" İLE "MÜDÜR GERİ ALABİLİR" AYRI CÜMLELER — ve ikincisi eksikken KAPI çözüm değildir.** M6-06 B'de mount geri alınamıyordu: yanlış duvara monte edilmiş bir plaket panelden **hiçbir duvara taşınamıyor**, stoka alınamıyor, yedek yoksa kart **hiçbir kontrol sunmuyor**, ve tek çıkış **çalışan bir plaketi kalıcı emekliye ayıran** bir replace. Bunu savunan **iki cümle *"measured rather than assumed"* etiketi taşıyordu** ve ölçüm **tersini** veriyordu. ⚠️ Denetçi iki seçenek sundu (kapıla / cümleleri indir); **ikisi de yanlıştı**: kapılamak **yanlış mount'u engellemez** — müdür yanlış kapıyı onay ekranında da onaylar — yalnız bir tıklama ekler ve *"korudum"* hissi verir. Zarar **yapmakta değil, geri alamamakta**. Doğru kapanış, cümlenin **zaten var olduğunu iddia ettiği şeyi sevk etmek**: bir geri alma yolu. ⚠️ İnce yeri: geri alma **kendisi** kapıyı hizmet dışı bırakıyor → **asimetri**: yapıcı yön kapısız, yıkıcı yön kapılı. **Kural: bir eylemin geri alınabilirliğini ŞEMADA değil ROTALARDA ara.** | M6-06 B panel |
| **🔢 KODUN SAHİP OLDUĞU BİR KÜMEYİ TARİF EDEN ÇIPLAK BİR TAMSAYI, YORUMLARDA YER ALMAZ.** Sayı-bayatlaması M6-06'da **on dört kez** bulgu oldu ve **beş turda beşinde de** tekrarladı; tek tek düzeltmek işe yaramadı — bir tur *"bir listenin yanındaki sayı bayatlar"* demek için yazılmış bir paragrafı **aynı düzenlemede bayatlattı**, bir diğeri bayat bir sayıyı geri çeken paragrafın **içine yeni bir yanlış sayı** koydu (*"altı gün sonra"* → bir gün). Çare mekanik bir kural oldu: **ya cümleyi sayısız kur** (*"the words below"*, *"every POST under /admin/locations/"*), **ya türet ve koşuda bas**, **ya tarihine + YÖNÜNE bağla ve seviyeyi bırak**. Süpürme: beş dizin, **169 aday → 17 kod-sahipli yer**, **11'i sayı SİLİNEREK** kapandı. ⚠️ **Şema-sahipli** sabitler (RESTRICT anahtarları, GRANT sütunları, payload alanları) **bırakıldı** — onları büyüten şey bir **migration** ve migration **kendi kaydını yazar**. | M6-06 |
| **🕸️ ELLE BAKILAN BİR N LİSTESİ, N BÜYÜYÜNCE DELİK AÇAR — M6-06'da YEDİ KEZ.** Guardrail bir **denylist**'ti ve dördüncü durum altından geçti · §4.7 taraması **yedi yasaklı kelimeydi** ve oturum id'si hiçbirine çarpmadı · tip duvarı **denylist**'ti ve `[44]byte` geçti · `venueDoneWords` elle sayılmıştı · **eylem sözlüğü** yeni bir eylemi kaçırdı ve panel müdüre **ham veritabanı adını** bastı · **yazma-rota ağı** elle yazılmış bir çiftti ve yeni rota **ikisinin de dışında** kaldı · **onay matrisi** beş eylemi dörtle dolaştı ve **atlanan çift, öznenin yardım etmediği tek çiftti**. Çözüm her seferinde aynı: **TÜRET.** Eylem sözlüğü `go/ast` ile domain'in kendi bildirimlerinden; rota kümesi **gerçek router'dan** `chi.Walk` ile — ve ikincisi **Faz A'nın dört rotasını da** kapsadı, ki onlar bu özellikle **hiç kapsanmamıştı**. **Kural: bir ağ bir listeyi elle taşıyorsa, ağın ömrü listenin bakımı kadardır.** | M6-06 |
| **🗣️ YASAK ŞEKLİ AÇIKLAMAK İÇİN ALINTILAMAK DA ONU TAŞIR.** M6-06 B'de başka bir görevin bıraktığı tel (`TestComments_DoNotQuoteTheDriftingRosterSize`, *"tenant" + parantezli dört haneli sayı* şeklini rezerve ediyor) yapıcının **kendi düzyazısını iki kez** yakaladı — biri kodda, biri kartta, ikisi de o telin **var olma sebebini anlatan** cümlelerdi. Yapıcı **teli gevşetmedi** (M6-05 bir tel genişletmesini **30 meşru ölçümü** işaretlediği için geri almıştı) ve cümleyi yeniden yazdı. Aynı şekil `tappa-brand`'in *"bir Tailwind sınıfını yazma, tarif et"* kuralında ve `employees_test.go`'nun kendini muaf tutmasında zaten vardı. **Kural: bir yasağı açıklarken örneği VERME, tarif et — yoksa ağını kendi metninle tetiklersin.** | M6-06 B panel |
| **📝 YAPILMAMIŞ BİR İŞİ YAPILMIŞ BİLDİRMEK, RİTMİN TAM TERSİDİR — ve iki kez oldu, ikisi de yakalandı.** M6-06 B panelinde yapıcı *"iki paragrafı yeniden yazdım"* dedi (yazılmamıştı, denetçi `grep`'le buldu) ve *"o sayı artık hiç yazılmıyor"* dedi (yazılıyordu **ve 9 kat yanlıştı**). ⚠️ Karşılaştır: aynı ajan **yedi kez kendi hatasını kendi bildirdi** (uygulanmayan mutasyon, `;` yüzünden daima 0 dönen Makefile, zsh word-splitting, kirlenen verdict, yalan söyleyen fixture, sıcak-uç zamanlama, kendi kırdığı `make audit`) ve her biri bir kusuru **sevk edilmekten kurtardı**. İkisi arasındaki fark **niyet değil, DOĞRULAMA**: kendi bildirdikleri **ölçüm sonrası**, bildirmedikleri **ölçüm öncesi** cümlelerdi. **Kural: brief her *"düzeltildi"* cümlesinin arkasında bir `grep`/test verdict'i istemeli — ve ritim, yapıcının raporuna değil, yapıcının FALLIBLE olduğuna dayanmalı.** | M6-06 B panel |
| **📜 BİR BORÇ MADDESİNİN ÖNERDİĞİ ÇÖZÜM DE, ÖNCÜLÜ KADAR ÖLÇÜLMELİDİR.** M6-06 B'de backlog **T9** hem trigger koşulunu (`NEW.last_ctr > OLD.last_ctr`) hem gerekli sütun listesini yazıyordu; **ikisi de yanlıştı**. Koşul `last_ctr`'a dokunmayan bir **retire'ı reddediyor** (canlı hata mesajıyla gösterildi), ve listede **`location_id` yoktu** — ama aynı günün ikinci kullanıcı kararı (envanter modeli) tam olarak o sütunun UPDATE'ini istiyordu: ***iki karar birbirini uygulanamaz kılıyordu ve bunu yalnız uygulamaya çalışmak ortaya çıkardı.*** Aynı turda **T8'in ÖNCÜLÜ** de çürüdü (*"mevcut satırlar zaten büyük harf, veri taşıması yok"* → **18.010** küçük harfli, **12.437'si append-only `transactions`'tan referanslı**) ve önerdiği düzeltme **§4.3'e çarpıyordu**. **Kural: bir borcu uygularken önce ÖNCÜLÜNÜ, sonra ÖNERİSİNİ ölç; ikisi de eskimiş olabilir.** | M6-06 B |
| **🧩 BİR DÜZELTME, AYNI DOSYADAKİ BAŞKA BİR CÜMLEYİ GEÇERSİZ KILABİLİR.** M6-06 B'de bir tur `tags_retired_keeps_its_location` CHECK'ini ekledi; aynı dosyanın `Down` bloğu **kısıt sayısını** (*"7 CHECK"* → gerçek **8**) **ve** belgelenen **kurtarma reçetesini** birden yanlışladı — reçete operatöre *"retire et, sonra Down"* diyordu, oysa yeni kısıt o UPDATE'i **23514** ile reddediyor ve `Down`'a hiç ulaşılmıyor. Yapıcı **doğru sayıyı aynı turda kartına yazmıştı** ama ölçtüğü dosyaya taşımamıştı. **Kural: bir kısıt/kural eklerken, o dosyanın kısıt SAYAN ve kısıt DAVRANIŞI TARİF EDEN her cümlesini yeniden ölç.** | M6-06 B |
| **🔭 BİR AĞIN KAPSAMI, TARAYICISININ KAPSAMIDIR — ve "çağıranı yok" sessiz bir boşluktur.** M6-06 B'de §4.5 kuşağı dört yeni sorguda **kanıtsızdı**: yüklem silinince `internal/db` **RC=0** kaldı, çünkü test **daima B'nin bağlamından** koşuyordu ve orada RLS zaten 0 satır veriyordu — test **kemeri** ölçüyor, **kuşağı** değil, ve yorumu tersini iddia ediyordu. Ağın onları **görmemesinin** sebebi ise sorgu şekli değil **tarayıcı kapsamıydı**: `storeQueryNames` yalnız kendi paket dizinini ayrıştırıyor **ve `_test.go`'yu eliyor**; dört sorgunun tek çağıranı bir test dosyası olduğu için **dördüncü bir AST kopyası sıfır isim türetip yeşil bir boşluk basardı**. Çözüm dördüncü tarayıcı değil, **dosyadan türetim**: sorgu **var olduğu için** denetlenir, çağıranı olduğu için değil. ⚠️ Ve yeni ağ kurulur kurulmaz **gerçek bir kör nokta** buldu (matcher'ın `stopRE`'sinde `FOR UPDATE` yoktu → doğru kapsanmış `AdvanceTagCounter` işaretleniyordu). **Kural: kuşağı kanıtlayan test AYNI tenant bağlamında koşmalı — yoksa RLS kanıtı yutar.** | M6-06 B |
| **🗣️ BİR `t.Fatal` MESAJI BİR MUTASYONU ADLANDIRIYORSA, O MUTASYONU KOŞ.** M6-06 B'de bir test *"the IS NOT NULL predicate is gone"* diye hata basıyordu; yüklem silindiğinde test **PASS** kaldı, çünkü fixture'da o dalı doğuracak satır (`channel='manual'`) **hiç yoktu**. Yapıcı bunu kapatırken **aynı sınıfı kendi dosyasında taradı ve iki boşluk daha buldu** — biri tam olarak **§4.4 replay deliğinin kendisi** (`prev.old_ctr <` → `<=`). **Kural: bir iddia cümlesi bir mutasyonu adlandırıyorsa, o cümle bir SÖZDÜR; sözü koşarak öde.** | M6-06 B |
| **🔒 BAZI GARANTİLER DAVRANIŞSAL OLARAK TEST EDİLEMEZ — o zaman KATALOG iddiası doğru araçtır, ve bu YAZILIR.** M6-06 B'de `FORCE ROW LEVEL SECURITY`'yi pinlemek istendi; ölçüldü ki **davranışsal bir test imkânsız**: `FORCE` yalnız **tablo sahibini** bağlar, sahip `tappa_owner` **superuser** ve RLS'i koşulsuz atlar → sonucu değişen **hiçbir sorgu yok**. Test `pg_class.relforcerowsecurity`'yi okuyor ve **neden davranışsal olamadığını** açıkça yazıyor. Aynı turda `NULLIF` **iki yarımla** pinlendi (katalog **artı** davranış — davranışın göründüğü tek yer GUC'un yazılıp boşaldığı bağlantı). **Kural: bir garantiyi pinleyemiyorsan uydurma; ARACI DEĞİŞTİR ve neden değiştirdiğini yaz.** | M6-06 B |
| **🕳️ YOKLUK, YOKLUKLA TEMSİL EDİLİR — sıfır bir değerdir.** M6-06 B'de *"son görülme"* bir liste sütunu **olamadı**: sqlc dört sorgu şeklinde de tipi yanlış çıkarıyor, cast'li hâli **derleniyor** ve **tam da o görevin eklediği satırlarda** çöküyor (`cannot scan NULL into *time.Time`). Yapıcının cümlesi: ***"sıfır `time.Time` = `uuid.Nil` hatasının ikizi"*** — ve o ikizi aynı turda ölçmüştü: `google/uuid` `sql.Scanner` uyguladığı için NULL **hatasız `uuid.Nil`** oluyor, yani bir plaketin **monte edilmemiş** olması *"sıfır id'li bir lokasyona monte"* gibi görünüyor. Çözüm ayrı bir sorgu: *"hiç taplanmadı"* artık **anahtarın yokluğu**. **Kural: nullable bir sütun Go'da sıfır değere düşüyorsa, kod artık YOKLUĞU göremez — tipi ya işaretçi yap ya yokluğu SATIRIN YOKLUĞUNA taşı.** | M6-06 B |
| **🧪 MUTASYON ARACININ KENDİSİ YALAN SÖYLER — dört ayrı şekilde ölçüldü.** M6-06'da: (1) bir `Makefile.probe` `go test …; echo "EXIT=$?"` ile bitiyordu → **`;` yüzünden make daima 0**, iki mutasyon **sahte YEŞİL**; (2) bir perl regex **hiç uygulanmadı** ve test yeşil geçti; (3) 19 dosyalık bir düzeltme **zsh word-splitting** yüzünden hiç uygulanmadı ve *"başarılı"* göründü (*"21 kaldı"* sayımıyla yakalandı); (4) yıkıcı bir DDL mutasyonunu `CHECK (true)` ile "geri yüklemek" **bir sonraki verdict'i kirletti**, ve bir başka DDL mutasyonu paylaşılan DB'ye **3 illegal satır commit etti** (kısıt geri eklenemedi). **Kural: verdict'i ARACIN çıktısından değil TESTİN kendi çıktısından oku · exit kodunu `$?` ile DOĞRUDAN yakala · mutasyonun uygulandığını `diff` ile kanıtla · ve YIKICI DDL için HER MUTASYONA TAZE BİR VERİTABANI.** | M6-06 A+B |
| **🚧 BİR GUARDRAIL DENYLIST OLARAK YAZILIRSA, SÖZLÜK HER BÜYÜDÜĞÜNDE BİR DELİK AÇILIR.** M6-06 B `tags.status`'a dördüncü bir değer ekledi ve `sys:tag-not-active` (`retired || lost`) onu **görmedi** → stok plakete tap §5 satır 1'e **hiç çarpmadı**; güvenlik merceği NFC'nin kapalı ama **yanlış gerekçeyle** kapalı olduğunu, **QR'ın ise açık** olduğunu ölçtü. Denetçi *"bugün erişilemez"* diye bloklamadı (haklıydı: `tags`'a INSERT eden sorgu yok) — **orkestratör yine de kapattı**, çünkü bu projede *"bugün üretim yolu yok"* koruması **iki kez sessizce sona erdi**. Eşdeğerlik **iddia edilmedi, ÖLÇÜLDÜ**: test durum sözlüğünü **migration'ın CHECK kümesinden türetiyor** ve iki formu dört değerin dördünde karşılaştırıyor. **Kural: guardrail ALLOW-LIST yazılır, ve sözlüğü ŞEMADAN türetilir — o zaman beşinci değer, kimse hatırlamasa da eklendiği gün reddedilir.** ⚠️ Aynı turda migration kendi riskini **FAZLA** yazmıştı (*"can end as `ok`"* — `ok` erişilemez, en kötü `flag`): **riski abartmak da bir ölçüm hatasıdır.** | M6-06 B |
| **🛡️ KALDIRILMASINI HİÇBİR TESTİN FARK ETMEDİĞİ BİR SAVUNMA, SAVUNMA DEĞİLDİR.** M6-06 A'da `parseGPS`'in `NaN` deliği kapatıldı ve kod *"THAT IS THE WHOLE REPAIR"* dedi. Denetçi muhafızı **eski hâline döndürdü**: suite **yeşil** kaldı — çünkü ondan önce koşan `plainDecimalRE` `NaN`'ı zaten reddediyordu. Davranış doğruydu, **atıf yanlıştı**: kimse o satırın silindiğini fark etmezdi. Çözüm testi gevşetmek değil, muhafızı **bağımsız** pinlemekti (aralık fonksiyonunu doğrudan `math.NaN()` ile çağıran birim testi) — ve iki savunmanın **tamamlayıcı** olduğunu yazmak. **Kural: bir savunmayı 'asıl düzeltme' diye adlandırmadan önce onu TEK BAŞINA geri al ve kırmızıyı gör.** | M6-06 A |
| **📊 BİR SAYI DOĞRU OLABİLİR VE POPÜLASYONU UYDURMA — VE BU AYNI GÖREVDE İKİ KEZ OLDU.** *"Dört `EXISTS` = 8,154 ms"* üç canlı yere yazıldı; denetçi **171–218 ms** ölçtü. Sebep sorgu değil **veri**: `EXISTS`'lerin `OR`'u **kısa devre yapıyor** ve o tenant'ta 9 lokasyonun **9'unda çalışan** var, yani `transactions` **hiç değerlendirilmiyordu** (sıra değişince ~125 ms). Yapıcı düzeltti — **düzeltmesi de yanlıştı**: bu kez rakam **tenant filtresiz**, 122 bin satırlık tam tablo taramasından geliyordu. Kendi cümlesi: ***"Her iki seferde de sayı doğruydu ve popülasyon uydurmaydı."*** Gerçek değişken **tenant'ın işlem hacmi** — aynı ifade **0,55 ms ile 242 ms** arasında. ⚠️ Ve yanlış olan sayı, tam olarak **elenen alternatifi ucuz gösteren** sayıydı. **Kural: bir zamanlama, TENANT'I + SATIR SAYISI + İŞLEM SAYISIYLA yazılır, ya da hiç yazılmaz.** | M6-06 A |
| **🕳️ BAZI SIZINTILARI DAVRANIŞSAL BİR TEST ASLA GÖREMEZ — O ZAMAN AĞ ÇAĞRI GRAFINA TAŞINIR.** M6-06 A'da bir alt-vaka (*koordinat normalleştiricisi vardiya alanına sızmasın*) **tam vakumdu** ve yapıcı bunu ölçtükten sonra **sebebini** buldu: replacer `,`→`.` yapıyor, `parseClock` **iki nokta** istiyor, hiçbir değişim iki nokta üretemez → **hiçbir girdinin ayrıştırma sonucu değişmez**. Yani ne kadar iyi yazılırsa yazılsın, sonucu gözleyen bir test bu sızıntıyı **hiçbir zaman** yakalayamaz. Çare **`go/ast` ile çağrı-grafı ağıydı**: değer yalnız izinli fonksiyondan referanslanabilir. ⚠️ **Ve o ağın İLK hâli de yanlıştı** — `os.ReadFile("locations.go")` ile **tek dosya** tarıyor ama mesajı **paket** diyordu; denetçi aynı paketin başka dosyasına gerçek bir sızıntı koydu (müdürün venue adındaki virgül sessizce noktaya dönüyordu) ve paket **160,5 sn yeşil** kaldı. **Kural: gözlenebilir sonucu değişmeyen bir kural DAVRANIŞLA değil YAPIYLA pinlenir — ve o yapının kapsamı DİLE bırakılır, dizgiye değil.** | M6-06 A |
| **🚫 YASAKLI-DİZE TARAMASI İLE İZİNLİ-ANAHTAR KÜMESİ AYNI ŞEY DEĞİLDİR.** M6-06 A'da bir `audit_log.detail` testi §4.7'yi **yedi yasaklı kelimeyle** (`token, cookie, gps, lat, lng, password, hash`) koruyordu. Denetçi struct'a `Note` alanı ekleyip içine **panel oturumu id'sini** koydu: **üç sevk edilmiş test de yeşil** — çünkü bir uuid hiçbir kelimeye çarpmaz, base64 bir jeton da çarpmaz. Aynı repoda **doğru kalıp zaten vardı**: `jsonb_object_keys` ile **izinli anahtar kümesi** iddia etmek, yani yeni bir alan **bir gerekçeyle eklenene kadar** kırmızı vermek. **Kural: bir kara liste, düşünebildiğin şeyleri korur; bir beyaz liste, düşünemediklerini.** | M6-06 A |
| **🚪 BİR KAPIYI YARIM KAPATMAK — DÖRDÜNCÜ KEZ.** M6-06 A'da silme `owner`-only yapıldı ve **üç yarı ayrı ayrı** gerekti: ekranda buton yok · sunucuda `mayRemove` · **ve onay jetonunun mint EDİLEMEMESİ**. Üçüncüsü olmasa kapı iki aşamalı olurdu ve ilki açık kalırdı. Doğrulama da ayrı ayrı yapıldı: **Y1** yalnız sunucu testini, **Y2** yalnız UI testini kırmızı yaptı — yani hiçbiri diğerinin yükünü taşımıyor. Önceki üçü: M6-01 B'de `tap.go`'nun üç aşamasından **biri** · M6-04'te `sameOriginGate`'in **sırası** · M6-05 B'de onayın **anahtarsız** olması. **Kural: bir korumayı 'kapattım' demeden önce PARÇALARINI SAY, sonra her parçayı AYRI mutasyonla kırmızıya düşür.** | M6-06 A |
| **🔬 MUTASYON ARACININ KENDİSİ YALAN SÖYLEYEBİLİR.** M6-06 A'nın son turunda yapıcının scratchpad `Makefile.probe`'u `go test …; echo "EXIT=$?"` ile bitiyordu — **`;` yüzünden make daima 0 dönüyor** — ve hedef paketi sabitliyordu. Sonuç: bir mutasyon **yanlış pakete** koşup *"no tests to run"* → YEŞİL, bir diğeri **gerçek `--- FAIL` bastığı hâlde** YEŞİL raporlandı. Yapıcı, çıktının verdict'iyle çelişmesinden fark etti, aracı yeniden yazdı (`$?`'ı doğrudan yayan, **derlemeyen mutasyona güvenmeyi reddeden**) ve dördünü de yeniden koştu: hepsi KIRMIZI. **Kural: mutasyon sonucunu ARACIN VERDICT'inden değil, TESTİN KENDİ ÇIKTISINDAN oku; ve `&&`/`;` zincirlerine exit kodu taşıtma.** | M6-06 A |
| **📍 SATIR NUMARASI KAYAN BİR İDDİADIR.** M6-06 A'da kart *"M6-09'un kabul kriteri (kart satır 3608)"* diyordu; kriter **3716**'daydı ve 3608 başka bir bloğun ortasıydı — kartın kendisi büyüdükçe işaretçi kaymıştı. Aynı sınıf: kart *"26 `audit.Event{}` çağrı yeri / 8 dosya"* diyordu ve **kendi eklediği dokuzuncu dosyayı** saymıyordu (gerçek 27/9). **Kural: bir belgeye BÖLÜM BAŞLIĞIYLA atıf yap, satır numarasıyla değil; ve bir sayıyı yazarken KENDİ değişikliğini de say.** | M6-06 A |
| **🗃️ `omitempty` BİR KAYIT İZİNDE "BOŞ"U "KAYDEDİLMEDİ"DEN AYIRT EDİLEMEZ KILAR (§4.6).** M6-06 A'da silinen lokasyonun `audit_log` izi **tek hayatta kalan kayıttı**, ve `static_ips` anahtarı **58 satırın 37'sinde yoktu** çünkü boş dilim `omitempty` ile düşüyordu. Beş yıl sonra *"bu lokasyonun IP kanıtı var mıydı?"* sorusunun cevabı **"hayır yoktu"** ile **"o sürüm bunu kaydetmiyordu"** arasında ayrılamaz. Kök neden daha da beteri çıktı: `created_at` **iki DELETE `RETURNING` listesinde de yoktu**, yani kayıt zamanı **kalıcı olarak** yok ediliyordu. ⚠️ Ve düzeltmenin testi **anahtarı** tutuyordu, **değeri** değil — `0001-01-01T00:00:00Z` hem *"var mı"*yı hem `time.Parse`'ı geçiyordu. **Kural: satırın gittiği yerde iz TAM olur; ve bir alanın VARLIĞINI değil DEĞERİNİ iddia et.** | M6-06 A |
| **🧭 BİR ORKESTRATÖRÜN "TEMİZ AĞAÇ" ÖLÇÜMÜ BİLE YANLIŞ PENCEREDEN OLABİLİR.** M6-06 A'nın başında `git status --short` temiz döndü, ama iki yapıcı ajan **aynı brief'le** koşmuştu ve ilki (reddedildiği bildirilen çağrı) 37 dakika çalışıp implementasyonu **yazmıştı**. Denetçi bunu transkriptlerden ve mtime'lardan çözdü. ⚠️ **Ve denetçinin bundan çıkardığı sonuç da yanlıştı** (*"tuzaklar konmadan yazılmış"*) — brief birebir aynıydı. **Kural: provenance bir arkeoloji merakı değil, DENETİMİN ŞEKLİNİ belirler; ama ondan çıkarılan sonuç da ölçülür.** | M6-06 A |
| **🔐 VENDOR'LANMIŞ BİR DOSYANIN DIGEST'İ YAZILIYOR AMA OKUNMUYORSA, TEDARİK ZİNCİRİ KORUMASI DEĞİL BİR NOTTUR.** M6-03 htmx 2.0.10'u gömdü, sha256'yı README'ye **yazdı**, ve bir test *"src `/static/` ile mi başlıyor"* + *"gömülü ağaçta var mı"* diye baktı — **hash'e hiçbir şey bakmadı**, `make audit` de bakmadı. Güvenlik denetçisinin sorusu bunu açığa çıkardı: *"bir sonraki güncellemede sha'yı KİM kontrol edecek?"* → cevap **disiplin**. Tek bayt değişse (yanlış indirme, bozuk mirror, özensiz yükseltme) hiçbir kapı kırmızıya dönmez ve dosya panelin **tek scripted sayfasında** `script-src 'self'` altında çalışır. Çare tek testti: **README'deki hash'i OKU** (ikinci bir kopya tutma — o da kayar) ve gömülü dosyaya karşı **yeniden hesapla**; bir bayt eklendiğinde kırmızı. **Kural: repoya giren her üçüncü-taraf ikilisi için, digest'i BİR MEKANİZMA okumalı** — ve o mekanizma köken (provenance) iddia etmemeli, yalnız *"baytlar ile kayıt tek bir edit'te birlikte değişmek zorunda"* demeli. | M6-03 |
| **🕸️ BİR AĞI GENİŞLETMEK, GENİŞLETİLMİŞ HÂLİNİ YENMEYİ ATLAMAK İÇİN GEREKÇE DEĞİLDİR.** M6-07 A'da §4.7 refleksiyon testi tam eşleşmeli **dokuz ad** tutuyordu; denetçi `Coords`/`GPS`/`SessionToken`/`InviteCode` ekledi, **test geçti**. Alt dizeye çevrildi — ve **bir sonraki** denetçi **`RemoteAddr`**'ı geçirdi: token listesinde `address` **vardı**, `addr` **yoktu**, ve `RemoteAddr` §4.7'nin yasakladığı `source_ip` için **Go'nun kendi alan adı** (`http.Request.RemoteAddr`). Aynı tur, aynı ağ, aynı sınıf. **Kural: bir ağı düzelttiğinde, DÜZELTİLMİŞ hâlini yeni bir saldırı listesiyle yeniden yen — düzeltme, sınıfın kapandığının kanıtı değildir.** ⚠️ Ve genişletmenin **yanlış alarm maliyeti ölçülür**: aynı ağ `Resource*`'u re-**source**'a takıyordu ve `position` repoda **82 meşru kullanım** taşıyordu (keyset cursor) → biri allow listesine, öteki bilinçli **yasaklanmadı**. | M6-07 A |
| **📜 BİR YORUMUN DAYANDIĞI DB KISITI VAR OLMAYABİLİR — VE FAIL-OPEN TAM ORADA SAKLANIR.** M6-07 A'da `endpointState`'in `default` dalı bilinmeyen her verdict'i **ödenebilir** sayıyordu; savunması *"migration 0005'in CHECK'i yalnız zincire katılan kayıtlara yön verir, yani yapısal test kesindir"*di. `pg_constraint` okundu: öyle bir kısıt **yok**, var olan **tek yönlü** (`verdict <> 'ok' OR type IS NOT NULL`). Rollback'li bir sonda `verdict='reject', type='in'` satırını **kabul ettirdi** ve o satır bordroya **8 saat** yazardı. Ulaşan yol bugün yok — **ama bariyer bir KOD değişmezi (`decide.go`'nun yön kapısı), yorumun iddia ettiği ŞEMA kısıtı değil**, ve M6-08 ikinci yazıcıyı ekliyor. **Kural: bir yorum bir DB kısıtına atıf yapıyorsa kısıtı KATALOGDAN OKU; ve bir `default`/`else` dalı, bilinmeyen girdiyi ürünün İYİMSER tarafına düşürüyorsa o bir fail-open'dır — §4.6'da doğru cevap "sayma ama KAYBETME"dir.** | M6-07 A |
| **🗺️ BİR PLAN ADI ÖLÇÜM DEĞİL, O ANIN İSTATİSTİKLERİNİN FONKSİYONUDUR.** M6-07 A'da aynı `EXPLAIN` bloğu **üç ardışık sürümde üç farklı indeks** iddia etti; ikisi ertesi gün, **üçüncüsü bağımsız bir okuyucu tarafından AYNI GÜN** çürütüldü (`_occurred_idx` yazılıydı, `_location_idx` çıktı). Blok her seferinde bloğun kendi talimatına (`ANALYZE` önce, sıcak önbellek) uyularak ölçülmüştü. **Kural: yazılacak şey plan ŞEKLİ (tenant-kapsamlı bitmap scan + süzen filtre) + ÖLÇÜLEN BÜYÜKLÜKLER + TARİH'tir; indeks ADI yazılmaz.** Bu, *"bir zamanlama tenant'ı + satır sayısı + işlem sayısıyla yazılır"* kuralının ikinci yarısı. | M6-07 A |
| **♻️ DEV DB BÜYÜDÜĞÜ İÇİN YORUMDAKİ HER MUTLAK SAYI KALICI OLARAK BAYAT KALIR.** M6-07 A'da yapıcının yazdığı `27 337 / 1 986 / 458 / 24 893` flag dağılımı **ertesi gün** `28 084 / 2 058 / 476 / 25 550`, gün sonunda `28 431` oldu — `make test` her koşuda satır ekliyor. Üstelik bir sayı **yanlış etiketliydi** (*"33 621 yönlü non-practice"* aslında tenant'ın **toplamı**; gerçek yönlü non-practice **14 177**) ve ondan türetilen *"bir hafta bunun ~%90'ı"* cümlesi **%35** çıktı. **Kural: mutlak sayı yerine ORAN yaz, ya da TARİHE demirle ve üreten SQL'i yanına koy; ve bir sayıyı yazmadan önce ETİKETİNİ ölç — "toplam" ile "filtreli" arasındaki fark, sayının kendisinden daha sık yanlış olur.** | M6-07 A |
| **🧪 GERİ-YÜKLEME BETİĞİ MUTASYONU HİÇ UYGULAMAMIŞ OLABİLİR — VE SONUÇ "AĞ TUTTU" GİBİ OKUNUR.** M6-07 A'nın düzeltme turunda yapıcının python betiği şablonu geri yüklerken **yalnız yorumu** koydu, `if` bloğunu değil — çünkü aradığı kontrol dizesi zaten yorumun içindeydi. Yani *"mutasyon ağı yenemedi"* diye raporlanacaktı, oysa **ağ hiç kurulmamıştı**. Yapıcı kendi debug probe'uyla (render edilen metin **boştu**) yakaladı. Aynı sınıfın önceki örnekleri: heredoc tırnak hatası (mutasyon hiç uygulanmadı) · zsh kelime bölmesi (21 yerin **hiçbirine** uygulanmadı) · `Makefile.probe`'un `;`'si (make **daima 0**). **Kural: her mutasyonda UYGULANDIĞINI ve GERİ ALINDIĞINI `git diff` (untracked dosyada `git diff --no-index <yedek> <dosya>`) ile GÖSTER — "koştum, yeşildi" mutasyonun varlığını kanıtlamaz.** | M6-07 A |
| **📐 KUŞAK+KEMERİN KEMER YARISI ÖLÇÜLEMEYEBİLİR — VE BU BİR KUSUR DEĞİL, BİR CÜMLE SEÇİMİDİR.** M6-07 A'da denetçi `LEFT JOIN employees e ON e.tenant_id = t.tenant_id` yüklemini düşürdü, `make sqlc` koştu → **belt testi yeşil, çapraz-tenant DB testi PASS**. Sebep: A'nın bağlamında **RLS zaten** B'nin `employees` satırlarını gizliyor. Yani JOIN'deki tekrar gerçek bir savunma ama **tek başına ölçülemiyor**. Sorgunun yorumu *"her JOIN tenant_id'yi yineliyor"* diyordu ve bu **doğruydu** — *test edildiğini* iddia etmiyordu. **Kural: bir savunmanın VARLIĞINI iddia etmek ile ÖLÇÜLDÜĞÜNÜ iddia etmek ayrı cümlelerdir; ikincisini ancak mutasyonu kırmızıya döndürebiliyorsan yaz.** | M6-07 A |
| **🤝 BİR GÖREV SANA BİR İŞİ *ADIYLA* DEVREDİYORSA, DEVREDENİN O SINIFI NASIL ÇÖZDÜĞÜNÜ OKU — ÇÖZÜM ZATEN REPODA OLABİLİR.** M6-07 B'nin CSV kaçış ağı `unicode.IsControl` kullanıyordu; Go'da o **yalnız Cc**, ve **13 Cf runeu** (BOM · ZWSP · SHY · WJ · LRM · **RLO**) formül önekini gizleyerek **çıplak geçiyordu**. Güvenlik merceğinin bulduğu kanıt kusurdan daha öğreticiydi: **`internal/session/manager.go:512` bu sınıfı ZATEN çözmüş** (`case unicode.Is(unicode.Cf, r)`) **ve o dosyanın doc yorumu formül nötralleştirmeyi ADIYLA M6-07'ye devretmiş** (*"Neutralising a leading =, +, - or @ is the CSV writer's job (M6-07)"*). Devralan taraf **sınıfın yarısını** almış. Bu, *kalıbın yarısını kopyalama* sınıfının **beşinci** vakası ve ilk kez **devir yazılıydı**. **Kural: bir kartın/yorumun sana devrettiği her iş için, devredenin kodunda o işin ADINI ara — `grep` ile, tahminle değil.** | M6-07 B |
| **📏 BİR SINIRIN İLKELİ OLUP OLMADIĞI, ONU DIŞLAYAN GEREKÇEYLE SINANIR.** M6-07 B'de kaçış ağının atlama kümesinden `Mc` dışarıda bırakıldı, gerekçe *"boşluk kaplar → saklanma yeri değil"*. Doğrulayıcı göz gerekçeyi **kendi kanıtıyla** çürüttü: **`U+3164` HANGUL FILLER** kategori **`Lo`** ve çoğu yazı tipinde **sıfır görünür genişlikte** render oluyor — yani o ölçüte göre **içeride olmalıydı**; aynı sınıfta `U+115F`, `U+1160`, `U+FFA0`, `U+2800`. Denetçinin cümlesi: ***"Sınır ilkeli değil, aramanın durduğu yer."*** Çözüm listeyi uzatmak **değildi**: ilke Unicode'un kendisinde vardı ve **Go onu ship ediyor** — `unicode.Properties["Other_Default_Ignorable_Code_Point"]`. **Kural: elle çizilmiş bir sınıf listesi, onu ÜRETEN ilkeyi bulana kadar hep bir üye eksiktir; ve bir dışlamayı savunan cümle, o dışlamanın TESTİDİR — cümleyi kendi ölçütüne uygula.** | M6-07 B |
| **🧮 BİR DENETÇİNİN SAYISI DA POPÜLASYONUYLA BİRLİKTE OKUNUR.** M6-07 B'de iki denetçi `ReportEventCap`'e olan mesafede çelişti: genel göz *"eşiğe **869 satır**"* dedi (ham `type IN ('in','out')`, haftaya göre), güvenlik merceği sorgunun **gerçekten okuduğu pencereyi** ölçtü (`NOT practice` + 18 sa `StaleOpenIn` kuyruğu). Yapıcı `ListWorkedShiftEvents`'in birebir `WHERE`'iyle **dört varyant** çıkardı ve üçüncü bir göz **dördünü de bağımsız SQL'le** üretti: **%96 · %111 · %54 · %63 (gerçek sorgu)** — **farkın tamamı `NOT t.practice`**. Yani *"ölçüm doğru, popülasyon uydurma"* sınıfı bu kez **denetçide** çıktı, ve **yanlış sayı tehlikeli yöndeydi** (eşiği olduğundan **16 kat yakın** gösteriyordu). **Kural: çelişen iki ölçümü hakemsiz bırakma — SORGUNUN KENDİ YÜKLEMİYLE yeniden ölç; ve bir denetçinin rakamını da yapıcınınki gibi popülasyonuyla iste.** | M6-07 B |
| **🧪 BİR MUTASYONUN YEŞİL KALMASI, KORUMANIN ÇALIŞTIĞI DEĞİL FIXTURE'IN O FARKI TAŞIYAMADIĞI ANLAMINA GELEBİLİR — İKİ KEZ.** M6-07 B'de yapıcı bunu **kendisi** buldu, iki ayrı yerde: parite fixture'ı **dejenereydi** (tek kişinin saati = haftanın toplamı), bu yüzden *"CSV kendi satırlarını toplasın"* mutasyonu **yeşil** kaldı; ve needs-action fixture'ında **37 bayat girişin hepsi ilk sayfadaydı**, bu yüzden sayacı sayfayla sınırlayan mutasyon **yeşil** kaldı. İkisi de fixture ayırt edici yapılınca **KIRMIZI**. **Kural: bir mutasyon yeşil geçtiğinde ilk hipotez "koruma çalışıyor" DEĞİL, "fixture bu farkı üretemiyor" olmalı — fixture'ın iki dalı AYIRT ETTİĞİNİ ayrıca kanıtla (tercihen testin içinden türeterek).** | M6-07 B |
| **🪓 TEK-KAYNAK BİR DOSYAYI IN-PLACE PYTHON'LA DÜZENLEME — `state.md` BİR KEZ SIFIR BAYTA DÜŞTÜ.** Orkestratör `state.md`'yi bir python betiğiyle güncelledi; betik dosyayı **yazmak için açtı (truncate)** ve sonra bir **surrogate kaçışında** (`"🔴"`) `UnicodeEncodeError` ile patladı — dosya **0 bayt** kaldı. Commit'li sürümden kurtarıldı, veri kaybı olmadı, ama son commit'ten sonraki iki düzenleme **yeniden yapılmak zorunda kaldı**. **Kural: `state.md`/`backlog.md` gibi tek-kaynak dosyalarda atomik `Edit` kullan; python şart ise ÖNCE geçici dosyaya yaz, SONRA `mv` et — ve emoji için `\U0001F534` yaz, surrogate çifti değil.** | M6-07 B, orkestratör |
| **📐 BİR KABUL KRİTERİNİN YAPISAL YARISI TUTARKEN ANLAMSAL YARISI TUTMAYABİLİR.** M6-08'in kartı *"var olan bir kaydı düzeltmek = yeni satır + audit; UPDATE yok"* diyordu. Yapısal yarı **kusursuz** (UPDATE/DELETE hiçbir yolda yok, trigger `tappa_owner`'ı da bağlıyor). Ama *"düzeltir"* yarısı ölçülünce **yarısı çalışmıyor**: rapor motoru **en geç `in` + en erken `out`** aldığı için eklenen satır **yalnızca KISALTIR** — az ödemeyi onaran iki yön çalışmıyor — ve bir `in` düzeltmesi **kapatılamaz** bir açık kayıt bırakıyor (her kapatma denemesi **kalıcı bir satır daha** + müdüre **yanlış bir cümle daha**). Yapıcı bunu **kendi kartını yanlışlayarak** buldu, davranışı **değiştirmedi**, müdürün commit'ten önce okuduğu **onay ekranına** yazdı, ve **ADR 0011**'e üç çıkış yoluyla kaydetti. **Kural: bir kriteri "sağlandı" yazmadan önce SONUCUNU ölç — mekanizmanın varlığı, işe yaradığı anlamına gelmez.** | M6-08 |
| **🧱 İKİNCİ YAZICI GELİNCE, BİR "KOD İNVARİANTI" TAŞIYICI HÂLE GELİR — AMA RİSKİ DE ÖLÇ.** M6-07 A ölçtü ki şemada *"ok ⟹ yön"* var, **tersi yok**: `verdict='reject', type='in'` **kabul ediliyor** (üç denetçi de rollback'li sondayla kabul ettirdi). Orkestratör bunu M6-08'e *"sen ikinci yazıcısın, bariyer düşebilir"* diye devretti ve **migration 00014** ile yapısal yapıldı. ⚠️ **Ama uyarının ikinci yarısı ölçümle ÇÜRÜDÜ:** *"böyle bir satır rapora çalışılmış saat girer"* — **girmiyor**, çünkü **iki tur önce onaylanan `endpointState` fail-safe'i** onu `HoursAwaiting`'e karantinaya alıyor (`worked=0 / awaiting=8h`). Bariyer **ikiden dörde** çıktı ve `decide.go`'nun `if`'i **çıplak invariant değildi** — adı konmuş bir test tablosunun konusuydu. **Kural: bir riski devrederken hem YAZ hem ÖLÇ; savunma derinliği zaten orada olabilir, ve "bariyer tek" demek onu aramamış olmaktır.** | M6-08 |
| **🔁 BİR DÜZELTME TURU, KENDİ DEĞİŞİKLİĞİNİN TERSİNİ SÖYLEYEN METİN BIRAKIR — TEK RED BUYDU.** M6-08'de migration 00014 sevk edildikten **sonra** iki dosya hâlâ *"şema yönlü bir refüzü kabul eder"* diyordu (biri **var olmayan bir test adına** atıf vererek), üçüncüsü *"her sonuç 303 döner"* diyordu (aynı turun kendi değişikliği **beş dalı 200** yapmıştı), ve migration'ın **kendi gerekçesi** bir sondayla yanlışlandı (*"`NOT IN` nullable sütunda sessizce bozulur"* → iki biçim de **aynı** bozuluyor; gerçek fayda **yeni bir verdict değerine karşı fail-closed** olmak). M6-06 B'de aynı sınıf: bir kısıt eklemek `Down`'ın kısıt **sayısını** ve **reçetesini** birden bozmuştu. **Kural: düzeltme turu bitince, DEĞİŞTİRDİĞİN DAVRANIŞI TARİF EDEN HER CÜMLEYİ yeniden oku — kendi turunun yazdıkları dahil; ve bir yorumun atıf verdiği test adının VAR OLDUĞUNU grep'le.** | M6-08 |
| **🧫 PAYLAŞILAN DEV DB'YE YAZAN BİR TEST, YAZDIĞINI GERİ ALMALI — İKİNCİ VAKA.** M6-08'de ölçüldü: `TestPlaqueJourneyDB_TheBudgetOnATenantWithHistory` her koşuda **2 plaket** ekliyor, `tags`'ten DELETE yok, demo tenant **287**'ye ulaşmış, liste kapağı **200** → **P(fail) ≈ %28,8 ve her koşuda artıyor**. Yani **o kilometre taşındaki her *"suite yeşil"* iddiası olasılıksaldı** — üç ajan da ona dayanmıştı. Birinci vaka M6-06 B'nin `vat_number` çakışmasıydı (**koşum başına %1,13**). Düzeltme: iddiayı **kapaktan bağımsız** bir satıra taşımak + `t.Cleanup` (⚠️ `tappa_app` `tags`'te DELETE'e sahip **değil** → `tappa_owner` gerekiyor; ve `transactions` **§4.3 gereği silinemez**, çözüm ona çarpmamalı). **Kural: bir denetçiye "suite yeşil" dedirtmeden önce EN AZ İKİ koşum iste; ve şüphe varsa koşum öncesi/sonrası TABLO SAYIMI al — delta sıfır olmalı.** | M6-08 |
| **🪞 BAŞKA BİR AJANIN RAPORUNDAN ALDIĞIN ADI/SAYIYI, KENDİ BRIEF'İNE KOYMADAN ÖNCE DOĞRULA.** Orkestratör M6-07'nin güvenlik denetçisinden **`RecordTransaction`** diye bir sorgu adı aldı ve M6-08'in devir notuna yazdı — **öyle bir sorgu yok**, adı `InsertTransaction` (`grep` boş dönüyor). Aynı turda ikinci bir hata: yapıcıya *"kuşak ağı 27/69 → 27/70, düzelt"* talimatı verildi; denetçi ölçtü — `git show HEAD:db/queries/*` → **69**, yani **69 devir anında DOĞRUYDU** ve 70 ancak yeni sorgu eklendikten sonra oldu. **Bir düzeltme talimatı da bir iddiadır ve ölçülmemiş olabilir.** **Kural: brief'e giren her ad `grep`'le, her sayı kendi komutunla doğrulanır — özellikle başka bir ajandan geliyorsa.** | M6-08, orkestratör |
| **🧯 BİR KUSURU ADLANDIRAN YORUM, KUSURUN KAPSAMINI DA ADLANDIRMALI.** M6-09 A'da ekran, dokuz baseline belgesinden **biri** okunamadığında motor guardrail'lere düşerken diğer **sekizini `In force`** ve onlardan türeyen izinleri **`Granted to`** diye basıyordu — ADR 0004'ün *"var olan en tehlikeli hata"* dediği şey. ⚠️ **Yapıcının kendi yorumu bunu ADIYLA koymuştu** (*"printing an unreadable document as if it were running is how a customer comes to believe a rule is in force when the engine is ignoring it"*) ve kusuru **o kural için** çözmüştü; **yayılım alanı** için çözmemişti. **Kural: bir kusuru yazan cümle, "bu SATIR için çözüldü" mü yoksa "bu SINIF için çözüldü" mü olduğunu söylemeli — ikisi ayrı iddiadır ve denetim ikincisini ölçer.** | M6-09 A |
| **🕳️ BİR AĞ ÜÇÜNCÜ KEZ YENİLDİĞİNDE YAMAYI BIRAK — KAPSAMI ÜRETEN KAYNAĞA BAĞLA.** M6-09 A'nın inert-markup ağı **yedi kez** yenildi, dört denetim boyunca, ve **her yama yeni bir kapı açtı**: deny-list dardı (`<a hx-post>`/`<details>`/`contenteditable`) · **düzeltmenin kendisi** ağın göremediği yere düştü (koşullu blok hiç render edilmiyordu **ve** çapa ondan sonraydı) · **dört koşullu bloğu hiçbir fixture sürmüyordu** (biri üretimde **her sayfada**, tam da *"no policy can widen this one"* cümlesinin dibinde) · kabuğun **kendi token sözlüğüyle** kurulan tam işlevli POST kontrolü · **ödünç tanık** · `switch`/düz-Go görünmezliği · ve bir **öznitelik değerindeki ham `>`** tarayıcıyı körleştiriyordu. **Kapatanlar üç TÜRETİM oldu:** fixture kümesi **`.templ`'in kendisinden** (ilk koşu **sekiz sürülmeyen dal + altı ayırt etmeyen tanık** buldu — elle liste denetçinin bulduğunun **iki katı** eksikti) · elle yazılmış 38 token yerine **bayt-tam eşitlik** (`outsideBefore + outsideAfter == shellOnly`) · ve tarayıcı okuyamadığı etiketi **REDDEDİYOR** (fail closed, yeni bağımlılık yok). **Kural: yama sayısı üçe ulaştıysa kapsamı elle tarif etmeyi bırak; ve bağlayamadığın kanalı İKİNCİ DURMA KURALIYLA say — ağın "NE TUTAR / NE TUTMAZ" envanteri, ağın kendisi kadar değerlidir.** | M6-09 A |
| **📣 BİR LİMİT CÜMLESİ DE BİR İDDİADIR VE ÖLÇÜLÜR.** M6-09 A'da yapıcı sayılmış limitini **iki kez olduğundan küçük** yazdı: *"a bare link is not caught — it is a limit"* (denetçi `<form>/<button>` ile hiç bare link'e ihtiyaç duymadan geçti) ve *"`switch` repoda üç şablonda kullanılıyor"* (gerçek: **18 kol / 9 şablon**, ve biri bir `<form>` basıyor). İkincisi ancak sayı **türetilerek** kapandı. ⚠️ Ve aynı görevde bir başlık *"her koşullu dalı **TÜRETİR**"* diyordu, **iki satır altta** *"HER DALI TÜRETMİYOR, VE BAŞLIK ÖYLE DİYORDU"* yazıyordu — **başlık değişmemişti**, yani düzeltme metni **yapılmamış bir değişikliği bildiriyordu**. **Kural: bir limiti yazarken onu YENMEYE çalış (küçük yazılmış bir limit, hiç yazılmamış bir limitten TEHLİKELİDİR — okuyucu korumanın kapsadığını sanır); ve bir düzeltme metni yazmadan önce düzeltmenin YAPILDIĞINI grep'le.** | M6-09 A |
| **🟢 DÖRT RED, SIFIR SEVK EDİLMİŞ KUSUR — VE AYRIMI HER TURDA YAZMAK DİSİPLİNİ AYAKTA TUTTU.** M6-09 A projenin en çok RED alan görevi oldu (**8 tur, 4 RED, beş denetçi**), ama **ürün yedi denetimde de temiz çıktı**: sevk edilen şablonda kontrol yok, view'da alan yok, okuma **hiçbir satır yazmıyor** (baseline'sız tenant **0/0/0 → 0/0/0**), guardrail **taklit edilemiyor** (tenant belgesine `sys:`/`base:` sid + `ignore`/`redirect` → **dördü de reddediliyor**). Her raporda *"kırılan KORUMA, sızıntı değil"* ayrımının yazılması, dört RED'i **panik değil ölçüm** yaptı. **Kural: bir denetim raporunda "üründe sızıntı var mı" ile "koruma kapsadığını söylediğini kapsıyor mu" AYRI başlıklardır — ikincisi bloklayabilir ama birincisi gibi okunmamalı.** | M6-09 A |
| **🔒 BİR KAYIP-ÖNLEME TALİMATI, KİLİDİN NEREDE OLACAĞINI SÖYLEMEZSE YENİ BİR YARIŞ AÇAR.** M6-09 B'de orkestratör *"aynı adı ikinci kez yazmayı reddet"* dedirtti; yapıcı bunu `SELECT EXISTS` + `if` ile yaptı ve sonuç klasik **oku-sonra-yaz** oldu. Ölçüldü: **eşzamanlı bağlantı başına bir satır** (`racers=64` → **16 ikiz**, `NumCPU=16`; TOCTOU birebir görüntülendi), ve `policies` **DELETE vermediği** için ikizler **hiçbir ürün yoluyla silinemiyor**. **§4.4'ün *"tek ifadede, asla iki adıma bölme"* dersi `ctr`'ye özel değil — DELETE verilmeyen HER tabloya ait.** Çözüm bir **migration** oldu (`UNIQUE (tenant_id, layer, name)`), ve indeksin taşıyıcı olduğu **guard'ı körelterek** kanıtlandı: 64 racer hâlâ **1 satır**, 63 reddin hepsi **23505'ten**. | M6-09 B, 5.–6. tur |
| **🩹 BİR DÜZELTME, BAŞKA BİR YERDEKİ ÖLÇÜMÜ SESSİZCE GEÇERSİZ KILABİLİR.** M6-09 B'nin 7. turunda yapıcı bir test fixture'ını **doğru** şekilde düzeltti (bozuk-belge senaryosu için dokuz baseline belgesini de seed etmek). Aynı hamle, kartın **başka bir yerde** *"seçenek (a) ölçümle elendi"* diye gösterdiği falsifier'ı **öldürdü**: fixture artık 9/9/9 seed ettiği için `forTenant` yazacak bir şey bulamıyor, dolayısıyla *"(a) olsaydı 0/0/0 → 9/9/9 olurdu"* assertion'ı **düşemiyor**. Kimse fark etmedi; bir sonraki denetçi mutasyonla buldu. **Ders: bir fixture/seed değişikliği yaptığında, o fixture'ın SAĞLADIĞI koşula dayanan assertion'ları ara — özellikle başka dosyalardakileri.** Boş bir assertion, olmayan bir assertion'dan **kötüdür**: ölçtüğü sanılır. | M6-09 B, 8. tur |
| **🧾 `make test` ≠ `make check` — VE FARKI ÖLÇMEK ON SANİYE SÜRÜYOR.** M6-09 B'de **sekiz tur boyunca** *"`make check` exit 2 veriyor ama yalnız son `git diff --exit-code`'dan, çünkü iş commit edilmedi"* diye raporlandı. Gerçek sebep **ilk adımdı**: `templ fmt` `policies.templ`'i **yeniden biçimlendiriyordu** (`changed=1`), yani `check` **kaynağı kirletiyor** ve o yüzden takılıyordu — CLAUDE.md §10'a göre **iş bitmemişti**. Ölçüm: `git diff | shasum` + tüm dosyaların manifest'ini **önce ve sonra** al, `make fmt gen` koş, **eşit mi** bak. ⚠️ Ve `.templ` düzenledikten sonra **`make gen` YETMEZ — `make fmt gen`**; yapıcı bunu **iki kez** öğrendi (ikincisini kendi yakaladı, çünkü rutini benimsemişti). | M6-09 B, 9. tur |
| **🧮 BİR AĞ ON ÜÇÜNCÜ KEZ YENİLİYORSA SORUN YAMA DEĞİL, SAYAÇTIR — SAYMAYI BIRAK, TÜRET.** M6-09'un mutlak bölgesi `<div>` **sayarak** bulunuyordu ve her yazım sayacı yendi: `<!-- </div> -->` · **HTML5 bogus comment** `<!x </div> >` (tarayıcı `<!` sonrası `--`/`DOCTYPE`/`[CDATA[` görmezse ilk `>`'a kadar **yutar**, `</div>` **üretmez**; sayaç **sayar**) · `<!DOCTYPE` · `<?x>` · `<![CDATA[`. Her yama bir yazımı kapattı, sınıfı **açık bıraktı**. Kapatan hamle: bileşeni **tek başına render et**, mutlak bölge **onun ürettiği baytlar** olsun, ve sayfa onları **tam bir kez** içersin — sayaç ortadan kalkınca sayacın yanılabileceği **her yazım** da kalktı. ⚠️ **Ve tamamlaması iki adım sürdü:** ilk hâlde sarmalayıcı çağıranda kaldı, yani *"çağrının yanına"* konan markup bölgenin dışındaydı; sarmalayıcı bileşenin **tek kök elemanı** yapılınca aralarına **hiçbir şey** giremez oldu. | M6-09 B, 6.–8. tur |
| **🎭 TEST DOUBLE'I SINANDIĞI ÖZELLİKTE ÜRETİMDEN AYRIŞABİLİR — VE TAM O ZAMAN İŞE YARAMAZ.** M6-09 B'de **iki** vaka çıktı: `storedAuthority` üretimin **hep-ya-hiç** kuralını uygulamıyordu (bozuk belgeleri tek tek atlıyordu) — oysa test edilen özellik **tam olarak** *"bir belge okunamıyorsa hepsi düşer"*di; ve `fakeScribe`'ın yazma metotları yetki alanını **hiç okumuyordu**, dolayısıyla apply adımının `ErrNotPermitted` kolu **ölü koddu** ve o kolu silmek paketi **yeşil** bırakıyordu. **Düzeltme *"bir assertion ekle"* değil, ikizi SÖZLEŞMEYE EŞİTLEMEK.** Denetçi brief'ine kalıcı madde: *"`internal/*`'daki her double'ı gerçek uygulamayla karşılaştır — sınandığı özellikte ayrışan var mı?"* | M6-09 B, 9.–10. tur |
| **🧱 BİR AĞIN DUVAR OLMADIĞINI YAZMAK, AĞI GÜÇLENDİRMEK KADAR ÖNEMLİ.** M6-09'da guardrail'in kapatılamazlığını **on üç kez yenilen** bir test koruyordu, ve envanteri *"bu bölümde sıfır kontrol"* diye **mutlak** konuşuyordu. Güvenlik merceği ölçtü: gerçek duvarlar **şemada ve motorda** — `layer='guardrail'` INSERT'i **superuser'a bile** CHECK'e takılıyor, ve **en özgül + koşulsuz + izin veren** bir tenant kopyası karşısında motor **yine** `deny/sys:*` veriyor. Ağ bir **regresyon freni**. Bu cümle envantere yazılınca, kalan kaçışları *"sayılmış limit"* olarak bırakmak **dürüst** hâle geldi — yazılmasaydı okuyan, ağın taşıyıcı olduğunu sanırdı. | M6-09 B, 6. tur |
| **🟩 ON RED, SIFIR SEVK EDİLMİŞ KUSUR — VE DURMA KURALI GERÇEKTEN DURDURDU.** M6-09 B projenin en uzun görevi oldu (**11 tur, 10 RED, on farklı üçüncü göz, iki güvenlik denetimi**), ama **ürüne tek kusur geçmedi**: düşen ad, kırık `make check`, checkbox dokunma hedefi — üçü de commit'ten **önce** yakalandı. Kapanış kararını **birinci durma kuralı** verdi: art arda **iki tur 0 ürün mantık kusuru / 0 build kırılması** çıkardı (yalnız beyan düzeltmeleri) → mekanizma oturmuştu, on birinci denetim turu **açılmadı**. ⚠️ Kuralın işe yaraması için denetçi brief'inde *"bulguların DAVRANIŞ mı METİN mi — açıkça SAY, ama aramadan söyleme, en az sekiz mutasyon koş"* maddesi zorunlu: sayım istenmezse tur sayısı **kendi kendine** artıyor. | M6-09 B kapanışı |
| **🗺️ KARTI ÖLÇMEK BRIEF'İN EN YÜKSEK GETİRİLİ ADIMI — VE KAYNAK YOKLUĞU EN SIK KAÇAN ŞEY.** M6-11'in sekiz kabul kriterinden **üç yarısının** bugün kullanılabilir bir kaynağı yoktu. **Kart yalnız birini biliyordu.** İkincisini orkestratör brief yazmadan önce ölçtü (`sessions.created_ip` **yok**; eldeki `device_info` **7.430 oturumda 25 farklı değer**, ⅕'i NULL → *"aynı cihaz + çok çalışan"* **273 grup** = her cihaz ailesi) ve yapıcı ilk turda doğru kararı verdi. **Üçüncüsünü ikisi de kaçırdı**, yapıcı uygularken buldu: *"hiç çapraz-lokasyon göstermeyen çalışan"* **974 çalışanın 974'ünü** işaretliyor. **Kural: bir kriterin kaynağını UYGULAMADAN ÖNCE sorgula.** Ve düşürme kararı **ölçümle** verilirse dürüsttür — düşürülen her sinyal **ekranda** ilan edildi. **Sessizce yarım yapmak, ölçüp düşürmekten kötüdür.** | M6-11, kart ölçümü + 1. tur |
| **⏱️ BİR DUVAR SAATİ BİR SORGUNUN ÖZELLİĞİ DEĞİLDİR — KESİR ÖLÇ, MİLİSANİYE DEĞİL.** M6-11'de aynı `lag()`-vs-self-join karşılaştırması **dört kez** ölçüldü ve dördü de ayrıştı: **209/221/252 ms** (yapıcı) → **17–19 ms** (denetçi) → **103–110 ms** (yapıcı, yeniden) → **276–290 ms** (kapanış denetçisi). Sebep sonunda ölçüldü: planlayıcı **üç farklı strateji** seçmişti (Hash · nested loop · **Merge**) ve pencere **her `make test` ile büyüyordu** (1.063 → 1.365 → 1.547 → 1.657, çünkü suite içine yazıyor ve §4.3 geri almayı yasaklıyor). Bir sayı **iki kez** geri çekildi. Yerine konan **makineden bağımsız** iddia plandan okunan bir **kesirdi** — `Rows Removed by Join Filter` / n² ≈ **%84 kartezyen** — ve kapanış denetçisi onu **farklı makinede, farklı stratejiyle, farklı pencerede %83,9** ölçtü. ⚠️ Ve kararın kendisi **doğruydu**: gerçek fark ~45×. **Yani sorun yanlış karar değil, ölçülemez kanıttı.** | M6-11, 1.–3. tur |
| **🌍 BİR DESEN NE GÖREMEDİĞİYLE TANIMLANIR — VE FIXTURE'INDAN SEÇİLEN BİR SINIR COĞRAFYA KODLAR.** M6-11'in §4.7 ağı koordinatı `\b\d{2,3}\.\d{2,}\b` ile arıyordu. Denetçi uçtan uca kanıtladı: `(gps_lat - 30)::text` → sayfada **`5.898909`**, **bütün duvarlar yeşil**. `\d{2,3}` **iki tam haneden az hiçbir dereceyi göremez** — 0°–10° arası her enlem/boylam: **Accra · Singapur · Londra · Paris**. **Malta desenin çalıştığı tek coğrafyaydı** ve hiçbir yerde yazmıyordu. Yapıcının kendi teşhisi: *"`\d{2,3}` fixture'ın kendi koordinatlarından seçtiğim bir karardı; Malta'yı kodladığı, denetçi Accra'yı adlandırana kadar bana görünmez kaldı."* **Kural: bir sınır koyarken 'bu neyi DIŞARIDA bırakır' sorusunu ölç — ve cevabı fixture'ından değil, ürünün pazarından oku.** | M6-11, 2. tur |
| **🏷️ "COUNTED" BİR ETİKET DEĞİL, BİR İDDİADIR — YAZMADAN ÖNCE YENMEYE ÇALIŞ.** M6-11'in sayılmış-limit bloğu **"COUNTED RATHER THAN GLOSSED"** başlığını taşıyordu; denetçi onu **tek oturumda iki yoldan** yendi (bilimsel gösterim `to_char(…,'9.99999999EEEE')` → sayfada **tam precision enlem**, bütün ağlar yeşil; ve ASCII dışı ondalık ayırıcılar `U+FF0E`/`U+066B`). Yapıcının kendi ifadesi: *"Aklıma gelen limitleri saydım, sonra o sayımı 'sayılmış' diye etiketledim."* ⚠️ **Ve düzeltme kararında ikinci bir ders var:** yapıcı genişletmenin bedelini **ölçtü** — üç sayfa şeklinde de yanlış alarm **sıfır**, yani genişletmek **bedava**ydı — ama yine **saymayı** seçti ve gerekçeyi *"maliyet değil kapsam"* diye yazdı (sevk edilen hiçbir sorgu koordinat seçmiyor; hiçbir şeyin yazmadığı şekilleri kovalayan bir desen köstebek-vurmacadır). **İki okumayı da koda yazdı, yani karar yeniden ölçülmeden geri alınabilir.** | M6-11, 2.–3. tur |
| **👨‍👩‍👧 BİR DENETÇİNİN ADINI KOYDUĞU KUSURU DÜZELTİP KARDEŞLERİNİ ARAMAMAK — AYNI GÖREVDE ÜÇ KEZ.** M6-11'de yapıcı üç kez aynı deseni yaşadı ve **üçünü de kendi bildirdi**: (1) `anomaliesview.go`'nun *"nowhere to hide"* cümlesi düzeltildi, **dört satır aşağıdaki** aynı kusurlu cümle (*"Records is … the row's own denominator"*) görülmedi · (2) DB fixture'ının `guardrail` damgası düzeltildi, **aynı sınıfın unit fixture'ları** (`Answerable: 0` → *"1 of 0 after a counter gap"*) aranmadı · (3) `report.go`'daki bir atıf düzeltilirken **aynı dosyadaki ikincisi** ancak orkestratör istediği için bulundu. **Kural: bir bulguyu kapatırken 'bu kusurun kardeşi nerede olabilir' diye ara — aynı dosya, aynı sınıf, aynı kalıp.** Denetçi bir örneği verir, **sınıfı sen kapatırsın**. | M6-11, 1.–3. tur |
| **🎭 BİR TEST DOUBLE'I ŞEMAYI ATLATMANIN KOLAY YOLUNU SEÇEBİLİR.** M6-11'in DB fixture'ı **her** `sid`'i `layer='guardrail'` damgalıyordu — ürünün üretemeyeceği bir kombinasyon (§5: `sys:*` guardrail, `base:*` baseline). Sebep tembellik değildi: şema CHECK'i (`transactions_policy_decision_consistent`) **baseline katmanında bir `policy_version_id` ŞART KOŞUYOR**, ve `guardrail` damgası o zorunluluktan **kaçmanın** yoluydu. Yani ikiz, üretimin **ödemek zorunda olduğu bedeli ödememişti**. Düzeltme fixture'a gerçek bir `policies` + `policy_versions` satırı yazdırmak oldu. **Kural: bir fixture bir kısıttan kolayca kaçıyorsa, kaçtığı kısıt muhtemelen üretimin taşıdığı bir sözleşmedir.** | M6-11, 2. tur |
| **📜 ÖLÇÜLMEMİŞ BİR DEVİR CÜMLESİ, MIGRATION YORUMUNA VE PLAN KARTINA KADAR YAYILIR — ORKESTRATÖRÜN CÜMLESİ DE KANIT MUAMELESİ GÖRÜR.** M6-12 A'da orkestratör devir notuna *"kirlilik seed'de **ve** test fixture'larında"* yazdı — **ölçmeden**. Yapıcı bunu daraltıp *"the seeded Kebab Factory tenant had 83 such rows"* diye **üç yere** işledi (migration yorumu · test yorumu · plan kartı). Denetçi ölçtü: `test/fixtures/seed.sql:225-227` damgayı statüden **türetiyor** → **seed sıfır çelişen satır üretir**; çelişenlerin **hepsi** tek bir test fikstüründen, sayı **kalıcı değil** (koşu başına +1), ve **hiçbir ürün yolu o satırı yazamaz** (iki `COALESCE`). Kural **doğruydu** ama gerekçesi yanlış defterdeydi: *düzeltici* değil **savunmacı**. ⚠️ **Ders çift taraflı:** brief'e yazılan her ölçüm **kaynağıyla** yazılmalı (*"şu komutla ölçtüm"*), ve yapıcı devraldığı ölçümü **kendi komutuyla** doğrulamalı — brief'te *"devral ama HER BİRİNİ kendi komutunla doğrula"* maddesi tam bu yüzden var ve bu turda **işlemedi**. | M6-12 A, 1. tur |
| **⚖️ BİR KISITIN MALİYETİ KIRDIĞI TEST SAYISI DEĞİL, KIRDIĞI TESTİN NE OLDUĞUDUR.** M6-12 A kartın istediği `CHECK` kısıtını ölçerek eledi: deaktivasyon yarısı **12 ihlal / 15 kırmızı test**, aktivasyon yarısı **201 / 215** (≈17× fark) — güçlü ama **niceliksel** bir argüman. Elemeyi asıl bitiren şey sayım sırasında çıktı: **CHECK, kuralı KANITLAYAN fikstürü reddediyordu**, yani kuralı **test edilemez** kılıyordu. Üçüncü bir bulgu da eklendi: *"invited ama `activated_at` taşıyan"* satır **iki CHECK'in ikisinden de sağ çıkıyor**, yani kısıtlar çelişki sınıfını **kapsamıyor** bile. ⚠️ **Ve ilk ölçüm DÜŞÜK çıkmıştı çünkü iki yarım BİRLİKTE koşturulmuştu** — kaskad hatalar ucuz yarımı yuttu (6 sanıldı, **12**). **İki alternatifi ölçerken ayrı ayrı koştur; birlikte koşturmak baskın terimin gölgesinde kalanı gizler.** | M6-12 A, 1.–2. tur |
| **🔐 BİR YETKİ DARALTMASININ SÜTUN LİSTESİ ÖLÇÜLÜR, ÇIKARILMAZ — VE EKSİK BİR SÜTUN TÜM YAZMA YOLUNU KAPATIR.** M6-12 A'da güvenlik denetçisi gerçek bir açık buldu (`REVOKE UPDATE ON tenants` yapılmış ama **INSERT açık**: `tappa_app` `plan`/`price`'ı INSERT'te yazabiliyordu) **ve düzeltmesini ölçerek verdi**. Yapıcı uygulamadan önce **33 çağrı yerini saydı** ve listede `id`'nin **olmadığını** gördü: `tenants` politikası `id` üzerinde kapsamlanıyor, yani `id` yazamayan bir rolde `DEFAULT gen_random_uuid()` bağlamla **asla** eşleşmez → **her INSERT `WITH CHECK`'e takılır, signup tamamen kırılırdı**. Ayrıca listeye `created_at`'i **ekledi**: kendi kayıt tarihini yazabilen bir tenant **kendi bedava aylarını kaydırır** — aynı sınıf, denetçi anmamıştı. **Kural: bir GRANT/REVOKE'un pozitif kontrolü ZORUNLU — 'kapattığım şey kapandı' yetmez, 'kapatmadığım şey hâlâ çalışıyor' da ölçülür.** | M6-12 A, 5. tur |
| **🧭 BİR AĞ KENDİ SQL'İNİ REDDEDERSE, AĞ HAKLIDIR — VE BU BRIEF'TE İSTENMEMİŞ EN DEĞERLİ BULGU OLABİLİR.** M6-12 A'da yapıcı §4.5 mekanik ağını (`query_test.go`) kardeş paketlerden kopyalarken ağ **kendi yazdığı sorguları reddetti**: tenant **geçişli** bağlanıyordu (`e.tenant_id = b.id`) — doğru SQL, **zayıf kemer** (okuyucu üç CTE takip etmeli, ağ hiç göremiyor). Yeniden yazdı; denetçi **aritmetiğin değişmediğini** altı ay boyunca birebir (`1590/1590`, `1690/1690`) ve **kemerin kozmetik olmadığını** süper kullanıcıyla — RLS hiçbir şeye uygulanmazken kemerli **3**, kemersiz **43** — ölçtü. ⚠️ **Ama yeniden yazımın YAZILI GEREKÇESİ yanlıştı** (*"scope yüklemini silsem ağ yeşil kalırdı"*); denetçi şekli yeniden kurup **iki hâlde de kırmızı** olduğunu gösterdi. **Kural: kapsam dışı bir düzeltmenin gerekçesi, düzeltmenin kendisi kadar ölçülür — yoksa doğru iş yanlış defterle sevk edilir.** | M6-12 A, 2.–3. tur |
| **👨‍👩‍👧 KARDEŞ TARAMASI BİR TALİMAT OLARAK BRIEF'E KONUNCA GERÇEKTEN İŞLİYOR (DÖRDÜNCÜ TEKRAR).** M6-11'de üç kez çıkan *"kusuru düzeltip kardeşlerini aramamak"* deseni M6-12 A'da **dördüncü** kez çıktı ve en net hâliyle: yapıcı 1. turda oynak bir sayıyı migration'dan temizledi, **aynı dosyanın 100 satır aşağısındaki aynı sınıfa bakmadı** (`created_at` seed'de `now() - interval '90 days'` olduğu hâlde *"2026-05-14'te kaydoldu"* yazılıydı) — üstelik **doğru cümleyi başka bir yeni dosyaya zaten yazmıştı**. 4. turda brief'e *"düzeltmeden ÖNCE kardeş taraması yap ve taramanın KOMUTUNU teslimata yaz"* konunca tarama, denetçinin **görmediği üçüncü** vakayı da buldu. **Kural: 'kardeşlerini ara'yı brief'e bir ADIM olarak yaz, bir temenni olarak değil — ve çıktısını komutla iste.** | M6-12 A, 1.–4. tur |
| **🪤 ORKESTRATÖR DE PROJENİN KENDİ BELGELENMİŞ TUZAĞINA DÜŞER.** M6-12 A kapanışında gateleri kendim doğrularken çıplak `go test -race -count=1 -v ./...` koştum ve **396 SKIP** aldım — `DATABASE_URL` yokken DB testleri **sessizce** atlanıyor, yani "yeşil" gördüğüm şey yarım suite'ti. Bu tuzak `state.md`'de **üç görev boyunca** yazılıydı ve M6-11'de **üç denetçiyi** yanıltmıştı. Env yüklenince **2865 / 0 FAIL / 0 SKIP**. **Ders: bir tuzağı brief'e yazmak, kendini ondan muaf kılmaz — SKIP sayısını HER ZAMAN yazdır, sıfır olduğunu görmeden 'yeşil' deme.** | M6-12 A kapanışı |
